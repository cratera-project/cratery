---
id: macro-supporter-7
categorySlug: macros
title: "Macro Export Scope"
difficulty: 2
tags: [macros, macro_export, modules]
---

# Prompt
What is the effect of placing `#[macro_export]` on a `macro_rules!` definition?

# Code
```rust
#[macro_export]
macro_rules! global_macro {
    () => { println!("global"); };
}
```

# Options
- [ ] A) Compiles the macro into a dynamic shared C library at build time in runtime memory
- [x] B) Places the macro at the crate root and makes it accessible to downstream crates
- [ ] C) Disables all macro hygiene rules for identifiers generated by the macro in runtime memory
- [ ] D) Converts the macro into a procedural derive macro automatically within local thread memory

# Hint
#[macro_export] exposes the macro at the root of the crate for external users.

# Explanation
`#[macro_export]` lifts the macro into the crate root module (`$crate::global_macro`) and exports it so external dependent crates can use it.
