/** Generated by scripts/generate-builtin-answers.mjs — do not edit. */
export type BuiltinAnswer = {
  correctIndex: number
  categorySlug: string
  explanation: string
}

export const BUILTIN_ANSWERS: Record<string, BuiltinAnswer> = {
  "borrow-aliasing-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`first` holds an immutable borrow into `v`. `push` needs `&mut v` and may reallocate, invalidating that pointer. The borrow checker forbids overlapping shared and exclusive access so this potential use-after-free never compiles."
  },
  "borrow-lifetime-link-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`x` is dropped at the end of the inner block, so `r` would dangle. Moving `x` into the outer scope keeps the referent alive for as long as `r` is used. Lifetime annotations describe relationships; they do not create storage."
  },
  "borrow-slice-invalidation-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "A `&str` points into the `String`’s heap buffer. `push_str` may reallocate that buffer, leaving the slice dangling. The borrow checker blocks the mutable access while the slice borrow is still live."
  },
  "borrow-split-borrow-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Multiple `&mut` are rejected when they alias. `split_at_mut` guarantees the two slices cover disjoint ranges, so both can be mutable at once. The compiler accepts this safe API as proving non-aliasing."
  },
  "borrow-nll-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "With non-lexical lifetimes (the default since Rust 2018), a borrow ends at its last use. Here `r` is last used in `println!`, so the shared borrow is over before `push_str` needs `&mut s`."
  },
  "borrow-reborrow-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "You may reborrow immutably from a `&mut`, but while that reborrow is live you cannot mutate through the original. Because `r2` is used later in `println!`, `*r1 += 1` overlaps a live shared borrow and is rejected."
  },
  "borrow-loop-scope-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Each `&v[i]` is stored in `refs`, and `refs` is used after `v.push(4)`, so the shared borrows stay live across the push. `push` needs exclusive access and conflicts with those borrows. If `refs` were never used again, NLL could end the borrows earlier and the push would compile."
  },
  "borrow-temporary-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`String::from(\"hello\")` is a temporary. `as_str()` returns a `&str` into that temporary, which is dropped at the end of the statement. Binding `r` for a later statement would create a dangling reference, so Rust rejects it."
  },
  "borrow-conditional-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "Both branches borrow `x` immutably into `r`. That borrow only needs to last until `r`’s last use (`println!`). Afterward NLL ends it, so `x += 1` is allowed."
  },
  "borrow-method-call-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`len()` takes `&self` only for the call. Once it returns an `usize`, no borrow of `s` remains, so `push_str` can take `&mut s`. The code compiles."
  },
  "borrow-exclusive-mut-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Rust allows either one `&mut` or any number of `&`, but not two overlapping `&mut` to the same place. Both `r1` and `r2` are used in `println!`, so both would be live together."
  },
  "borrow-fn-param-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`first` still borrows `data` when `add(&mut data)` runs. That call needs exclusive access (and may reallocate), so the overlapping borrows are rejected. Dropping `first` before the call would fix it."
  },
  "borrow-shared-ok-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Any number of immutable (`&`) borrows may exist at once because they cannot mutate. The exclusivity rule applies to `&mut`. Here both `r1` and `r2` are shared, so the code is fine."
  },
  "borrow-index-mut-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`v[i]` desugars to an `Index`/`IndexMut` call that borrows the entire vector. The compiler cannot see that `0` and `1` are disjoint the way `split_at_mut` promises, so the second `&mut v[1]` conflicts with `a`."
  },
  "borrow-return-local-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "`s` is a local; it is dropped when `make` returns. Returning `&s` would hand the caller a dangling pointer. Return an owned `String` (or borrow from a parameter / `'static` data) instead."
  },
  "borrow-iter-mut-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`for x in &v` keeps an immutable borrow of `v` alive for the loop. `v.push` needs a mutable borrow of the same vector, which conflicts. Collect indices/values first, or use other patterns that don’t alias mutably while iterating."
  },
  "borrow-match-scrutinee-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`map.get` returns a reference into the map. While `v` is still live (used by `println!`), `insert` cannot take `&mut map`. Drop/end the borrow first, or use the `entry` API for check-then-insert patterns."
  },
  "borrow-deref-mut-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "The borrow checker understands disjoint field paths: `&mut p.a` and `&mut p.b` do not alias. Borrowing `p` as a whole (`&mut p`) would conflict with either field borrow."
  },
  "borrow-closure-capture-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "`r` holds an immutable borrow of `s`. The closure captures `s` mutably to call `push_str`, which needs exclusive access. Those borrows overlap, so the code is rejected."
  },
  "borrow-slice-split-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "You cannot take two `&mut` borrows of overlapping parts of the same slice via ordinary indexing. `split_at_mut` (implemented with `unsafe` internally, safe API) returns disjoint halves the borrow checker accepts."
  },
  "borrow-static-local-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`'static` references must be valid for the whole program. A local `String` is freed when `main` ends, so `&owned` is temporary. String literals and leaked/`Box::leak` data can be `'static`."
  },
  "borrow-disjoint-capture-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "Edition 2021 disjoint capture (RFC 2229) lets a closure capture only `p.x`. That leaves `p.y` free to borrow. In 2018 the closure would capture all of `p` and this would not compile. `let _ = &p;` inside the closure forces capturing the whole binding if you need the old behavior."
  },
  "borrow-match-ergonomics-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "Match ergonomics default-binds against a referenced scrutinee: `x` is `&Option<String>`, so `Some(s)` gives `s: &String` (equivalent to `Some(ref s)` on `*x`). Moving a `String` out of `&Option<String>` is not allowed. `s` is not `&str` unless you add a further deref/coercion in the pattern or body."
  },
  "borrow-two-phase-reservation-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Rust's non-lexical lifetimes (NLL) support two-phase borrows for method calls. In `list.push(list.len())`, the mutable borrow of `list` for `.push(...)` starts in a 'reserved' state, permitting shared borrows like `list.len()` during argument evaluation. The exclusive borrow only activates once argument evaluation completes and the method is invoked."
  },
  "borrow-closure-disjoint-capture-edition-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "In Rust 2021 and 2024 editions, closures capture individual fields rather than whole struct instances if only specific fields are referenced. Because `mutate_x` only mutates `p.x`, only `&mut p.x` is captured. Field `p.y` remains unborrowed, allowing `println!(\"y is {}\", p.y)` to safely access `p.y` concurrently with the closure's existence."
  },
  "borrow-supporter-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "Under Two-Phase Borrows, calling `vec.push(vec.len())` initially takes a \"reserved\" mutable borrow of `vec` that permits shared reads (like `vec.len()`) during argument evaluation, activating as exclusive only when `push` is called."
  },
  "borrow-supporter-2": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Under NLL, a reference's lifetime ends at the point of its last actual use in the control flow graph, allowing mutations like `data.push(4)` even before the block scope closes."
  },
  "borrow-supporter-3": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`slice[i]` invokes `IndexMut::index_mut(&mut self, i)`, mutably borrowing the entire slice. The borrow checker cannot verify disjointness of indices at compile time, so `split_at_mut` uses unsafe internally to provide safe disjoint slices."
  },
  "borrow-supporter-4": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Under Match Ergonomics, when matching on a reference (like `&Option<T>`), pattern bindings automatically inherit the reference mode, treating `Some(x)` as `Some(ref x)` without requiring explicit `ref` syntax."
  },
  "borrow-supporter-5": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "Because `helper(&mut self)` takes an exclusive borrow of the whole `Worker` struct, it conflicts with any active borrow of individual fields (`&self.items`). Factoring the helper to take only `&mut self.count` resolves the conflict."
  },
  "borrow-supporter-6": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "When passing a mutable reference `r` to a function expecting `&mut T`, Rust automatically inserts a reborrow `&mut *r`. The reborrow is active only for the duration of the call, leaving `r` valid afterwards."
  },
  "borrow-supporter-7": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "Rust's core safety invariant states: for any memory location, you can have any number of immutable shared references (`&T`), OR exactly one exclusive mutable reference (`&mut T`), but never both simultaneously."
  },
  "borrow-supporter-8": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Iterating with `for x in &vec` holds a shared borrow of `vec`. Calling `vec.push(...)` requires an exclusive `&mut vec` borrow because `push` might reallocate the heap buffer, which would leave `x` pointing to deallocated memory."
  },
  "borrow-supporter-9": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`RefCell` enforces the borrow rules dynamically using an internal counter. If you attempt to acquire a mutable borrow while a shared borrow is active, `RefCell::borrow_mut` panics immediately."
  },
  "borrow-supporter-10": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "The temporary `Resource(\"data\")` created in the scrutinee of the `match` expression lives until the end of the entire `match` block. The drop runs after `\"matched\"` is printed."
  },
  "borrow-supporter-11": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "By using `move`, the closure takes ownership of `x` by value. Because the closure contains no borrowed references to the stack frame of `make_fn`, it can be returned and live for 'static."
  },
  "borrow-supporter-12": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`ref first` creates a shared borrow `&pair.0` instead of moving the `String`. Because `pair.0` was not moved, `pair.0` remains fully initialized and accessible after the `match`."
  },
  "borrow-supporter-13": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "Because `len(&self) -> usize` returns a primitive `usize` that contains no references connected to `&self`, the shared borrow of `l` ends immediately when `len()` finishes executing."
  },
  "borrow-supporter-14": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Types offering interior mutability (like `RefCell`, `Cell`, `Mutex`) wrap `UnsafeCell<T>`, allowing mutation through `&self` by enforcing aliasing rules dynamically at runtime rather than statically at compile time."
  },
  "borrow-supporter-15": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "Passing individual fields (e.g. free functions taking `&mut canvas.pixels` and `canvas.width`) allows the borrow checker to verify that distinct fields are accessed without locking the entire struct."
  },
  "borrow-supporter-16": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "In Rust, all references (`&T` and `&mut T`) are guaranteed by the language definition to be non-null, properly aligned, and pointing to valid memory for their entire lifetime."
  },
  "borrow-supporter-17": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`split_first_mut` reborrows `&mut *slice` into `(&mut head, &mut tail)`. Reassigning `slice = tail` narrows the scope of `slice` to the remaining elements without violating borrow rules."
  },
  "borrow-supporter-18": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Local variables drop in reverse order of declaration (LIFO). `_b` (which borrows `a`) is dropped first, ensuring `a` is still valid when `_b`'s destructor runs."
  },
  "borrow-supporter-19": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "Any variable held across an `.await` point becomes a field in the compiler-generated `Future` generator struct. If that variable is not `Send` (like a `MutexGuard` or raw pointer), the entire `Future` becomes `!Send`."
  },
  "borrow-supporter-20": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`ref s` creates a reference `&opt` rather than moving `opt`. Because no move occurred, `opt` remains fully initialized and accessible in both branches and after the `if let`."
  },
  "borrow-supporter-21": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`std::thread::spawn` requires `F: 'static` because the spawned thread can execute for an arbitrary duration and might outlive the stack frame where `s` resides. Use `thread::scope` for non-`'static` borrows."
  },
  "borrow-supporter-22": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "&'a mut T is covariant with respect to 'a (while invariant with respect to T). Therefore, a longer lifetime 'b can be shortened to a shorter lifetime 'a when passing or returning."
  },
  "borrow-supporter-23": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "The temporary `PrintOnDrop(\"A\")` is created for the statement `inspect(&PrintOnDrop(\"A\"));` and dropped at the terminating semicolon, printing `\"A\"` before `\"B\"`."
  },
  "borrow-supporter-24": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`Option::as_mut(&mut self)` converts `&mut Option<T>` into `Option<&mut T>`, allowing callers to pattern match and mutate the inner value directly in place."
  },
  "borrow-supporter-25": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`split_at_mut` splits a mutable slice into two disjoint mutable sub-slices `(&mut [T], &mut [T])` whose memory ranges do not overlap, satisfying the borrow checker."
  },
  "borrow-supporter-26": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Because `Cell::get_mut` takes `&mut self`, the caller has statically proven unique exclusive ownership of the `Cell`. No aliasing is possible, so returning `&mut T` directly is 100% sound."
  },
  "borrow-supporter-27": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "The pattern `&a` matches against the reference `&x: &i32`, unwrapping the reference layer and binding `a` directly to the `i32` value (copying the integer)."
  },
  "borrow-supporter-28": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`&mut *b` triggers a `DerefMut` coercion: `DerefMut::deref_mut(&mut b)`, reborrowing the heap-allocated memory as an exclusive `&mut str` without moving the `Box`."
  },
  "borrow-supporter-29": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "Because `let r = &data;` is inside the loop body, `r` is created and dropped on each iteration. Its borrow does not outlive that single iteration, so subsequent iterations start fresh."
  },
  "borrow-supporter-30": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "`Option::as_ref(&self)` converts `&Option<T>` into `Option<&T>`, making it easy to match against `Some(s)` where `s` is `&T` without consuming or moving the original `Option`."
  },
  "borrow-nll-two-phase-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Under two-phase borrows, `&mut v` is reserved for `push()`, allowing shared read `v.len()` in arguments before activating the mutable borrow upon entry."
  },
  "borrow-reborrow-mut-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "Reborrowing a mutable reference creates a temporary child borrow. The parent borrow cannot be used until the child borrow expires."
  },
  "borrow-cell-borrow-mut-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "`RefCell` enforces aliasing rules dynamically. Attempting to acquire `borrow_mut()` while an active `Ref` exists panics with `AlreadyBorrowed`."
  },
  "borrow-entry-api-vacant-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "The `Entry` API borrows the map once to locate the key slot and allows direct conditional insertion or in-place mutation without borrow conflicts."
  },
  "borrow-closure-capture-disjoint-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "Since Rust 2021, closures only capture the specific struct fields they access, allowing other fields to be borrowed concurrently elsewhere."
  },
  "borrow-split-at-mut-slice-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "`split_at_mut` uses safe encapsulation over unsafe pointers to split one `&mut [T]` into two non-overlapping mutable slices that can be modified concurrently."
  },
  "borrow-stacked-borrows-tree-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Stacked Borrows is an operational semantics model for Rust defining pointer provenance and aliasing rules, ensuring compiler optimizations (like `noalias`) remain sound."
  },
  "borrow-deref-mut-coercion-1": {
    "correctIndex": 3,
    "categorySlug": "borrow-checker",
    "explanation": "Rust automatically coerces `&mut T` to `&mut U` if `T: DerefMut<Target = U>`, allowing methods on the inner target to be invoked directly on smart pointer wrappers."
  },
  "borrow-slice-split-mut-chunks-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`chunks_mut` returns an iterator yielding disjoint mutable sub-slices `&mut [T]`, allowing distinct chunks of the same parent slice to be mutated without aliasing."
  },
  "borrow-pin-as-mut-reborrow-1": {
    "correctIndex": 2,
    "categorySlug": "borrow-checker",
    "explanation": "`Pin::as_mut` reborrows the inner `&mut T` into a new `Pin<&mut T>` with a shorter lifetime, allowing multiple pinned method calls without consuming the outer `Pin`."
  },
  "borrow-cell-swap-in-place-1": {
    "correctIndex": 0,
    "categorySlug": "borrow-checker",
    "explanation": "`Cell<T>` never exposes references `&T` or `&mut T` to its contents; `swap` performs an in-place value swap on stack/heap without creating intermediate aliasing references."
  },
  "borrow-closure-immut-mut-capture-1": {
    "correctIndex": 1,
    "categorySlug": "borrow-checker",
    "explanation": "Rust infers capture modes automatically: if the closure body only reads the variable, it borrows by shared reference `&T`; if it mutates the variable, it borrows by unique reference `&mut T`."
  },
  "conc-thread-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "Both threads are runnable concurrently. The OS scheduler chooses when each runs, so `\"A\"` may print before or after `\"B\"`. `join()` only waits for the spawned thread to finish; it does not fix relative ordering of earlier prints."
  },
  "conc-move-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`std::thread::spawn` needs `Send + 'static`. A borrow of `v` would not outlive the caller safely. `move` transfers ownership of `v` into the closure so the new thread owns the data."
  },
  "conc-channel-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "For non-`Copy` types, `Sender::send` moves the value into the channel. The sender cannot use `val` afterward; `recv` gives ownership to the receiver. `Copy` types are copied instead."
  },
  "conc-mutex-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`Mutex<T>` is a single-owner value. `Arc` provides shared ownership of that mutex so each thread can hold a clone and lock the same guard. `Mutex` is already `Send`/`Sync` when `T: Send`; `Arc` is about ownership, not replacing the lock."
  },
  "conc-send-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`Send` means it is safe to move ownership of a value to another thread. `Sync` is about sharing `&T` across threads. Types like `Rc<T>` are not `Send` because their refcounts are not atomic."
  },
  "conc-sync-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`thread::scope` joins all spawned threads before returning, so borrows of stack data are sound. Sharing `&Vec<i32>` across threads also requires `Vec<i32>: Sync`, which holds for `i32`."
  },
  "conc-static-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`thread::spawn` requires `F: Send + 'static` because the thread can outlive the caller. Borrowing `s` would be a non-`'static` capture. Fix with `move`, owned data, or `thread::scope`."
  },
  "conc-scope-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "Scoped threads are joined before `thread::scope` returns, so borrowing parent-stack data is safe. They do not guarantee scheduling order or fairness of locks."
  },
  "conc-mutexguard-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`lock()` returns a `MutexGuard`. The mutex stays locked until that guard is dropped: end of scope or an explicit `drop(guard)`. Reading through the guard does not unlock early."
  },
  "conc-mpsc-iter-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`Receiver`'s iterator blocks waiting for the next message. When every `Sender`/`SyncSender` is dropped, the channel hangs up and the iterator ends with no panic and no spin loop."
  },
  "conc-condvar-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`wait` atomically releases the mutex and blocks. On wake it reacquires the lock and returns a new `MutexGuard`. That is why the `while !*ready` loop can recheck the condition safely. Spurious wakeups are why the loop is needed."
  },
  "conc-parking-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`thread::park` blocks until the thread is unparked (or already has a park token). With no `unpark` and `main` blocked in `join`, the program hangs. Prefer channels or `Condvar` for most coordination."
  },
  "conc-rwlock-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`RwLock` allows multiple concurrent readers or one writer. `Mutex` always grants exclusive access, even for read-only use. Neither removes deadlock risk if lock order is wrong."
  },
  "conc-barrier-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "A barrier blocks each caller of `wait` until `N` threads have arrived. Then all are released together. One thread's `BarrierWaitResult` reports `is_leader()`, but leadership is not the main guarantee."
  },
  "conc-poisoned-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "If a thread panics while holding a `Mutex`, the mutex is poisoned. Later `lock()` calls return `Err(PoisonError<_>)` so callers can decide whether to recover with `into_inner` or propagate failure."
  },
  "conc-atomic-ordering-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`Relaxed` operations are still indivisible, but they do not create happens-before edges with other memory ops. Use stronger orderings (or fences) when one thread must publish data another must observe."
  },
  "conc-mpsc-clone-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "Standard library `mpsc` channels are multiple-producer, single-consumer. Cloning `Sender` supports extra producers; a single `Receiver` keeps consume-once semantics. `Sender` is `Clone`, not `Copy`."
  },
  "conc-once-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "After a successful `call_once`, the closure does not run again; other callers wait for completion then proceed. Prefer `OnceLock`/`LazyLock` for storing an initialized value. A panic during init poisons the `Once`."
  },
  "conc-thread-local-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`thread_local!` gives each thread a separate `COUNTER`. Setting it in `main` does not affect the spawned thread, which still starts at `0`. There is no data race; the cells are distinct."
  },
  "conc-scope-return-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "Scoped threads cannot outlive the `thread::scope` call, so a reference into `s` remains valid until `join` returns inside the scope. Plain `thread::spawn` would require `'static` and reject this borrow."
  },
  "conc-join-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`join` waits for the thread and returns `Result<T, _>` where `T` is the closure’s return type. Panic in the child becomes `Err`. It does not borrow the child’s stack."
  },
  "conc-move-closure-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`thread::spawn` requires a `'static` closure. `move` forces captures by value so the thread owns what it needs instead of borrowing the caller’s stack. Scoped threads can borrow without `'static`."
  },
  "conc-channel-drop-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "When every `Sender` is dropped, `recv` unblocks with `Err(RecvError)` so consumers can stop cleanly. It does not hang forever or fabricate values."
  },
  "conc-mutex-poison-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "If a thread panics while holding a `MutexGuard`, the mutex becomes poisoned. Later `lock` calls return `Err(PoisonError<_>)` so you can decide whether to recover with `into_inner` or propagate."
  },
  "conc-lazylock-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`LazyLock` (stable since 1.80) runs its closure once on first deref or `force()`, even if several threads race; others wait. Later accesses reuse the stored value. Unlike `OnceLock`, the initializer is fixed at `new` and you do not pass extra arguments later. A panic in the closure poisons the lock unrecoverably."
  },
  "conc-sync-channel-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`sync_channel(bound)` is a bounded channel: `send` returns immediately while space remains, then blocks until a `recv` frees a slot. `bound == 0` is a rendezvous channel. Order is still FIFO, `SyncSender` can be cloned, and there is still a single `Receiver`, like `channel()`."
  },
  "conc-scoped-threads-borrow-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`std::thread::scope` creates a scoped thread environment where all threads spawned via `s.spawn` are guaranteed to terminate and join before the scope closure returns. Because the threads cannot outlive the stack frame, they can safely borrow non-`'static` stack data without `Arc` or `Mutex`. Once the scope block finishes, exclusive access to `numbers` is restored."
  },
  "conc-compare-exchange-order-rule-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "In Rust's atomic `compare_exchange`, the failure ordering specifies the memory ordering for the load operation performed when the comparison fails. A failed `compare_exchange` only reads memory; it does not write or release any values. Therefore, the failure ordering cannot be `Release` or `AcqRel`, and cannot be stronger than the success ordering. Passing `Release` panics at runtime."
  },
  "conc-rwlock-read-recursion-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`std::sync::RwLock` allows multiple concurrent read locks as long as no writer holds or waits for an exclusive lock. Here, both `r1` and `r2` obtain shared access on the same thread without issue, and `*r1 + *r2` evaluates to 10. Note that recursive read locks can still deadlock if another thread queues a pending write lock in between."
  },
  "conc-supporter-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "A failed `compare_exchange` performs only a load (read) of the current value. Because no store (write) occurs on failure, it cannot establish a \"release\" synchronization relationship."
  },
  "conc-supporter-2": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "On Load-Linked/Store-Conditional architectures (ARM, RISC-V), `compare_exchange_weak` maps directly to single LL/SC pairs without wrapping them in an inner loop to handle spurious failures, making it faster when already inside a loop."
  },
  "conc-supporter-3": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`thread::scope` joins all threads spawned within the scope before returning. This guarantees that all threads finish before the stack frame of the enclosing function is popped, making borrowing stack data 100% safe."
  },
  "conc-supporter-4": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "If a thread holding a `MutexGuard` panics, the `Mutex` is flagged as poisoned. Future calls to `lock()` return `Err(PoisonError<MutexGuard>)`, allowing other threads to detect potentially inconsistent state or recover via `into_inner()`."
  },
  "conc-supporter-5": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`LazyLock` takes an initialization closure at declaration and computes the value upon first dereference (`Deref`). `OnceLock` is manually written to with `get_or_init` or `set`."
  },
  "conc-supporter-6": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "An `Acquire` load synchronizes-with a `Release` store on the same atomic variable. Any memory operations preceding the `Release` store in program order become visible to the thread that observes the store via an `Acquire` load."
  },
  "conc-supporter-7": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "Condition variables can wake up spuriously (without any thread calling `notify_one` or `notify_all`) due to operating system scheduling details. The predicate condition must always be rechecked in a `while` loop."
  },
  "conc-supporter-8": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "If reader threads acquire shared locks in an overlapping fashion, writer threads attempting to acquire an exclusive lock may be starved indefinitely (\"writer starvation\") depending on the OS scheduler and lock implementation."
  },
  "conc-supporter-9": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`Arc` (\"Atomic Reference Counted\") uses atomic fetch-add and fetch-sub instructions to safely update its reference count from multiple threads concurrently, whereas `Rc` uses non-atomic arithmetic and is not thread-safe."
  },
  "conc-supporter-10": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`Arc::make_mut` checks if the `Arc` is uniquely owned. If `strong_count > 1`, it clones the inner data into a fresh unique `Arc` allocation and returns a mutable reference `&mut T` to the private copy."
  },
  "conc-supporter-11": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "When all `Sender` instances for an `mpsc` channel are dropped, the channel is closed. Any pending messages in the buffer are yielded first; once empty, `rx.recv()` returns `Err(RecvError)`."
  },
  "conc-supporter-12": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "In Rust, `T: Sync` is defined as: `&T: Send`. In other words, a type `T` is `Sync` if and only if a shared reference `&T` can safely be sent across thread boundaries."
  },
  "conc-supporter-13": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`Cell` and `RefCell` provide interior mutability via shared references (`&Cell<T>`) without using atomic operations or mutexes. Sharing `&Cell<T>` across multiple threads would cause unsynchronized concurrent writes (data races)."
  },
  "conc-supporter-14": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`Ordering::SeqCst` enforces a single, globally agreed-upon total execution order of all `SeqCst` operations across all threads in the system, preventing reordering that could otherwise appear in Acquire/Release."
  },
  "conc-supporter-15": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`thread_local!` static variables are initialized lazily per-thread the first time that specific thread accesses the variable using `.with(...)`. Each thread maintains its own independent copy."
  },
  "conc-supporter-16": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`Ordering::Relaxed` guarantees only that the atomic operation itself is atomic (no tearing) and that all threads agree on a single modification order for that specific atomic variable, with zero cross-variable synchronization or ordering."
  },
  "conc-supporter-17": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "Acquiring the lock requires `Ordering::Acquire` so subsequent critical section memory operations cannot be reordered before the lock. Releasing requires `Ordering::Release` so critical section writes become visible."
  },
  "conc-supporter-18": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`std::hint::spin_loop()` emits processor-specific spin-wait hints (such as `PAUSE` on x86 or `YIELD` on ARM), preventing memory order violation pipeline stalls and saving CPU power."
  },
  "conc-supporter-19": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "Standard `std::sync::Mutex` is non-reentrant. Attempting to acquire the lock a second time from the same thread causes a permanent deadlock waiting for itself to release the lock."
  },
  "conc-supporter-20": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`sync_channel(bound)` creates a bounded channel. When the queue reaches capacity (`bound`), further calls to `tx.send(item)` block the sending thread until a receiver pulls an item with `recv()`."
  },
  "conc-supporter-21": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`UnsafeCell<T>` implements `Send` if `T: Send`. Moving ownership of an `UnsafeCell` across threads transfers exclusive ownership of `T`, which is safe whenever `T` can be sent."
  },
  "conc-supporter-22": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`std::sync::atomic::AtomicPtr<T>` provides atomic load, store, swap, and compare-and-swap operations on `*mut T` pointers across threads."
  },
  "conc-supporter-23": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "A `Barrier` enables multiple threads to synchronize the beginning of some computation. Calling `barrier.wait()` blocks the thread until exactly `N` threads have called `wait()`, at which point all threads are released simultaneously."
  },
  "conc-supporter-24": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "False sharing occurs when variables modified by different cores reside on the same cache line (typically 64 bytes), causing unnecessary cache invalidations. Using `#[repr(align(64))]` ensures each variable occupies its own cache line."
  },
  "conc-supporter-25": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`AtomicBool::fetch_or(val, ...)` performs a bitwise OR operation and returns the *previous* boolean value that was contained in the atomic before the operation."
  },
  "conc-supporter-26": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`Arc::downgrade` creates a `Weak<T>` pointer that tracks reference counts without preventing the inner value from being dropped when all `Arc` instances go out of scope, preventing memory cycles."
  },
  "conc-supporter-27": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "Some 32-bit processors do not support hardware 64-bit atomic instructions. On such architectures, `AtomicI64` is omitted or requires library emulation, while `AtomicIsize` is guaranteed to match the pointer width."
  },
  "conc-supporter-28": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "Many operating systems (including POSIX pthread mutexes) mandate that a mutex must be unlocked by the exact same thread that locked it. Therefore, `MutexGuard` is `!Send` to prevent releasing the lock on a different thread."
  },
  "conc-supporter-29": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`std::sync::atomic::fence(ordering)` inserts a memory barrier that synchronizes memory accesses without operating directly on a specific atomic variable."
  },
  "conc-supporter-30": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "`std::thread::Builder` provides fine-grained control over thread attributes, allowing callers to set the thread name (`name`) and stack size (`stack_size`) before spawning."
  },
  "conc-supporter-31": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`AtomicUsize` compiles directly to single hardware atomic CPU instructions (e.g. `LOCK XADD` on x86), avoiding OS syscalls, thread parking, and context switches involved in `Mutex`."
  },
  "conc-supporter-32": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "When `strong_count` drops to 0, the inner `T` is destroyed (`Drop::drop`). However, the memory block containing the reference counters remains allocated until all `Weak` references are dropped (`weak_count == 0`)."
  },
  "conc-supporter-33": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`std::thread::park()` blocks the current thread until its token is made available via `handle.thread().unpark()`. Unpark tokens are saturated (at most 1), avoiding lost wakeups if `unpark` is called before `park`."
  },
  "conc-supporter-34": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "The ABA problem occurs when a memory location is read as `A`, modified to `B` by another thread, and then modified back to `A`. A subsequent CAS succeeds because the value is `A`, even though internal state or pointer targets may have changed."
  },
  "conc-supporter-35": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "Because raw pointers bypass Rust's borrow checker (allowing unconstrained aliasing, mutation, and nullability), the compiler cannot verify thread-safety invariants. Developers must manually use `unsafe impl Send` / `Sync` after verifying safety."
  },
  "conc-sync-send-raw-ptr-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "Raw pointers do not implement `Send` or `Sync` because the compiler cannot verify aliasing or safety across threads. Implementing them requires explicit `unsafe impl`."
  },
  "conc-condvar-wait-mutex-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`Condvar::wait` atomically unlocks the associated `MutexGuard` and blocks the calling thread, re-acquiring the lock before returning when notified."
  },
  "conc-atomic-fetch-update-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`fetch_update` repeatedly loads the current value, computes the new value via closure, and performs `compare_exchange_weak` in a lock-free CAS loop until successful."
  },
  "conc-rwlock-upgrade-deadlock-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "If multiple threads holding shared read locks attempt to upgrade to exclusive write access, they will mutually wait for each other to release the read lock, causing deadlock."
  },
  "conc-once-cell-lazy-init-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "`OnceLock::get_or_init` guarantees that even under concurrent multi-threaded access, the initialization closure is executed at most once, safely sharing the result."
  },
  "conc-atomic-ordering-release-acquire-1": {
    "correctIndex": 2,
    "categorySlug": "concurrency",
    "explanation": "A store with `Ordering::Release` synchronizes-with a load with `Ordering::Acquire` that reads the stored value, ensuring all prior writes are visible to the acquiring thread."
  },
  "conc-scoped-threads-stack-borrow-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "`thread::scope` guarantees that all spawned threads complete before the scope exits, ensuring any stack references borrowed by threads remain valid for their entire execution."
  },
  "conc-mutex-poisoning-unwind-panic-1": {
    "correctIndex": 1,
    "categorySlug": "concurrency",
    "explanation": "When a thread panics while holding a `MutexGuard`, the `Mutex` becomes \"poisoned\". Subsequent `lock()` attempts return `Err(PoisonError)`, signaling potential invariant corruption."
  },
  "conc-atomic-fence-synchronization-1": {
    "correctIndex": 3,
    "categorySlug": "concurrency",
    "explanation": "`atomic::fence` establishes synchronization and memory ordering constraints between threads without performing an atomic operation on a specific memory location."
  },
  "conc-channel-disconnection-hang-1": {
    "correctIndex": 0,
    "categorySlug": "concurrency",
    "explanation": "When all `Sender` handles have dropped and the queue is empty, `rx.recv()` returns `Err(RecvError)`, indicating the channel is disconnected."
  },
  "err-result-vs-option-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "Use `Option` when absence is a normal outcome (not found). Use `Result` when the caller may need to distinguish failure modes (I/O error vs not found, parse error, …). They are not performance synonyms."
  },
  "err-question-operator-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "In a function returning `Result` (or `Option`), `?` early-returns the failure to the caller, optionally via `From`. `unwrap`/`expect` panic on `Err`/`None`. Prefer `?` for recoverable errors."
  },
  "err-recoverable-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "Missing or invalid config is typically recoverable: return `Result` and let the caller retry, prompt, or fall back. `panic!` is for broken invariants/programmer errors. Silent defaults hide failures unless that is an explicit policy."
  },
  "err-custom-types-1": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "A library error type can represent every failure mode you expose, often with `From` impls (or `thiserror`) for ergonomic `?`. `io::Error` can be returned, but it cannot express parse/config variants cleanly by itself."
  },
  "err-option-chaining-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "For a linear transform plus default, combinators are concise and equivalent in meaning to `match`. They do not weaken borrowing, are not always faster, and `match` remains fully supported, often clearer for multi-branch logic."
  },
  "err-from-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`?` on `Result` returns early on `Err` after applying `From::from` to the error. That is how `io::Error` becomes `MyError` here. The `Ok` types still need to be compatible with the function's `Ok` type."
  },
  "err-anyhow-vs-thiserror-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "Libraries usually return structured error types so dependents can match and react. `anyhow`/`eyre`-style boxed errors are popular in applications and binaries. `String` errors lose structure; panics are not an API for expected failures."
  },
  "err-option-question-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "For `Option`, `?` early-returns `None` when the value is `None`. It does not panic, invent `Err`, or default to zero. Mixing `Option`/`Result` with `?` needs a compatible return type or conversion (`ok_or`, etc.)."
  },
  "err-map-err-1": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`map_err` applies a function to `Err(e)` and leaves `Ok(v)` unchanged, useful before `?` or when adapting error types. `map` adjusts the success side; `ok`/`err` convert to `Option`."
  },
  "err-unwrap-or-else-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`unwrap_or(default)` evaluates `default` eagerly even on `Some`/`Ok`. `unwrap_or_else(f)` calls `f` only for `None`/`Err`, which matters for expensive or side-effecting defaults. Both exist on stable."
  },
  "err-and-then-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`map` wraps the closure’s return in `Ok`/`Some`. `and_then` expects the closure to return a `Result`/`Option` and flattens, so you avoid `Result<Result<_,_>,_>`. Errors short-circuit without calling the closure."
  },
  "err-ok-or-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`ok_or(err)` maps `Some(v)` → `Ok(v)` and `None` → `Err(err)`. That bridges lookup APIs (`Option`) into fallible APIs (`Result`) so `?` can propagate a typed failure."
  },
  "err-transpose-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`transpose` flips nesting: `Some(Ok(v))` → `Ok(Some(v))`, `Some(Err(e))` → `Err(e)`, `None` → `Ok(None)`. Handy after combinators that produce the “wrong” outer type for `?`."
  },
  "err-inspect-err-1": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`inspect_err` borrows the error for a side effect (log, metric) and returns the original `Result` unchanged. It does not recover, convert, or panic. Prefer it over `map_err` when you only need observation."
  },
  "err-try-from-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`From`/`Into` must always succeed. Use `TryFrom`/`TryInto` when some inputs are invalid (range, parse shape) and callers should handle `Err`. Infallible cases stay on `From`."
  },
  "err-bail-pattern-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "A function returning `Result` can return `Err(...)` directly. `?` is convenient for propagating from callees, but validation branches often use an explicit early `return Err`."
  },
  "err-boxed-error-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Box<dyn Error>` (or `anyhow::Error`) lets many error types convert via `?` without a unifying enum. You lose easy matching on variants, which is fine for apps but often wrong for library APIs that need structured errors."
  },
  "err-expect-1": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`unwrap` and `expect` both panic on failure. `expect(\"…\")` adds a clear message for logs and crash reports, which helps when the failure means a broken invariant rather than a handled error."
  },
  "err-try-main-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`std::process::Termination` is implemented for `()`, `ExitCode`, and `Result<T, E>` (with `T: Termination` and `E: Debug`). `Ok` becomes success; `Err` prints the error and returns a failure code. That is why `?` works in `main` on std. It is not limited to `no_std`, and `?` still returns from `main` rather than calling `exit` itself."
  },
  "err-option-try-result-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`?` uses the `Try` trait. In a `Result`-returning function it expects a `Result` (or another type whose residual converts). An `Option` residual does not convert to `Result<_, &str>`, so the compiler rejects `x?`. Convert first (`x.ok_or(\"missing\")?`) or return `Option` from `f`."
  },
  "err-question-mark-from-conversion-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "When the `?` operator encounters an `Err(e)` value, it performs an early return with `Err(From::from(e))`. Because `CustomError` implements `From<std::io::Error>`, the compiler automatically transforms the I/O error into `CustomError`, returning `Err(CustomError(\"read fail\"))`."
  },
  "err-option-flatten-nested-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Option::flatten` converts `Option<Option<T>>` into `Option<T>`. If the outer option is `Some(Some(v))`, it returns `Some(v)`. If the outer option is `Some(None)` or `None`, it returns `None`. Thus, `a.flatten()` produces `Some(10)`, while both `b.flatten()` and `c.flatten()` evaluate to `None`."
  },
  "err-supporter-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "When `?` encounters an `Err(e)`, it calls `From::from(e)` to convert the error into the return type's error variant before returning early with `return Err(From::from(e))`."
  },
  "err-supporter-2": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`Result::transpose` turns `Result<Option<T>, E>` into `Option<Result<T, E>>`. `Ok(None)` becomes `None`, `Ok(Some(x))` becomes `Some(Ok(x))`, and `Err(e)` becomes `Some(Err(e))`."
  },
  "err-supporter-3": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`Option::ok_or(err)` constructs the error value eagerly. `Option::ok_or_else(|| ...)` computes the error lazily only if the `Option` is `None`, saving unnecessary allocations."
  },
  "err-supporter-4": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Error::source(&self)` returns `Option<&(dyn Error + 'static)>`, allowing consumers to walk up the causal error chain from high-level errors to low-level root causes."
  },
  "err-supporter-5": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`panic::catch_unwind` requires the closure to implement `UnwindSafe`. This marker trait prevents types with interior mutability (like `&mut RefCell`) from being observed in a potentially corrupted state after a panic."
  },
  "err-supporter-6": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`AssertUnwindSafe<T>` is a wrapper that forces `UnwindSafe` for `T`. It is used when the developer has audited the code and verified that captured mutable state will not be used in a corrupted manner after a panic."
  },
  "err-supporter-7": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`Backtrace::capture()` captures the current execution stack frames if backtrace capture is enabled by environment variables (e.g. `RUST_BACKTRACE=1` or `RUST_LIB_BACKTRACE=1`). To force capture unconditionally, use `Backtrace::force_capture()`."
  },
  "err-supporter-8": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Result::and_then` calls a closure that takes the `Ok` value `T` and returns a new `Result<U, E>`, propagating errors if the original was `Err` or if the closure returns `Err`."
  },
  "err-supporter-9": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`Result::map_err(f)` applies a closure `f: FnOnce(E) -> F` to transform the error value if the result is `Err(E)`, leaving `Ok(T)` untouched."
  },
  "err-supporter-10": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`std::panic::set_hook` sets a custom global panic hook that receives `&PanicHookInfo`. It executes before stack unwinding begins, allowing custom crash logging, Sentry reporting, or terminal cleanup."
  },
  "err-supporter-11": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`std::convert::Infallible` is an empty enum (`enum Infallible {}`) with no variants. When used as `Result<T, Infallible>`, it statically proves to the compiler and caller that the operation never produces an error."
  },
  "err-supporter-12": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Option::unwrap_or_default()` returns the contained value if `Some(v)`, or `Default::default()` if `None`. It requires `T: Default`."
  },
  "err-supporter-13": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`Result::copied` maps a `Result<&T, E>` to `Result<T, E>` by dereferencing and copying the `T` (requires `T: Copy`)."
  },
  "err-supporter-14": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`main` can return any type that implements `std::process::Termination`. `Result<(), E>` implements `Termination`, returning exit code `0` on `Ok(())` and printing the error and exiting with code `1` on `Err(e)`."
  },
  "err-supporter-15": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`Option::flatten` converts `Option<Option<T>>` to `Option<T>`, returning `Some(v)` if both layers are `Some(Some(v))` and `None` otherwise."
  },
  "err-supporter-16": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Result::flatten` collapses `Result<Result<T, E>, E>` into a single `Result<T, E>`. If the outer is `Err(e)` or the inner is `Err(e)`, it returns `Err(e)`."
  },
  "err-supporter-17": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`std::error::Error` has `Debug` and `Display` as supertraits (`pub trait Error: Debug + Display`). Implementing `Error` requires implementing `Debug` (for developer inspection) and `Display` (for user-facing error messages)."
  },
  "err-supporter-18": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`Option::filter` returns `Some(v)` if the option is `Some(v)` and `predicate(&v)` is `true`; otherwise it returns `None`."
  },
  "err-supporter-19": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "With `panic = \"abort\"`, any panic causes the process to terminate immediately without unwinding the stack or running destructors, producing smaller binary sizes at the expense of `catch_unwind` support."
  },
  "err-supporter-20": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Option::inspect` (and `Result::inspect`) calls a closure with a shared reference to the contained value for side-effects (like logging or debugging) and passes the original `Option` through unchanged."
  },
  "err-supporter-21": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`Result::is_ok_and(f)` returns `true` if the result is `Ok(v)` and `f(v)` returns `true`; otherwise it returns `false`."
  },
  "err-supporter-22": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`Option::xor` computes logical XOR: if exactly one of `a` or `b` is `Some`, it returns that `Some`; if both are `Some` or both are `None`, it returns `None`."
  },
  "err-supporter-23": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`Result::into_ok` is implemented for `Result<T, Infallible>` (and `Result<T, !>`). Because the error variant can never exist, `into_ok()` extracts `T` safely without panicking or branch checks."
  },
  "err-supporter-24": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "The established Rust ecosystem idiom is: libraries use `thiserror` to define strongly-typed, enum-based error types that callers can match on, while application binaries use `anyhow` for flexible context wrapping and backtrace printing."
  },
  "err-supporter-25": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`Option::zip` combines two options: if `self` is `Some(a)` and `other` is `Some(b)`, it returns `Some((a, b))`; otherwise it returns `None`."
  },
  "err-supporter-26": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`Result::unwrap_err` extracts the `Err` value. If the result was actually `Ok(v)`, it panics with a message including the debug representation of `v`."
  },
  "err-supporter-27": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`Option::replace(&mut self, value)` inserts `Some(value)` into `self` and returns the old `Option<T>` that was previously held in `self`."
  },
  "err-supporter-28": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Result::as_ref` converts `&Result<T, E>` into `Result<&T, &E>`, allowing matching or inspecting inner values by reference without moving `T` or `E`."
  },
  "err-supporter-29": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`#[track_caller]` enables `std::panic::Location::caller()` to retrieve the exact source file, line number, and column number of the function's call site."
  },
  "err-supporter-30": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "In functions returning `Option`, the `?` operator extracts `v` from `Some(v)`, and returns `None` early from the enclosing function if applied to `None`."
  },
  "err-try-trait-residual-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`Try` and `FromResidual` parameterize the behavior of the `?` operator, enabling types like `Result`, `Option`, and `ControlFlow` to customize early returns and error conversions."
  },
  "err-context-anyhow-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`Context::context` wraps errors in a chain of descriptive failure messages, preserving the underlying source error for backtraces."
  },
  "err-option-ok-or-else-lazy-1": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`ok_or_else` evaluates its closure lazily only when the `Option` is `None`, avoiding expensive error allocations when the value is `Some`."
  },
  "err-from-residual-branch-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "The `?` operator calls `From::from(error)` to implicitly convert the error from the callee into the caller function's error return type."
  },
  "err-panic-catch-unwind-boundary-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`catch_unwind` relies on stack unwinding; if a program or crate is compiled with `panic = \"abort\"`, panics immediately terminate the process without unwinding."
  },
  "err-error-source-backtrace-1": {
    "correctIndex": 1,
    "categorySlug": "error-handling",
    "explanation": "`Error::source()` returns a reference to the lower-level error that caused the current error, enabling chaining of errors for rich diagnostic reporting."
  },
  "err-control-flow-break-continue-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`ControlFlow` cleanly distinguishes early-exit/break values from continue values without semantic connotation of \"success\" or \"error\", integrating with the `?` operator."
  },
  "err-result-map-or-else-fallback-1": {
    "correctIndex": 3,
    "categorySlug": "error-handling",
    "explanation": "`map_or_else` maps an `Ok(t)` value using `map_fn(t)`, or computes a fallback return value by evaluating `fallback_fn(e)` on `Err(e)`."
  },
  "err-panic-location-file-line-1": {
    "correctIndex": 2,
    "categorySlug": "error-handling",
    "explanation": "`#[track_caller]` allows helper functions (like `unwrap()` or custom assertions) to report the caller's source code location (`file!()` and `line!()`) in panic messages."
  },
  "err-io-error-custom-payload-1": {
    "correctIndex": 0,
    "categorySlug": "error-handling",
    "explanation": "`io::Error::other` and `io::Error::new` allow developers to package any arbitrary error type into standard `std::io::Error` envelopes with custom error kinds."
  },
  "iter-vs-loop-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "Iterator adapters read as the transformation you want and compose well. LLVM often optimizes them similarly to loops. Prefer a loop when control flow is complex (early exits, multi-step state) rather than from a blanket speed rule."
  },
  "iter-closure-ownership-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "By default the closure immutably borrows `prefix`. `move` forces capture by value. Here `prefix: &str` is `Copy`, so `move` copies the reference, not the string bytes."
  },
  "iter-lazy-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "Creating a `map` adapter does not run the closure. Work happens when something consumes the iterator (`collect`, `for`, `next`, …). Leaving it unused is fine; there is no panic."
  },
  "iter-fold-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`fold` starts from an initial accumulator and combines each item into it, producing one final value. `map` transforms items one-by-one without reducing them."
  },
  "iter-idiomatic-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "For a simple search/transform, adapters like `filter`/`find` (or a single `find` with a compound predicate) are idiomatic. Loops are fine for messier control flow, but not required here."
  },
  "iter-collect-type-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`FromIterator` is implemented for many types (`Vec`, `HashSet`, …), so Rust needs a concrete target. Write `.collect::<Vec<_>>()` or `let doubled: Vec<_> = ...`. Multiplying `&i32` by `2` is fine via auto-deref/`Mul`."
  },
  "closure-fn-traits-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "Mutating a captured variable requires `FnMut`. All closures implement `FnOnce`; this one is not limited to a single call. It does not implement `Fn`, which allows calling through a shared `&self`."
  },
  "iter-chain-efficiency-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`filter`/`map`/`take` are lazy zero-cost wrappers. The closure work runs as `collect` pulls items, and that is when the output `Vec` is allocated. LLVM may optimize further, but that is not a parse-time rewrite."
  },
  "closure-move-copy-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`move` forces by-value capture. For `i32`, that value is copied, so `x` remains valid. A non-`Copy` type like `String` would be moved and unusable afterward."
  },
  "iter-filter-map-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`filter_map` maps to `Option<U>`, keeps `Some`, and discards `None`, ideal for parse-or-skip. It works with any `Option`, not only `Result` (often via `.ok()`). The longer chain is noisier and uses `unwrap` after filtering."
  },
  "iter-into-iter-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`iter()` yields `&T` and leaves the collection owned. `into_iter()` consumes the collection and yields owned `T`. A bare `for x in v` desugars to `into_iter()`."
  },
  "closure-lifetime-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "This compiles: the closure captures `s: &'a str` and returns it. Returning `impl Fn() -> &'a str + 'a` (or an equivalent bound) is valid. Boxing is only needed for type erasure or mixed concrete closure types."
  },
  "iter-flat-map-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`flat_map(f)` is `.map(f).flatten()`: each element produces an iterable (`IntoIterator`), and one level of nesting is removed. It works with many iterable outputs, including `Option`/`Result` as 0-or-1 iterators."
  },
  "iter-zip-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::zip` yields pairs until one side ends. Different payload types are fine (`(&i32, &&str)`). No padding and no length panic in std."
  },
  "closure-capture-scope-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "The `filter` closure reads `threshold`; the `map` closure reads `factor`. `data` is used to build the iterator, not inside those closures, so it is not captured by them."
  },
  "iter-take-while-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`take(n)` yields at most `n` items. `take_while` yields while the predicate holds and stops at the first failure (even if later items would pass). Both are lazy adapters."
  },
  "iter-enumerate-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`enumerate` yields `(usize, Item)` starting at index `0`. It does not expose addresses, remaining counts, or hashes."
  },
  "closure-return-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`impl Fn` works for one concrete closure type. Here the `if` arms produce two distinct types, so they cannot unify. `Box<dyn Fn...>` erases them behind one trait object. A single-branch function can return `impl Fn` without boxing."
  },
  "iter-partition-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`partition` consumes the iterator and builds two collections: predicate-true first, false second. It keeps both sides, unlike `filter`. Chunking/grouping consecutive equals are different adapters/crates."
  },
  "iter-scan-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`scan` is a stateful adapter: the closure gets `&mut State` and may yield `Some`/`None` (ending early). `map` is stateless per element. Use `scan` for running totals and similar pipelines."
  },
  "iter-by-ref-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "Many iterator adapters consume `self`. `.by_ref()` borrows the iterator so you can apply adapters (like `take`) and still use the original afterward for the remainder."
  },
  "iter-fuse-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "A fused iterator promises that once `next` returns `None`, all later `next` calls also return `None`. `.fuse()` wraps iterators that might not uphold that, which matters for combinators that assume fusion."
  },
  "closure-fnonce-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "Calling the closure moves `s` into `drop`. A second call would have nothing left to move, so the closure is `FnOnce` only, not `FnMut`/`Fn`. Closures that only borrow can be called repeatedly."
  },
  "iter-copied-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`.copied()` is for iterators of `&T` where `T: Copy`: it bitwise-copies each item. `.cloned()` needs `T: Clone` and calls `clone()` (which may allocate). Neither consumes the slice; `v.iter()` still only borrows. `collect` is a separate adapter."
  },
  "iter-peekable-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`Peekable::peek` returns `Option<&Item>` without advancing. The following `next()` still yields that same item (`Some(10)`). A common mix-up is thinking `peek` consumes, or that `next` would keep returning a reference."
  },
  "iter-rev-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::rev` requires `DoubleEndedIterator` so it can pull from the back. `RangeInclusive<i32>` implements that, yielding 3, then 2, then 1, without collecting first. Many iterators (e.g. most `Map` over a one-way source) are not double-ended and cannot `.rev()`."
  },
  "iter-by-ref-partial-consumption-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::by_ref` creates an adapter that borrows the underlying iterator mutably (`&mut I`). When `take(2).collect()` runs, it advances `numbers` by two elements and drops the `take` adapter without consuming `numbers` by value. Subsequent calls to `numbers.next()` resume directly at the third element (`Some(3)`)."
  },
  "iter-closure-fn-cell-capture-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "The closure trait implemented by a closure depends on how it accesses its captured variables. Here, `bump` captures `counter` by shared reference (`&Cell<i32>`). Because `Cell::set` operates on `&self` via interior mutability, the closure's call operator only requires `&self` (`&bump`), allowing it to implement `Fn`."
  },
  "iter-inspect-laziness-side-effect-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "Iterators in Rust are completely lazy. Adapter methods like `inspect`, `map`, and `filter` do not perform any work until a consuming method (like `next()`, `collect()`, or `for_each()`) actively pulls elements through the iterator chain. Because `iter` is dropped without being polled, `count` remains 0."
  },
  "iter-supporter-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::by_ref` borrows the iterator mutably as `&mut I` (which also implements `Iterator`). When consuming adapters like `.take(3)` drop, the underlying iterator `iter` remains valid and ready for further items."
  },
  "iter-supporter-2": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "Starting in Rust 2021 and continuing in 2024, closures capture precise field paths (`s.a`, `s.b`) rather than the entire struct `s`, enabling independent simultaneous borrows of disjoint struct fields."
  },
  "iter-supporter-3": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`DoubleEndedIterator::next_back()` removes and returns an element from the rear of the iterator. Elements can be drawn from either end until the iteration converges and yields `None`."
  },
  "iter-supporter-4": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`ExactSizeIterator` guarantees that `iter.len()` accurately reports the exact number of elements remaining in the iteration (`(len, Some(len))`), allowing callers to preallocate exact capacity."
  },
  "iter-supporter-5": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "Rust iterators are completely lazy: calling `.map(...)` simply constructs an iterator adapter struct. No closure code executes until the iterator is driven by calling `.next()`, `.collect()`, or a `for` loop."
  },
  "iter-supporter-6": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::reduce` uses the first element of the iterator as the initial accumulator value and returns `Option<T>` (yielding `None` if the iterator is empty), whereas `fold` takes an explicit initial value."
  },
  "iter-supporter-7": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::try_fold` is the foundational primitive for all fallible/short-circuiting iterator consumers. If the accumulator closure returns `Err(e)` or `Break(b)`, `try_fold` stops immediately without processing remaining items."
  },
  "iter-supporter-8": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`FnMut` defines `extern \"rust-call\" fn call_mut(&mut self, ...)`. Because the call takes `&mut self` to modify its captured environment, the binding `inc` must be declared with `let mut`."
  },
  "iter-supporter-9": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::scan(initial_state, f)` holds a state value and passes `&mut state` to the closure `f` on each iteration. The closure returns `Option<B>`, allowing stateful transformations and early termination."
  },
  "iter-supporter-10": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::partition(predicate)` consumes the iterator and collects elements into a tuple `(A, B)` of collections: `A` receives elements where the predicate returned `true`, and `B` receives `false`."
  },
  "iter-supporter-11": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`FnOnce` defines `fn call_once(self, ...) -> Self::Output`. Because it takes `self` by value, the closure (and any owned resources it captured) is consumed on the first call and cannot be called again."
  },
  "iter-supporter-12": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`Peekable::peek_mut` returns `Option<&mut I::Item>`, enabling callers to inspect and modify the buffered next item in place before extracting it with `next()`."
  },
  "iter-supporter-13": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::zip` pairs elements until either underlying iterator yields `None`, terminating at the length of the shorter iterator."
  },
  "iter-supporter-14": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "Standard `Iterator` contracts do not specify behavior if `next()` is called after returning `None`. `fuse()` wraps the iterator so that once `None` is yielded, all future `next()` calls are guaranteed to return `None`."
  },
  "iter-supporter-15": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`FromIterator` for `Result<V, E>` collects an iterator of `Result<T, E>`: if all are `Ok(x)`, it returns `Ok(Vec<T>)`; on the first `Err(e)`, iteration stops and `Err(e)` is returned immediately."
  },
  "iter-supporter-16": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::flat_map(f)` maps each item to an `IntoIterator` and flattens the resulting iterators into a single linear sequence."
  },
  "iter-supporter-17": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "A closure that captures no variables from its environment has an empty environment struct (zero-sized) and can be implicitly coerced to a plain function pointer `fn(...) -> ...`."
  },
  "iter-supporter-18": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`take_while` yields items only until the predicate returns `false` for the first time, at which point it terminates. `filter` tests every item in the iterator and keeps all matching ones."
  },
  "iter-supporter-19": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::cycle` repeats the elements endlessly: when the end is reached, it starts again from the beginning (requires `Self: Clone`)."
  },
  "iter-supporter-20": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::chain` yields elements from `iter_a` until exhaustion, and then seamlessly proceeds to yield elements from `iter_b`."
  },
  "iter-supporter-21": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "In Rust, a closure is desugared into an anonymous struct containing all captured variables (either as references or by-value fields). Its size is the struct layout size of those captured fields."
  },
  "iter-supporter-22": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::enumerate` yields `(usize, Item)` pairs, with the zero-based index typed as `usize` matching pointer widths."
  },
  "iter-supporter-23": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::position` searches from left to right and returns `Some(index)` (`Option<usize>`) for the first element where `predicate` returns `true`, or `None` if not found."
  },
  "iter-supporter-24": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::rposition` searches from right to left, returning `Some(index)` (`usize`) representing the index from the front of the collection."
  },
  "iter-supporter-25": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::skip_while` ignores items as long as `predicate` is `true`. As soon as it encounters the first `false`, it yields that element and all subsequent elements without ever calling the predicate again."
  },
  "iter-supporter-26": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::step_by(step)` requires `step >= 1`. Calling `step_by(0)` panics with an assertion error because a step size of 0 would loop infinitely on the first element."
  },
  "iter-supporter-27": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::rfold` operates on `DoubleEndedIterator` by consuming elements in reverse order (from back to front)."
  },
  "iter-supporter-28": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`std::iter::Extend` defines `fn extend<T: IntoIterator>(&mut self, iter: T)`. It extends an existing collection by iterating through `iter` and appending each element."
  },
  "iter-supporter-29": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::all` short-circuits immediately upon the first element for which `predicate` returns `false`, returning `false` without inspecting the rest."
  },
  "iter-supporter-30": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`IntoIterator` for `&[T]` yields shared references `&T`, while `IntoIterator` for `&mut [T]` yields exclusive mutable references `&mut T`."
  },
  "iter-supporter-31": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::unzip` takes an iterator of tuples `(A, B)` and separates them into two collections, extending the first collection with `a` and the second with `b`."
  },
  "iter-supporter-32": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::flatten` requires `I::Item: IntoIterator`. It flattens one level of nested structure (e.g. `Vec<Vec<T>>` or `Vec<Option<T>>`)."
  },
  "iter-supporter-33": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`impl Fn(...) -> ...` accepts any callable type (including closures with captured state and plain `fn` pointers), monomorphizing the call with zero heap or indirection overhead."
  },
  "iter-supporter-34": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`Iterator::count()` iterates through every element by repeatedly calling `next()` until exhaustion. `slice.len()` is an O(1) field read from the slice's fat pointer metadata."
  },
  "iter-supporter-35": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "Because `collect()` can produce any type implementing `FromIterator` (such as `Vec`, `HashSet`, `BTreeSet`, `String`, `Result`), type inference cannot guess the target container without annotations or turbofish syntax `::<Vec<_>>()`."
  },
  "iter-fuse-once-none-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`fuse()` adapts any iterator so that once `next()` returns `None`, all subsequent calls to `next()` are guaranteed to keep returning `None`."
  },
  "iter-by-ref-adapter-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`by_ref()` creates a mutable borrow of an iterator, allowing you to apply consumer adapters (like `take(5)`) without moving or consuming the original iterator."
  },
  "iter-inspect-side-effects-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`inspect()` takes a closure `&FnMut(&Item)` to perform side-effects (such as logging or debugging) on elements as they pass through without altering the sequence."
  },
  "iter-flat-map-option-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "Because `Option<T>` implements `IntoIterator`, `flat_map` flattens `Some(v)` into `v` and silently ignores `None`, functioning similarly to `filter_map`."
  },
  "iter-rchunks-exact-remainder-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`rchunks_exact(n)` yields exact chunks of length `n` starting from the end of the slice, leaving any remainder at the start available via `.remainder()`."
  },
  "iter-advance-by-n-efficiency-1": {
    "correctIndex": 1,
    "categorySlug": "iterators-closures",
    "explanation": "`advance_by(n)` allows specialized iterators (like slices and ranges) to jump forward `n` elements in O(1) pointer arithmetic rather than executing an O(n) stepping loop."
  },
  "iter-collect-result-short-circuit-1": {
    "correctIndex": 3,
    "categorySlug": "iterators-closures",
    "explanation": "`collect()` into `Result<Vec<T>, E>` short-circuits: upon encountering the first `Err(e)`, iteration immediately stops and returns `Err(e)`."
  },
  "iter-zip-shortest-termination-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`zip()` terminates as soon as either iterator returns `None`. Remaining elements in the longer iterator are not consumed."
  },
  "iter-scan-internal-state-1": {
    "correctIndex": 2,
    "categorySlug": "iterators-closures",
    "explanation": "`scan()` maintains internal mutable state across iteration steps, allowing stateful transformations and early termination by returning `None` from the closure."
  },
  "iter-peekable-peek-mut-1": {
    "correctIndex": 0,
    "categorySlug": "iterators-closures",
    "explanation": "`peek_mut()` returns a `PeekMut` guard holding a mutable reference to the next element, allowing you to modify the peeked value before it is consumed."
  },
  "life-basic-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Lifetime parameters do not keep values alive longer. The shared `'a` means the returned reference is borrowed from one of the inputs and is only usable for as long as that borrow remains valid; in practice, the overlap of the two concrete input lifetimes."
  },
  "life-struct-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "A struct field `&'a str` borrows data owned elsewhere. The lifetime parameter ties the struct’s validity to that borrow so the compiler can reject dangling references when the struct would outlive its referent."
  },
  "life-elision-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "With exactly one input reference, elision assigns that input lifetime to any elided output reference. This is equivalent to `fn first_word<'a>(s: &'a str) -> &'a str`."
  },
  "life-static-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "As a reference lifetime, `'static` means the data lives for the remaining program run (Rust By Example). Literals are the usual case; runtime values can also be `'static` if never dropped afterward (e.g. `Box::leak`). It is not about mutability or requiring a `static fn`."
  },
  "life-mult-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "The return type `&'a str` ties the result to `x`’s borrow. Nothing requires `y` to live as long as `'a`, so you generally cannot return a borrow of `y` without an extra bound such as `'b: 'a`."
  },
  "life-dangle-local-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "The `&str` points into `s`’s buffer. `s` is dropped when the function returns, so the reference would dangle. A lifetime parameter describes a relationship; it cannot extend a local’s storage. Return owned data or borrow an input from the caller."
  },
  "life-elision-method-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "If there is a `&self`/`&mut self` receiver, elision uses the receiver’s lifetime for elided output lifetimes. The returned `&str` is treated as borrowed from `self` (and thus constrained by that borrow), which fits common accessors."
  },
  "life-two-inputs-need-anno-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "With two input references, elision gives each its own lifetime and does not invent an output lifetime. Because the body may return `x` or `y`, you must state the contract, e.g. one shared lifetime, or an output tied to a specific input."
  },
  "life-static-misuse-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "`'static` means the referent can live for the whole program. A `String` created in the function is dropped on return, so a reference into it cannot be `'static`. Prefer returning `String` or borrowing caller-owned data."
  },
  "life-struct-field-vs-param-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "The return type is `&'a str`, so the result is valid for `'a`, the struct field’s lifetime. `other` has `'b` with no `'b: 'a` bound, so returning `other` would be illegal. As written, the contract is independent of `'b`."
  },
  "life-elision-two-inputs-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "Even if the body only returns `x`, elision still refuses to guess an output lifetime when there are multiple input references. You must annotate, e.g. `fn first<'a>(x: &'a str, y: &str) -> &'a str`."
  },
  "life-subtype-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "Lifetime subtyping: if data is valid for `'static`, it is valid for any shorter `'a`. Passing a string literal into `take<'a>` coerces `&'static str` to `&'a str`. Lifetimes are not ignored; longer outlives shorter."
  },
  "life-anon-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "`'_` stands for a lifetime the compiler infers (often the same as writing an elided `&str`). Here it still ties the output to the input `s`; it does not mean `'static` or turn off checking."
  },
  "life-outlives-bound-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "`T: 'a` means `T` is valid for `'a`: any references inside `T` outlive `'a`. Plain owned values like `i32` or `String` satisfy it. It does not force `T` itself to be a reference."
  },
  "life-impl-elision-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "`Wrap` is generic over `'a`, so `impl` must introduce that parameter (`impl<'a> Wrap<'a>`) before naming the type. Elision can still apply to method inputs like `&self`, but the struct’s lifetime parameter remains explicit."
  },
  "life-mut-reborrow-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "These are two successive `&mut` borrows of `x`, not a reborrow of an existing reference. Non-lexical lifetimes end `p` after its last use (`*p += 1`), so `q` can borrow `x` next. Two overlapping live `&mut` aliases would still be rejected."
  },
  "life-hrtb-intro-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "`for<'a> Fn(&'a str) -> &'a str` is a higher-ranked trait bound: the closure/function must be valid for all lifetimes, not just one. That is stronger than having a single `'a` on `apply` itself."
  },
  "life-static-promoted-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "When a value expression is eligible, constant promotion places it in a `'static` slot (Reference, Destructors). That is why `&10` can be a `&'static i32`. Arbitrary locals are not promoted."
  },
  "life-same-lifetime-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "A shared `'a` means both references are borrowed for (at least) that same region, the overlap where both are valid. They need not be the same allocation, and non-`'static` borrows are fine if they overlap long enough."
  },
  "life-return-shorter-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "With more than one input reference, elision will not assign the output lifetime (Reference, Lifetime elision). You must name which input the return borrows from. `_y` can stay elided as `&str`; the important part is tying the return to `x` via `'a`."
  },
  "life-static-bound-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "`T: 'static` means `T` can be held indefinitely: it has no non-`'static` borrows. Owned types like `String` satisfy that even though they are dropped at the end of `hold`. `&'static T` is a reference that must remain valid for the rest of the program. `hold(&s)` fails because `&s` only lasts as long as the local `s`."
  },
  "life-mut-invariant-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "`&'a mut T` is covariant in `'a` but invariant in `T` (Reference, Subtyping). If `T` could shrink from `&'static str` to `&'short str`, `assign` could store `&short` into `forever` and leave a dangling `'static` after `short` drops. Shared `&T` is covariant in `T`, which is why `&'static str` can be used as `&'a str`."
  },
  "life-elision-method-struct-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "Under Rust's lifetime elision rules for methods with a `self` parameter, the lifetime of any returned reference defaults to the lifetime of `&self`. Here, `next_token(&self) -> &str` is elided as `fn next_token<'b>(&'b self) -> &'b str`, tying the return value's lifetime to the temporary borrow of the parser rather than the underlying `'a`. To return a reference valid for `'a`, write `fn next_token(&self) -> &'a str`."
  },
  "life-variance-fn-ptr-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "In Rust's subtyping rules, function pointers `fn(Arg) -> Ret` are contravariant with respect to `Arg` and covariant with respect to `Ret`. Because `'static` outlives `'b`, a function that can accept any shorter lifetime `'b` (`identity`) can safely be used where a function accepting `'static` is required (`apply`)."
  },
  "life-supporter-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "&'a T is covariant with respect to 'a. Because 'static outlives 'a ('static: 'a), 'static is a subtype of 'a and can safely be substituted wherever 'a is expected."
  },
  "life-supporter-2": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "If &mut T were covariant over T, a caller could pass &mut &'static str to a function expecting &mut &'a str, which could then write a short-lived &'a str into what the caller believes is a 'static slot."
  },
  "life-supporter-3": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Function pointers are contravariant over their argument types. A function that can accept any short-lived reference &'a str is capable of accepting a 'static reference, so fn(&'a str) is a subtype of fn(&'static str)."
  },
  "life-supporter-4": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "for<'a> is a Higher-Ranked Trait Bound (HRTB). It requires F to be callable with a reference of *any* lifetime 'a chosen by the implementation of apply (such as a local reference inside apply), not a single fixed caller lifetime."
  },
  "life-supporter-5": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "Rust performs 'rvalue static promotion': taking a reference to a constant expression (like &42 or &'hello') that contains no interior mutability automatically places the data in read-only static memory, giving it lifetime 'static."
  },
  "life-supporter-6": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "By Rust's lifetime elision rules, if there is a &self or &mut self parameter, the lifetime of self is assigned to all elided output lifetimes. So get(&self) -> &str is desugared to fn get<'s>(&'s self) -> &'s str."
  },
  "life-supporter-7": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Using distinct lifetime parameters ('a and 'b) allows first and second to borrow from independent scopes with different durations, avoiding artificially shrinking the longer borrow to the shorter one."
  },
  "life-supporter-8": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "Reader<'_> asks the compiler to infer an anonymous lifetime for the parameter, following standard lifetime elision rules rather than requiring an explicit generic declaration <'a>."
  },
  "life-supporter-9": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "In Rust, moving a struct copies its bytes to a new location. If a struct contained a reference to one of its own fields, moving the struct would leave that reference pointing to the old, invalid memory location."
  },
  "life-supporter-10": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "For &'a &'b T to be valid, the inner reference must live at least as long as the outer reference that borrows it ('b: 'a). If 'b ended while 'a was still active, the outer reference would point to invalid memory."
  },
  "life-supporter-11": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "By default, trait objects inside Box<dyn Trait> default to Box<dyn Trait + 'static>, meaning the underlying concrete type cannot contain non-'static references unless an explicit bound is specified."
  },
  "life-supporter-12": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "Unlike Box<dyn Trait> which defaults to 'static, a trait object behind a reference &'a dyn Trait defaults to &'a (dyn Trait + 'a)."
  },
  "life-supporter-13": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "The bound T: 'a ('T outlives 'a') means that T is valid for at least 'a. If T contains any borrowed references, those references must live for at least 'a. Owned types with no references satisfy T: 'static and thus T: 'a for any 'a."
  },
  "life-supporter-14": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "Standard Iterator defines type Item; without a lifetime parameter, requiring items to outlive the iterator. GATs allow type Item<'a>, enabling Item to borrow from &'a mut self for each step."
  },
  "life-supporter-15": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Because mutable references &'a mut T are covariant in 'a and invariant in T, using PhantomData<&'a mut T> informs the compiler that the struct is covariant with respect to 'a and invariant with respect to T."
  },
  "life-supporter-16": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "PhantomData<&'a T> acts as a phantom shared reference, conveying covariance over both 'a and T without implying ownership or running destructors on T."
  },
  "life-supporter-17": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "In trait bounds, Fn(&i32) automatically desugars to for<'a> Fn(&'a i32). This allows take_fn to pass references created within its own local stack frame to f."
  },
  "life-supporter-18": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "s is allocated on the local stack frame and dropped when the function exits. Returning &s would produce a dangling reference to deallocated memory, which Rust statically prevents."
  },
  "life-supporter-19": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "String literals ('hello world') are embedded directly into the read-only data segment (.rodata) of the binary. They are valid for the entire execution of the program ('static)."
  },
  "life-supporter-20": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "When a function has multiple input reference parameters and no &self, the compiler cannot determine which input the output reference borrows from. You must explicitly declare lifetime parameters (e.g. fn pick<'a>(x: &'a str, y: &'a str) -> &'a str)."
  },
  "life-supporter-21": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "A reborrow produces a new reference whose lifetime is bounded by the original reference. It can be shorter (e.g. limited to a local block), but can never outlive the source borrow."
  },
  "life-supporter-22": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "In closures taking a single reference parameter, lifetime elision applies: the returned reference is tied to the lifetime of the input reference argument s."
  },
  "life-supporter-23": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Rust supports mixing lifetime parameters, type parameters, and const generic parameters in any struct or function declaration (e.g. struct Buffer<'a, T, const N: usize>)."
  },
  "life-supporter-24": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "This HRTB specifies that a shared reference &'a T of *any* arbitrary lifetime 'a implements IntoIterator, enabling borrowing and iterating without consuming T."
  },
  "life-supporter-25": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "A reference to a field inside a struct borrowed for 'a can be held for up to 'a. It cannot outlive the struct borrow, but can be held for the full duration 'a."
  },
  "life-supporter-26": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "T: 'static is a type bound meaning T contains no non-'static borrows (e.g. String, i32, Vec<u8>). In contrast, &'static T is specifically a reference pointing to data that lives forever."
  },
  "life-supporter-27": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Associated constants can hold reference types as long as the lifetime is 'static (const DEFAULT: &'static str), ensuring the data is available for any evaluation context."
  },
  "life-supporter-28": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "Box<T> owns its contents exclusively and is covariant over T. Because &'static str is a subtype of &'a str, Box<&'static str> is a subtype of Box<&'a str> and can be assigned directly."
  },
  "life-supporter-29": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "UnsafeCell<T> is the core primitive for interior mutability. Because it allows mutation through a shared reference (&UnsafeCell<T>), allowing covariance over T would reintroduce the ability to overwrite longer lifetimes with shorter ones."
  },
  "life-supporter-30": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "Cow<'a, B> ('Clone on Write') has a lifetime 'a representing the duration of the borrowed variant (Cow::Borrowed(&'a B)). If owned (Cow::Owned), the data is independent of 'a."
  },
  "life-supporter-31": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Vec<T> owns its allocation and has no shared mutable aliasing through Vec<T>. Therefore Vec<T> is covariant over T, allowing Vec<&'static str> to be used as Vec<&'a str>."
  },
  "life-supporter-32": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "cell.borrow() returns a Ref<'a, T> that is bound to the lifetime 'a of the &'a RefCell<T>. The guard cannot outlive the cell it borrows from."
  },
  "life-supporter-33": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "async fn fetch(&self) -> String desugars to fn fetch<'a>(&'a self) -> impl Future<Output = String> + 'a. The returned Future captures &'a self and cannot outlive self."
  },
  "life-supporter-34": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "In Rust, reference types like &'a str or &'a mut [u8] are first-class types. You can implement traits directly on them with generic lifetime parameters on the impl block."
  },
  "life-supporter-35": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "Rust features 'implied bounds': because the field &'a T requires T: 'a to be well-formed, the compiler automatically infers T: 'a without needing an explicit T: 'a bound on the struct declaration."
  },
  "life-variance-fn-arg-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "Function arguments are contravariant over their lifetimes. A function expecting a short lifetime can accept a function accepting a longer lifetime."
  },
  "life-static-bound-struct-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "`T: 'static` means `T` is capable of living for the entire program duration; it can be an owned type (like `String` or `i32`) or a reference with `'static` lifetime."
  },
  "life-higher-rank-trait-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "`for<'a>` is a Higher-Rank Trait Bound (HRTB) indicating that `F` can accept a reference with *any* lifetime `'a` chosen by the caller."
  },
  "life-elision-impl-block-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "If there are multiple input lifetimes but one is `&self` or `&mut self`, the lifetime of `self` is assigned to all elided output lifetimes."
  },
  "life-anonymous-param-ref-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "Rule 1 of lifetime elision states that each elided lifetime in input parameters is assigned a distinct, unique lifetime parameter (`fn parse<'a, 'b>(&'a str, &'b str)`)."
  },
  "life-multiple-lifetime-bounds-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "The lifetime subtyping bound `'a: 'b` means that lifetime `'a` outlives (is at least as long as) lifetime `'b`."
  },
  "life-early-vs-late-bound-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "Early-bound lifetimes (often involved in trait bounds or `where` clauses) are monomorphized when referencing the item, whereas late-bound lifetimes are quantified at each specific call site."
  },
  "life-ref-mut-invariant-param-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "`&mut T` must be invariant over `T` because it allows reading from and writing to `T`. Covariance would allow writing a shorter-lived reference into a place expected to live longer."
  },
  "life-trait-object-default-bound-1": {
    "correctIndex": 1,
    "categorySlug": "lifetimes",
    "explanation": "For references to trait objects `&'a dyn Trait`, the default object lifetime bound is inferred as `'a` (i.e. `&'a (dyn Trait + 'a)`). For `Box<dyn Trait>`, it defaults to `'static`."
  },
  "life-static-promotion-rvalue-1": {
    "correctIndex": 3,
    "categorySlug": "lifetimes",
    "explanation": "Rust automatically applies \"rvalue static promotion\" to immutable references of compile-time evaluatable expressions with no destructors, placing them in static program memory."
  },
  "life-opaque-return-lifetime-capture-1": {
    "correctIndex": 0,
    "categorySlug": "lifetimes",
    "explanation": "In Rust 2024, return position `impl Trait` captures all in-scope lifetime parameters by default, simplifying async functions and trait returns unless restricted using `+ use<'a>`."
  },
  "life-for-lifetime-fn-ptr-subtyping-1": {
    "correctIndex": 2,
    "categorySlug": "lifetimes",
    "explanation": "`for<'a> fn(&'a T)` is a subtype of `fn(&'b T)`: a function capable of handling references of *any* lifetime can safely be passed wherever a specific lifetime `'b` is expected."
  },
  "macro-rules-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`macro_rules!` expands during compilation, producing tokens that are then parsed and type-checked. Functions take runtime values. Macros are powerful but not inherently “safer.”"
  },
  "macro-match-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`:expr` matches a Rust expression. Other common designators include `:ident`, `:ty`, `:path`, `:tt`, and `:stmt`. Literals-only or types-only need different matchers."
  },
  "macro-repeat-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "In `macro_rules!`, `$( ... ),*` means zero or more repetitions of the inner pattern, separated by commas. `+` is one or more; `?` is optional (zero or one). Trailing commas need an explicit pattern if you want them."
  },
  "macro-proc-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "Procedural macros are compiled plugins: functions from `TokenStream` to `TokenStream` (often via `syn`/`quote`). Declarative macros use pattern → template rules. Proc macros power `derive` and many attributes; they are not deprecated."
  },
  "macro-hygiene-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`macro_rules!` hygiene (via spans) keeps identifiers introduced by the macro from capturing or colliding with the caller's identically named locals. The caller's `x` stays `1`. This is stronger than “just a new block,” though expansion still respects ordinary scopes too."
  },
  "macro-tt-muncher-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "A `tt` (token tree) is a single token or a delimited group. `$($t:tt)*` matches an arbitrary token sequence, which is why it is used for pass-through and recursive “tt muncher” macros. It does not require the tokens to type-check first."
  },
  "macro-export-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`#[macro_export]` places the macro at the crate root and makes it available to dependents (`use your_crate::my_assert;`). Ordinary `pub` on modules is not enough for classic `macro_rules!` export across crates."
  },
  "macro-paste-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Macro expansion happens at compile time as part of turning the crate into an AST that can be resolved and type-checked. By the time types are checked, `make_add!(1, 2)` is already `1 + 2`."
  },
  "macro-metavariable-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Substituting `$e` twice duplicates the expression tokens. Pure locals like `n` are fine; expressions with side effects run twice. Capture once in a `let` inside the macro if you need single evaluation."
  },
  "macro-crate-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`$crate` resolves to the crate where the macro is defined, so exported macros can refer to that crate's items reliably even when invoked from dependents. It is not the caller's crate path."
  },
  "macro-ident-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`:ident` matches a single identifier. Expressions need `:expr`, types `:ty`, paths `:path`. Using the wrong specifier is a common macro debugging dead-end for juniors."
  },
  "macro-trailing-comma-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`),*` matches comma-separated items without a final trailing comma. Idiomatic macros add an optional trailing comma arm (e.g. `$( $x:expr ),* $(,)?`) so call sites can use Rust’s usual trailing-comma style."
  },
  "macro-eager-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Without `#[macro_export]`, a `macro_rules!` macro is visible in the defining module (and children, depending on definition order/legacy rules). It is not automatically crate-wide like a casual mental model of “macros are global.”"
  },
  "macro-stmt-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`:stmt` matches one statement. Juniors often confuse it with `:expr`. A `let` binding is a statement; many expressions need `:expr` (and semicolon rules differ in expansion)."
  },
  "macro-dbg-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`dbg!(expr)` prints file/line and `Debug` output to stderr, then yields `expr`’s value (by move/copy as appropriate). That is why `dbg!(x) + 1` works. It does not return `()` or a `String`."
  },
  "macro-vec-macro-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`vec!` supports zero or more elements (and `[elem; n]`). Rust functions cannot be variadic like that in the same syntactic way, so a macro expands into the right `Vec` construction code."
  },
  "macro-attr-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Attribute procedural macros take an item (and optional attribute tokens) and emit a new `TokenStream`. `#[tokio::main]` rewrites `async fn main` into a synchronous entry that builds a runtime. It is not a derive."
  },
  "macro-recurse-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "Declarative macros may invoke themselves (or other macros) in their expansion. Classic “tt muncher” / list-peeling patterns reduce tokens each step until a base case matches. This is compile-time recursion, not runtime."
  },
  "macro-optional-rep-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "In `macro_rules!`, `$( … )?` matches zero or one time (Reference, Macros by example). Unlike `*`/`+`, `?` cannot take a separator token. `$(,)?` after a list is the usual trailing-comma matcher; here the comma is inside the optional group so the second argument itself is optional."
  },
  "macro-stringify-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`stringify!` turns its tokens into a string literal without evaluating them, so `1 + 2` stays `\"1 + 2\"`. `concat!` concatenates string (and some other) literals at compile time (`\"ab\"`). Neither runs at runtime like `format!`."
  },
  "macro-matcher-arm-precedence-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "In declarative macros (`macro_rules!`), matcher arms are checked sequentially from top to bottom. The first arm that successfully parses the input tokens is expanded. Because `42` is a valid expression, `$x:expr` matches immediately and produces `\"expr\"`. To match more specific tokens like `$literal` or `$ident`, place those arms before broader fragment matchers like `$expr`."
  },
  "macro-matcher-pat-param-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "In Rust 2021 and 2024 editions, the `$pat` fragment specifier matches top-level or-patterns (e.g. `A | B`). Because of this, `$pat` cannot be followed by a vertical bar `|` due to ambiguity in the macro parser follow-set rules. The `$pat_param` fragment specifier matches patterns excluding top-level or-patterns, allowing `|` to legally follow it in macro matchers."
  },
  "macro-supporter-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`$e:expr` requires the compiler to parse a complete Rust expression (like `1 + 2`). `$t:tt` matches a single token tree (an identifier, literal, punctuation mark, or balanced `(...)`, `[...]`, `{...}`)."
  },
  "macro-supporter-2": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "Rust declarative macros (`macro_rules!`) are hygienic for local variables: identifiers created inside the macro expansion carry a distinct syntax context and cannot accidentally capture or shadow variables in the caller's scope."
  },
  "macro-supporter-3": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`$($x:expr),*` matches zero or more occurrences of `$x:expr` separated by commas. Using `+` instead of `*` would require at least one expression."
  },
  "macro-supporter-4": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Rust enforces follow-set rules: certain fragment specifiers (like `pat`, `expr`, `ty`) can only be followed by specific tokens (e.g. `=>`, `,`, `=`) so the parser can reliably detect the boundary of the fragment without lookahead ambiguity."
  },
  "macro-supporter-5": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "Rust supports three kinds of procedural macros: 1) Custom Derive (`#[proc_macro_derive]`), 2) Attribute-like (`#[proc_macro_attribute]`), and 3) Function-like (`#[proc_macro]`)."
  },
  "macro-supporter-6": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`macro_rules!` tests arms sequentially from top to bottom. The first arm whose pattern matches the provided input tokens is expanded; subsequent arms are ignored."
  },
  "macro-supporter-7": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`#[macro_export]` lifts the macro into the crate root module (`$crate::global_macro`) and exports it so external dependent crates can use it."
  },
  "macro-supporter-8": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`$crate` expands to the path of the defining crate (e.g. `crate` when used internally or `::my_crate` when used downstream), ensuring paths resolve correctly regardless of the caller's scope."
  },
  "macro-supporter-9": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`pat_param` matches standard patterns excluding top-level `|` alternatives (e.g. matching `A` in `A | B`). This allows `|` to be used safely as a separator token after `$p:pat_param`."
  },
  "macro-supporter-10": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "A TT muncher is a recursive macro pattern that matches one token tree `$head:tt` and the rest `$($tail:tt)*`, processes `$head`, and recursively calls itself on the remaining `$tail` until empty."
  },
  "macro-supporter-11": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "The inner attribute `#![recursion_limit = \"N\"]` at the root of a crate adjusts the maximum recursion depth for macro expansions, auto trait checks, and const evaluation."
  },
  "macro-supporter-12": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`$lit:literal` matches literal tokens like `\"string\"`, `123`, `true`, `3.14`, and `'a'`. It rejects variable identifiers, complex expressions, and keyword paths."
  },
  "macro-supporter-13": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`$id:ident` matches a single identifier name (variable name, function name, struct name). To match paths with `::`, use `$p:path`."
  },
  "macro-supporter-14": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`$p:path` matches item and type paths such as `crate::module::Item` or `std::sync::Arc`."
  },
  "macro-supporter-15": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`$t:ty` matches any complete Rust type expression (e.g. `i32`, `&'a str`, `Option<Box<dyn Trait>>`, `[u8; 32]`)."
  },
  "macro-supporter-16": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`$b:block` matches a code block delimited by braces `{ ... }`, containing zero or more statements and an optional trailing expression."
  },
  "macro-supporter-17": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`$s:stmt` matches a single statement (such as `let x = 10;`, `foo();`, or an item declaration), allowing macro authors to wrap or sequence statements."
  },
  "macro-supporter-18": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "Procedural macros operate on `proc_macro::TokenStream`, which represents a stream of token trees with associated source location metadata (`Span`), enabling tools like `syn` and `quote` to parse and emit code."
  },
  "macro-supporter-19": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "To define procedural macros, the crate's `Cargo.toml` must declare `[lib] proc-macro = true`. This compiles the crate as a dynamic library loaded by the compiler during compilation of dependent crates."
  },
  "macro-supporter-20": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`Span` attaches file, line, and column position information to tokens. When compiler or macro errors occur, the compiler uses the span to point precisely to the user's original code rather than macro internals."
  },
  "macro-supporter-21": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "In Rust macro repetition syntax, `$(...)?` denotes an optional occurrence (0 or 1 time). `$(,)?` conveniently permits an optional trailing comma at the end of parameter lists."
  },
  "macro-supporter-22": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "When a macro pattern has multiple nested repetition levels, the expansion template must match the nesting depth so each inner repetition iterates within its corresponding outer repetition group."
  },
  "macro-supporter-23": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "`$v:vis` matches visibility qualifiers (e.g. `pub`, `pub(crate)`, `pub(super)`, or nothing for private items), allowing macros to preserve the caller's intended visibility on generated items."
  },
  "macro-supporter-24": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Unlike functions and structs (which are available throughout their module regardless of declaration order), unexported `macro_rules!` follow strict textual scoping and are only available after their point of declaration in source order."
  },
  "macro-supporter-25": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "Applying `#[macro_use]` to a module brings all `macro_rules!` macros defined within that module into the parent module's scope, making them available without explicit path qualification."
  },
  "macro-supporter-26": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`$m:meta` matches the inner content of an attribute (such as `inline`, `derive(Debug)`, or `doc = \"...\"`), allowing macros to accept and forward attributes."
  },
  "macro-supporter-27": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "When declaring `#[proc_macro_derive(Trait, attributes(helper))]`, `helper` is registered as an inert attribute. The compiler permits `#[helper(...)]` on struct fields for the derive macro to read without raising \"unknown attribute\" errors."
  },
  "macro-supporter-28": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "An attribute macro has the signature `pub fn my_attr(attr: TokenStream, item: TokenStream) -> TokenStream`. `attr` holds any tokens inside `#[my_attr(...)]`, and `item` holds the item to which the attribute was applied."
  },
  "macro-supporter-29": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "The `quote!` macro turns Rust syntax into a `proc_macro2::TokenStream`, allowing clean variable interpolation (`#variable`, `#(#items)*`) instead of manual token construction."
  },
  "macro-supporter-30": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "Push-down accumulation is a declarative macro technique where intermediate state is collected into a helper bracketed buffer (like `[$($acc)*]`) and passed down through recursive macro calls until matching a terminal base case."
  },
  "macro-hygiene-dollar-crate-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "`$crate` ensures hygienic path resolution, expanding to the root path of the defining crate so helper items and dependencies resolve correctly in external crates."
  },
  "macro-fragment-spec-expr-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "Because `$e:expr` can end with arbitrary expressions, macro matchers restrict following tokens to delimiters (like `,`, `;`, or `=>`) to eliminate ambiguity in the parser."
  },
  "macro-tt-muncher-base-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "A TT (token tree) muncher pattern matches the leading token tree(s), transforms or stores them, and recursively invokes the macro on the remaining tail until empty."
  },
  "macro-stringify-stringify-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "`stringify!(...)` takes tokens at compile time and converts their exact textual representation into a static `&'static str` literal without evaluating expressions."
  },
  "macro-repetition-separator-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "In `macro_rules!`, `$($x:expr),*` matches zero or more repetitions of `$x:expr` separated by commas."
  },
  "macro-proc-macro-derive-helper-attrs-1": {
    "correctIndex": 2,
    "categorySlug": "macros",
    "explanation": "Listing `attributes(my_helper)` in `proc_macro_derive` informs `rustc` that `#[my_helper]` is an inert attribute meant for this derive macro, preventing \"unknown attribute\" errors."
  },
  "macro-macro-rules-local-inner-macros-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`#[macro_export(local_inner_macros)]` tells the compiler to expand internal macro calls within the macro body by prefixing them with `$crate::`, allowing helper macros to resolve correctly."
  },
  "macro-token-tree-group-delimiter-1": {
    "correctIndex": 1,
    "categorySlug": "macros",
    "explanation": "In procedural macro token streams, `Group` bundles delimited syntax with its delimiter kind (`Parenthesis`, `Bracket`, `Brace`, or `None`) and an inner `TokenStream`."
  },
  "macro-ident-macro-hygiene-resolution-1": {
    "correctIndex": 3,
    "categorySlug": "macros",
    "explanation": "Declarative macros have identifier hygiene: variable bindings declared inside macro bodies cannot shadow or collide with variables in caller scopes."
  },
  "macro-compile-error-diagnostic-1": {
    "correctIndex": 0,
    "categorySlug": "macros",
    "explanation": "`compile_error!(\"message\")` causes compilation to fail with the specified error message, frequently used in macro match fallback arms to signal invalid macro arguments."
  },
  "own-move-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "Assigning a non-`Copy` value like `String` moves ownership. After `let s2 = s1`, `s1` is invalid, so using it in `println!` is a use-after-move error. This prevents a double free when both bindings would otherwise drop the same heap buffer."
  },
  "own-copy-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`i32` implements `Copy`. Assignment bitwise-copies the value, so `x` and `y` are independent. Types with heap ownership (like `String`) do not implement `Copy` and move instead."
  },
  "own-func-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "Passing a non-`Copy` value by value moves ownership into the function. `some_string` owns the `String`, and `s` in `main` is invalidated. Borrow with `&s` (or clone) if the caller still needs it."
  },
  "own-mut-borrow-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "Rust allows either one mutable reference or any number of shared references, not overlapping mutable borrows. Two simultaneous `&mut s` values would alias and could race, so the borrow checker rejects this."
  },
  "own-dangle-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`s` is owned by `dangle` and dropped at the end of the function. A returned `&s` would point at freed memory. Return an owned `String`, or borrow from data the caller provides."
  },
  "own-vec-push-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`first` immutably borrows into `v`. `v.push(4)` needs a mutable borrow and may reallocate, invalidating that reference. The borrow checker forbids mutable access while the immutable borrow is live."
  },
  "own-partial-move-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "Moving `p.name` is a partial move: the whole `p` and `p.name` cannot be used again, but fields that were not moved stay usable. Here `p.age` still works (`u32` is also `Copy`, so reading it does not move it). Remaining non-`Copy` fields can still be moved out one by one."
  },
  "own-clone-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`Clone` requires an explicit `.clone()` call (and may allocate). `Copy` is implicit on assignment for types that implement it. There is no size cutoff in the language; `Copy` means bitwise copy is always valid and cheap enough to be implicit."
  },
  "own-ref-lifetime-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`x` lives only in the inner block. When that block ends, `x` is dropped, but `r` would still hold a reference to it. The borrow checker rejects this dangling reference."
  },
  "own-mut-immut-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "You may have multiple shared borrows or one mutable borrow, but not both at once. Here `r1`/`r2` and `r3` overlap at the `println!`, so the code is rejected."
  },
  "own-return-move-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "Returning `s` transfers ownership to the caller. No clone or lifetime magic is required; this is the usual way to hand heap data out of a function."
  },
  "own-slice-borrow-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "A `&str` is a borrowed view into existing UTF-8 data. Lifetime elision treats this as borrowing from `s`, so the returned slice cannot outlive that input. Nothing is cloned or promoted to `'static`."
  },
  "own-method-self-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "A `self` receiver consumes the value. `Point` does not implement `Copy` here (no `#[derive(Copy)]`), so `p` is moved into `consume` and cannot be used afterward. Use `&self`/`&mut self` to borrow instead."
  },
  "own-tuple-move-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "Building `(s, x)` moves the non-`Copy` `String` and copies the `i32`. `s` is invalidated; `x` remains valid. `t.1` reads the copied integer inside the tuple."
  },
  "own-ref-mut-scope-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "With non-lexical lifetimes, `r1`’s borrow ends after its last use (`push_str`), so `r2` can borrow mutably afterward. The mutable borrows never overlap in use, which satisfies the exclusivity rule."
  },
  "own-drop-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "Ownership implies a single owner that runs `Drop` when the binding goes out of scope. For `String`, that frees the heap buffer at the end of `main` (unless moved earlier). There is no GC; `drop(s)` only forces an earlier drop."
  },
  "own-box-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Box<T>` is an owning pointer. Assigning `b2 = b1` moves that ownership so only one `Box` will free the allocation. `b1` cannot be used afterward; use `.clone()` if you need two owned boxes."
  },
  "own-copy-drop-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "`Copy` means assignment duplicates the value freely. `Drop` runs cleanup for a unique owner. If both were allowed, copies could cause double-free or skipped cleanup. The language therefore forbids implementing both."
  },
  "own-reassign-mut-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "Reassigning `s` drops the previous owned value before storing the new one. The `\"one\"` allocation is freed at the assignment; `\"two\"` is what `println!` sees. No leak occurs in safe code."
  },
  "own-shadow-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "The second `let s` shadows the first. RHS runs first: `s.len()` borrows the `String` and yields a `usize`. Then the new `s` binding takes that `usize`, and the previous owned `String` is dropped. This is shadowing, not a move of `s` into an integer."
  },
  "own-into-iter-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "`for x in v` desugars to `IntoIterator::into_iter(v)`, which consumes the vector. Use `for x in &v` or `v.iter()` to borrow, or `iter_mut` to mutate in place."
  },
  "own-mem-replace-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`mem::replace` moves the current value out of a mutable place and writes a new owned value in. That keeps the place valid while you take ownership of the previous contents, which is common when a field must stay initialized."
  },
  "own-rc-vs-clone-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`Clone` creates another owned copy (often expensive). `Rc` (single-threaded) shares one allocation via reference counts. Cross-thread sharing needs `Arc`; shared mutation needs interior mutability."
  },
  "own-partial-reborrow-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "Moving `p.name` partially moves out of `p`. Fields that were not moved stay usable (`p.age` here; `u32` is also `Copy`). Using the whole `p` needs every field, including the moved `name`, so that is rejected. Other non-`Copy` fields could still be moved out separately."
  },
  "own-drop-order-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "When a block ends, locals are dropped in reverse declaration order, so `b` prints `second` then `a` prints `first` (Reference, Destructors). Struct fields, by contrast, drop in declaration order. Leading-underscore names still drop; only a wildcard `let _ = ...` drops immediately."
  },
  "own-mem-take-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`mem::take` moves the old value out and writes `T::default()` into the place, so `old` is `\"hi\"` and `s` is an empty `String`. The place stays valid; nothing is cloned, and `Copy` is not required (`String` is not `Copy`). Use `mem::replace` when the stand-in is not `Default`."
  },
  "own-destructure-ref-mut-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "Rust's borrow checker understands disjoint field access for tuples and structs. In `let (ref mut x, ref y) = pair`, `x` holds an exclusive reference to `pair.0` (`&mut pair.0`) and `y` holds a shared reference to `pair.1` (`&pair.1`). Because the borrows target separate fields, they do not conflict and both are valid."
  },
  "own-drop-prevent-partial-move-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "Rust forbids moving individual fields out of types that implement the `Drop` trait (compiler error `E0509`). Because `drop(&mut self)` is called on the entire instance when it goes out of scope, leaving partially-moved or uninitialized fields would violate memory safety. To extract a field from a `Drop` type, you must take it through `std::mem::replace`, `Option::take`, or drop the wrapper entirely."
  },
  "own-supporter-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`ManuallyDrop<T>` inhibits the compiler from running the destructor of `T`. When `s` goes out of scope, its memory buffer is not freed unless `ManuallyDrop::drop(&mut s)` is called explicitly with `unsafe`."
  },
  "own-supporter-2": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`std::mem::swap` exchanges the values at two mutable locations by copying their bit patterns without running destructors. Exclusive `&mut` references ensure neither location is read or aliased during the swap."
  },
  "own-supporter-3": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "In Rust 2021 and 2024 editions, `[T; N]` implements `IntoIterator` yielding owned `T` values. Calling `for item in arr` moves the elements out of `arr`, making `arr` unavailable after the loop."
  },
  "own-supporter-4": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "Because `_ref` borrows the returned `usize` from `.len()`, the temporary `String` is not extended and is dropped at the end of the statement before `\"after let\"` prints."
  },
  "own-supporter-5": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`Option::take` replaces `self.task` with `None` and returns the `Some(String)` by value. This leaves `self.task` initialized while safely moving ownership out through `&mut self`."
  },
  "own-supporter-6": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "A pattern match guard cannot move values out of bindings because if the guard evaluates to `false`, matching continues to subsequent arms where the value must still be intact and initialized."
  },
  "own-supporter-7": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "In Rust, fields of structs and elements of tuples are dropped in order of declaration: index 0 then index 1 (`A` then `B`). In contrast, local variables in a block are dropped in reverse order."
  },
  "own-supporter-8": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "When a struct is partially moved, un-moved fields (like `user.age` and `user.active`) remain completely accessible. The struct as a whole cannot be used, but individual untouched fields can."
  },
  "own-supporter-9": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`ref s` creates a shared reference `&pair.0` without moving the `String`. `n` copies the `i32` (`Copy`). Because `pair.0` was only borrowed and `pair.1` is `Copy`, `pair.0` remains usable."
  },
  "own-supporter-10": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`Box<T>` is a pointer to heap memory. Moving `b1` into `b2` copies only the pointer metadata (usize) on the stack and invalidates `b1`. The 1024-byte heap allocation is untouched."
  },
  "own-supporter-11": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "In `if let Some(_) = make_temp()`, the temporary `Option<Logger>` created as the scrutinee lives for the duration of the if-let expression. It is dropped after the block body finishes, before `\"after if-let\"`."
  },
  "own-supporter-12": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "`mem::replace(&mut dest, src)` writes `src` into `dest` and returns the previous value that was in `dest`. Here, `prev` receives 5 and `count` becomes 10."
  },
  "own-supporter-13": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "In pattern matching, `_` does not bind a variable name. The value corresponding to `_` is moved and dropped immediately, leaving `a` as the sole owned binding."
  },
  "own-supporter-14": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "Methods taking `self` (by value) move the receiver out of the caller. Once `buf.consume()` completes, `buf` is consumed and can no longer be used."
  },
  "own-supporter-15": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Box::leak` consumes the `Box` without running its destructor and yields a mutable reference with lifetime `'static` (`&'static mut T`), valid for the remaining duration of the program."
  },
  "own-supporter-16": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "`Vec::into_iter` consumes the vector by value and yields owned elements (`String`), transferring ownership of each element to the loop body."
  },
  "own-supporter-17": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "In Rust, `&mut *s` reborrows from `s` for a shorter lifetime matching each loop iteration. Because the reborrow expires at the end of each iteration, `s` is available for the next iteration."
  },
  "own-supporter-18": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "When a variable might or might not be moved based on runtime conditions (e.g. `if cond`), the compiler generates a hidden boolean drop flag on the stack frame to track initialization status."
  },
  "own-supporter-19": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Cell<T>` never hands out references to its interior to avoid aliasing bugs. Therefore, `get()` must return an owned duplicate of `T`, requiring `T: Copy`. For non-`Copy` types, use `take()` or `replace()`."
  },
  "own-supporter-20": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "`let _ = Guard` does not create a variable binding; the expression is evaluated and dropped immediately. In contrast, `let _guard = Guard` creates a named binding that lives until the end of the enclosing block."
  },
  "own-supporter-21": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "Because `unwrap_or` takes `default: T` by value, the expression passed to it is evaluated eagerly before the method is called. To compute defaults lazily only when needed, use `unwrap_or_else(make_default)`."
  },
  "own-supporter-22": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "Rust understands disjoint field access for local structs. Because `u.name` and `u.age` occupy distinct memory locations, an exclusive borrow of `u.name` does not conflict with a shared borrow of `u.age`."
  },
  "own-supporter-23": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "Dereferencing `&Box<String>` yields the owned `Box<String>`, but moving out of a shared reference (`&T`) is forbidden because the referent must remain valid for other readers. Clone or borrow instead."
  },
  "own-supporter-24": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "Because `println!(\"{s}\")` only needs read-only access to `s`, the closure captures `s` by shared reference `&s`. Thus `s` remains fully valid in `main` after calling `c()`."
  },
  "own-supporter-25": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "The `move` keyword forces a closure to take ownership of all referenced outer variables by value (moving non-`Copy` types and copying `Copy` types), even if the body only reads them."
  },
  "own-supporter-26": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`String::from(\"hello\")` is a temporary owned value that is dropped at the end of the `let` statement. The returned `&[u8]` slice would be left pointing to deallocated heap memory, causing a compiler error."
  },
  "own-supporter-27": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "When an instance of a type implementing `Drop` is destroyed, its `drop(&mut self)` method is executed first, and then the compiler automatically runs the destructors for all its individual fields."
  },
  "own-supporter-28": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "Matching `Point { x, y }` by value moves both `x` and `y` out of `p`. Because `String` is not `Copy`, `p` is fully consumed and cannot be used after the `match` block."
  },
  "own-supporter-29": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`Vec::retain` takes `mut f: impl FnMut(&T) -> bool`. It passes a reference `&T` to the predicate so elements can be inspected without being moved out of the vector."
  },
  "own-supporter-30": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`ManuallyDrop::into_inner` consumes the `ManuallyDrop<T>` wrapper and extracts the contained `T` by value. Normal destructor semantics are restored for the returned value."
  },
  "own-supporter-31": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Rc::clone(&rc)` (or `rc.clone()`) is a cheap pointer copy that increments the strong reference count. Dereferencing `(*rc).clone()` invokes `Vec::clone`, duplicating all 10,000 bytes on the heap."
  },
  "own-supporter-32": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "When a temporary expression is directly bound by reference (`let ref x = ...` or `let x = &...`), Rust extends the temporary lifetime to match the scope of the enclosing block."
  },
  "own-supporter-33": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "In safe Rust, `let s = *b;` on an owned `Box<T>` moves the `T` out of the box and immediately deallocates the heap pointer memory without running the destructor on `T` (which is now owned by `s`)."
  },
  "own-supporter-34": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "When panic unwinding occurs (`panic = \"unwind\"`), Rust unwinds the stack frame by frame, running the destructors of all live local variables in reverse order of declaration."
  },
  "own-supporter-35": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Option::as_deref` converts `&Option<T>` (or `Option<T>`) into `Option<&T::Target>` by coercing the inner reference via `Deref`. For `String`, it returns `Option<&str>` without cloning or moving."
  },
  "own-box-leak-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Box::leak` consumes the `Box` and returns a mutable reference with `'static` lifetime without dropping the allocated heap memory."
  },
  "own-cow-clone-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`Cow` (Clone-on-Write) holds either borrowed data (`Cow::Borrowed`) or owned data (`Cow::Owned`), allocating only when modification (`to_mut()`) is required."
  },
  "own-pin-drop-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "The `Pin` drop guarantee ensures that once a value is pinned, its allocated memory will not be overwritten or reused until its `Drop` implementation is executed."
  },
  "own-manually-drop-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`ManuallyDrop<T>` disables the automatic destructor call for `T` when the wrapper goes out of scope, leaving cleanup responsibility to the programmer."
  },
  "own-slice-into-vec-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`to_vec` allocates a new `Vec<T>` on the heap and clones each element from the borrowed slice into the newly allocated buffer."
  },
  "own-temp-val-drop-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "In Rust, temporary values created within an expression are dropped at the end of the statement (the semicolon), unless extended by a `let` binding."
  },
  "own-into-raw-leak-1": {
    "correctIndex": 1,
    "categorySlug": "ownership",
    "explanation": "`Box::into_raw` consumes the `Box` and returns a raw pointer `*mut T`. The destructor is not run, transferring memory cleanup responsibility to the caller."
  },
  "own-rc-try-unwrap-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`Rc::try_unwrap` returns `Ok(value)` if there is exactly one strong reference (strong count = 1), consuming the `Rc`. Otherwise, it returns `Err(rc)`."
  },
  "own-box-from-raw-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "`Box::from_raw` requires that the pointer was previously obtained from `Box::into_raw` with the matching allocator layout, alignment, and type `T`."
  },
  "own-string-shrink-to-fit-1": {
    "correctIndex": 3,
    "categorySlug": "ownership",
    "explanation": "`shrink_to_fit()` requests the global allocator to reallocate the string's heap buffer to match its current length, releasing excess allocated capacity."
  },
  "own-mem-take-default-1": {
    "correctIndex": 0,
    "categorySlug": "ownership",
    "explanation": "`std::mem::take` moves the owned value out of `dest` and replaces it with `Default::default()`, leaving a valid value in the reference without allocating."
  },
  "own-drop-glue-order-1": {
    "correctIndex": 2,
    "categorySlug": "ownership",
    "explanation": "According to the Rust reference (§Destructors), the fields of a struct are dropped in declaration order (first field declared is dropped first)."
  },
  "ptr-box-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`Box<T>` owns a heap-allocated `T` exclusively. When the `Box` is dropped, the value is dropped too. Sharing needs `Rc`/`Arc`; interior mutability needs types like `RefCell`/`Mutex`."
  },
  "ptr-rc-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Rc::clone` bumps the strong reference count so multiple owners share one allocation. The inner `String` is not deep-copied. The value is dropped when the last strong `Rc` goes away."
  },
  "ptr-rc-thread-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`Rc` uses non-atomic reference counts and is neither `Send` nor `Sync`. Sharing across threads needs `Arc`, which uses atomic counting."
  },
  "ptr-arc-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Arc<T>` is a thread-safe reference-counted pointer for shared ownership across threads. Mutation of the inner data still needs something like `Mutex`/`RwLock` unless the data is immutable."
  },
  "ptr-weak-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`Weak<T>` is non-owning. `upgrade()` returns `Some(Rc<T>)` if strong owners still exist, otherwise `None`. It does not panic and does not return a plain reference."
  },
  "ptr-rc-downgrade-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`Rc::downgrade(&rc)` produces a `Weak<T>` pointing at the same allocation without increasing the strong count."
  },
  "ptr-refcell-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`RefCell` enforces the same exclusive/shared rules dynamically. A second conflicting borrow panics (or `try_borrow_mut` returns `Err`). It is not thread-safe and does not block."
  },
  "ptr-refcell-2": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`RefCell<T>` provides interior mutability with runtime borrow checking: many `borrow`s or one `borrow_mut`, never both. Violations panic. It is not `Sync`."
  },
  "ptr-rc-refcell-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`Rc` enables multiple owners; by itself the inner `T` is immutable through those owners. Wrapping with `RefCell` adds single-threaded interior mutability so owners can `borrow_mut`. The combo is still not thread-safe."
  },
  "ptr-cell-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`Cell<T>` offers simple interior mutability: `set`/`get`/`replace` work through a shared reference. It is not thread-safe and does not hand out long-lived `&mut T` aliases."
  },
  "ptr-cell-2": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`get` copies the contained value out. Restricting it to `T: Copy` avoids moving a non-`Copy` value through a shared reference. Non-`Copy` types use `replace`/`take` (when `T: Default`) or `RefCell` instead. `Cell` is not `Sync`."
  },
  "ptr-mutex-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "The mutex guard represents “lock held.” When it drops, it unlocks. Returning a bare `&mut T` would not automatically release the lock or tie the borrow to the critical section."
  },
  "ptr-arc-mutex-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Arc` gives thread-safe shared ownership of the mutex. `Mutex` provides synchronized exclusive access to `T`. You need both when multiple threads must mutate shared state."
  },
  "ptr-cow-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "If the `Cow` is `Borrowed`, `to_mut` clones the data into the `Owned` variant, then returns a mutable reference to that owned value. If it is already owned, no clone is needed. Borrowed `&str` data itself is never mutated in place."
  },
  "ptr-box-deref-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Box<T>` implements `Deref<Target = T>`, so `&Box<String>` can coerce to `&String`. The types are not identical, and no clone of the `String` is required for this call."
  },
  "ptr-rc-get-mut-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`Rc::get_mut` yields `&mut T` only when this handle is exclusive: strong count 1 and weak count 0. Another `Rc` or any `Weak` makes it return `None`. Shared mutation otherwise needs interior mutability."
  },
  "ptr-arc-clone-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`Arc::clone` bumps an atomic strong count so another owner shares the same allocation. It does not clone the inner `Vec`. Use `(*a).clone()` / `T: Clone` when you need a deep copy."
  },
  "ptr-pin-intro-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`Pin` constrains moving certain values in memory, which is critical for self-referential structs and many `Future` state machines. It is not about making things `Copy` or allocating by itself."
  },
  "ptr-rc-cycle-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "If two `Rc` values point at each other (a cycle), strong counts never reach zero and the allocations are never dropped, a memory leak. Use `Weak` for non-owning back-edges (parent/child graphs)."
  },
  "ptr-box-leak-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`Box::leak` consumes the `Box` and returns a `&'static mut T` (or shared) to the heap value, which will not be freed. Useful for certain global tables; misuse is just a deliberate leak."
  },
  "ptr-into-raw-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`Box::into_raw` consumes the `Box` and returns `*mut T` without running the destructor. The caller owns the allocation and should later `Box::from_raw` (typical FFI round-trip) or free it with the matching layout. `Box::leak` instead yields a `'static` reference meant to last for the rest of the program."
  },
  "ptr-once-cell-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`OnceCell::set` initializes an empty cell (`Ok(())`) and returns `Err(value)` if it was already set, leaving the original contents. `get` then yields `&T` without `RefCell`-style runtime borrow flags. `OnceCell` is `!Sync`; the thread-safe counterpart is `OnceLock`."
  },
  "ptr-cow-to-mut-allocation-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`Cow::to_mut` returns a mutable reference to the owned value. If the `Cow` is currently `Cow::Borrowed`, `to_mut()` invokes `clone()` on the borrowed data, mutates the `Cow` in-place to `Cow::Owned`, and returns a mutable reference to the newly created owned buffer (here, a `String`). Subsequent mutations modify this owned buffer."
  },
  "ptr-pin-primitive-unpin-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`Pin` prevents values from moving in memory unless their type implements `Unpin`. Primitive types like `i32`, `f64`, `&T`, and most standard types implement the auto trait `Unpin` because they have no self-referential pointers or pinning invariants. When `T: Unpin`, `Pin::new` is safe and `Pin<&mut T>` implements `DerefMut`, allowing safe mutable access to the underlying value."
  },
  "ptr-supporter-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "When `T: !Unpin`, `Pin<&mut T>` prevents moving `T` out of its memory location (e.g. via `std::mem::swap` or `replace`) until `T` is dropped, making self-referential structures completely sound."
  },
  "ptr-supporter-2": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "Under the Strict Provenance model, `ptr.addr()` inspects the address portion of a pointer without \"exposing\" its provenance to the compiler's alias analysis, preserving optimization opportunities and memory sanitizer checks."
  },
  "ptr-supporter-3": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`Cow::to_mut` checks the variant: if `Borrowed`, it clones the data into `Owned` (allocating on the heap) and mutates in place; if already `Owned`, it returns `&mut Owned` with zero new allocations."
  },
  "ptr-supporter-4": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`NonNull<T>` is guaranteed to be non-null and covariant over `T`. This invariant enables the compiler to use `0` as the `None` representation for `Option<NonNull<T>>` (niche optimization)."
  },
  "ptr-supporter-5": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::read(src)` creates an owned `T` by bitwise copying from the raw pointer `src` without running any destructor on `src`. The caller must ensure `src` is not double-dropped."
  },
  "ptr-supporter-6": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::write(dest, src)` overwrites memory at `dest` with `src` without reading or dropping the previous content. This is essential for initializing `MaybeUninit` memory where dropping garbage would cause UB."
  },
  "ptr-supporter-7": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`MaybeUninit::assume_init()` asserts to the compiler that the contained `T` is fully initialized with a valid bit representation. Calling it on uninitialized memory is instant Undefined Behavior (UB)."
  },
  "ptr-supporter-8": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "Pin projection is the technique of converting `Pin<&mut Struct>` into `Pin<&mut Field>` (for structurally pinned fields) or `&mut OtherField` (for unpinned fields) while upholding pinning invariants."
  },
  "ptr-supporter-9": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "Creating multiple raw pointers (`*const T` or `*mut T`) that point to the same memory is permitted in safe Rust code. Undefined Behavior only arises if dereferencing violates Rust's aliasing or validity rules in `unsafe`."
  },
  "ptr-supporter-10": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Box::from_raw` assumes the pointer was allocated by the global allocator with the exact layout of `T`. Calling it on stack pointers or mismatched types causes memory corruption when `Box` drops."
  },
  "ptr-supporter-11": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`RefCell::try_borrow_mut` is the non-panicking counterpart of `borrow_mut()`. If an existing borrow is active, it returns `Err(BorrowMutError)` allowing the caller to handle the conflict gracefully."
  },
  "ptr-supporter-12": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::copy` is equivalent to C `memmove` (handles overlapping memory regions correctly). `copy_nonoverlapping` is equivalent to `memcpy` and requires source and destination to be non-overlapping for performance."
  },
  "ptr-supporter-13": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::null<T>()` returns a `*const T` null pointer, while `std::ptr::null_mut<T>()` returns a `*mut T` null pointer."
  },
  "ptr-supporter-14": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`ptr.add(count)` is unsafe because the caller must guarantee that both the starting pointer and the resulting pointer remain within the bounds of the same allocated object (or one byte past the end)."
  },
  "ptr-supporter-15": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`&[u8]` is a slice fat pointer consisting of two words: a pointer to the first element and a `usize` length. In contrast, `&i32`, `*const f64`, and `Box<u64>` are thin single-word pointers."
  },
  "ptr-supporter-16": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`Pin::new_unchecked` is unsafe because the caller must uphold the Pin contract: once pinned, the memory location occupied by the pointee must not be overwritten, moved, or deallocated without dropping."
  },
  "ptr-supporter-17": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`Pin::as_mut` reborrows the pinned reference as `Pin<&mut T>` for a shorter lifetime, allowing multiple method calls on the pinned object without consuming the original `Pin` handle."
  },
  "ptr-supporter-18": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`PhantomPinned` is a marker type that does not implement `Unpin`. Adding a `_pin: PhantomPinned` field makes the enclosing struct `!Unpin`, enabling compile-time pinning guarantees."
  },
  "ptr-supporter-19": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`ptr.is_null()` is a safe method on raw pointers (`*const T` and `*mut T`) that returns `true` if the pointer points to address zero."
  },
  "ptr-supporter-20": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::eq` compares the raw memory addresses of two references, returning `true` only if they point to the exact same memory location, bypassing any `PartialEq` implementation."
  },
  "ptr-supporter-21": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::drop_in_place(ptr)` executes the destructor (`Drop::drop`) and field drop glue for the value at `ptr` in-place, without deallocating the memory holding it."
  },
  "ptr-supporter-22": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`std::mem::size_of_val(val)` inspects the metadata of the dynamically sized slice and returns the size of the underlying elements: 3 `i32`s * 4 bytes = 12 bytes."
  },
  "ptr-supporter-23": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`Cell<T>` has identical memory layout and size as `T` with zero runtime check overhead. It achieves interior mutability purely through compile-time ownership rules (`get`/`set` by value)."
  },
  "ptr-supporter-24": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "In Rust, all reference and standard raw pointer dereferences (`*ptr`) require the pointer to be properly aligned for `T`. To read unaligned memory safely, you must use `std::ptr::read_unaligned`."
  },
  "ptr-supporter-25": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::read_unaligned` reads a `T` from a raw pointer that may not be aligned to `align_of::<T>()`. It is common in network packet parsing and binary protocol deserialization."
  },
  "ptr-supporter-26": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Box::new_in` (and the `Allocator` API) allows collections and smart pointers to allocate and deallocate their heap memory using custom memory managers (such as arenas or pool allocators)."
  },
  "ptr-supporter-27": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "Reference cycles (where node A owns node B and node B owns node A via strong `Rc`) prevent reference counts from ever reaching 0, leaking memory. Using `Weak` for back-references breaks the cycle."
  },
  "ptr-supporter-28": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`Unpin` is an auto trait. If all fields in a struct implement `Unpin` (which all normal standard library types do), the struct automatically implements `Unpin`."
  },
  "ptr-supporter-29": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`DerefMut` is defined as `pub trait DerefMut: Deref { fn deref_mut(&mut self) -> &mut Self::Target; }`. It does not declare a new associated type; it reuses `Deref::Target`."
  },
  "ptr-supporter-30": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Box::pin(x)` allocates `x` on the heap and returns `Pin<Box<T>>`. Because heap allocations have stable addresses that do not move unless explicitly reallocated, heap pinning is safe for any type."
  },
  "ptr-supporter-31": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "Casting pointer types (e.g. `ptr as *const u64`) is allowed in safe code. The pointer only becomes subject to safety requirements (such as alignment and validity) when dereferenced."
  },
  "ptr-supporter-32": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`std::ptr::slice_from_raw_parts` builds a raw slice pointer (`*const [T]`). Unlike `std::slice::from_raw_parts` (which returns a reference `&[T]`), creating the raw pointer is safe and does not assert reference validity."
  },
  "ptr-supporter-33": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`ptr::without_provenance(addr)` creates a pointer with zero provenance. It cannot be legally dereferenced for memory access, but is useful for sentinel values and address comparisons."
  },
  "ptr-supporter-34": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`Pin::set(&mut self, value)` safely overwrites the pinned location: it drops the existing value in place (`drop_in_place`) and writes `value` into the exact same memory address, upholding the pin contract."
  },
  "ptr-supporter-35": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`UnsafeCell::get(&self)` takes an immutable shared reference `&self` and returns a raw mutable pointer `*mut T` to the wrapped value, providing the foundational primitive for all interior mutability in Rust."
  },
  "ptr-addr-of-mut-uninit-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`addr_of_mut!` directly computes the raw pointer to a memory location without creating an intermediate `&mut T` reference, which requires validity."
  },
  "ptr-null-mut-cast-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "Dereferencing a null pointer is undefined behavior (UB) in Rust, which can cause segfaults, compiler miscompilations, or crashes."
  },
  "ptr-nonnull-dangling-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`NonNull::dangling()` returns a properly aligned, non-null pointer without allocating memory, widely used by collections (like empty `Vec`) as a sentinel."
  },
  "ptr-offset-from-isize-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`offset_from` requires that both pointers point to or one past the end of the same allocated object and that their distance in bytes fits in `isize`."
  },
  "ptr-provenance-expose-addr-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`expose_addr` converts a pointer to `usize` while exposing its provenance, signaling to compiler optimization passes that this address may later be cast back to a pointer."
  },
  "ptr-align-of-val-raw-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`align_of_val_raw` computes type alignment from a raw pointer without requiring a valid reference, making it sound even for uninitialized or unaligned memory."
  },
  "ptr-copy-nonoverlapping-memcpy-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "`copy_nonoverlapping` requires that the source and destination memory regions do not overlap (equivalent to `memcpy`). For overlapping memory, use `ptr::copy` (memmove)."
  },
  "ptr-read-unaligned-soundness-1": {
    "correctIndex": 3,
    "categorySlug": "pointers",
    "explanation": "`read_unaligned` reads a value `T` from memory without requiring the pointer to be aligned to `align_of::<T>()`, preventing hardware alignment faults."
  },
  "ptr-write-bytes-zeroing-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`write_bytes` sets `count * size_of::<T>()` contiguous bytes to the given byte value (like `memset`), without invoking `Drop` on existing contents."
  },
  "ptr-slice-from-raw-parts-len-1": {
    "correctIndex": 2,
    "categorySlug": "pointers",
    "explanation": "`slice_from_raw_parts` merely packages a data pointer and length into a fat raw pointer `*const [T]`. Because raw pointers are not dereferenced, the constructor itself is safe."
  },
  "ptr-strict-provenance-with-addr-1": {
    "correctIndex": 0,
    "categorySlug": "pointers",
    "explanation": "`with_addr` creates a new raw pointer with the address `new_addr` while preserving the exact provenance (allocation permission) of `self`."
  },
  "ptr-null-dangling-zst-compare-1": {
    "correctIndex": 1,
    "categorySlug": "pointers",
    "explanation": "Because zero-sized types occupy 0 bytes, reading or writing a ZST through a properly aligned, non-null (even dangling) pointer performs zero memory accesses and is sound."
  },
  "trait-def-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "A trait declares method signatures (and optional defaults) that describe shared behavior. Types `impl` the trait to provide those methods. Traits do not define class hierarchies or field inheritance."
  },
  "trait-bound-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`T: Display + Clone` means `T` must implement every listed trait. The caller must supply a type that is both `Display` and `Clone`."
  },
  "trait-default-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Traits may supply default method bodies. An empty `impl Summary for NewsArticle {}` is valid and inherits that default `summarize` unless you override it."
  },
  "trait-derive-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "`#[derive(Debug)]` runs a derive macro that generates an `impl Debug for Point`, enabling formatting with `{:?}`. It does not change mutability or compiler logging."
  },
  "trait-dyn-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`Dog` and `Cat` have different sizes. Boxing them as `dyn Animal` gives a uniform fat-pointer element type so one `Vec` can hold mixed implementors behind dynamic dispatch."
  },
  "trait-impltrait-two-args-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Each `impl Summary` in argument position is its own type parameter. The two arguments may therefore be different concrete types, as long as both implement `Summary`."
  },
  "trait-bound-same-type-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "One `T` means one concrete type for that call. Both parameters must therefore be the same type that implements `Summary`, unlike two separate `impl Summary` parameters."
  },
  "trait-multi-bounds-plus-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "The `+` syntax combines bounds. The caller must provide a type that implements both `Summary` and `Display`. Rust has no trait subtyping hierarchy like classical OOP."
  },
  "trait-where-clause-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Here `where` only relocates bounds that could also be written as `T: Display + Clone` and `U: Clone + Debug` after each type parameter. That inline form is a shorthand for simple parameter bounds. `where` is the general place for bounds and does not change dispatch, performance, or force `T` and `U` to match."
  },
  "trait-where-needed-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Inline `T: Trait` after a type parameter is only a shorthand for bounds on that parameter. A `where` clause can bound arbitrary types, such as `Option<T>: Debug` here (the value being printed). That form cannot be written as a plain `T: …` parameter shorthand. `where` is the general bound syntax; the angle-bracket form is the limited shorthand."
  },
  "trait-return-impl-single-type-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`-> impl Summary` means “some single concrete type that implements `Summary`.” Returning `NewsArticle` or `SocialPost` depending on `switch` needs a trait object (or an enum) instead."
  },
  "trait-object-unsized-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "`dyn Draw` is a DST (dynamically sized). Put it behind a pointer such as `Box<dyn Draw>` (or `&dyn Draw`) so `Vec` has a sized element type."
  },
  "trait-object-vtable-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "Per the Reference, `dyn Draw` is the trait object (an unsized opaque value). Pointers to it (`&dyn Draw`, `Box<dyn Draw>`) are fat: a pointer to the concrete value plus a vtable for dynamic dispatch."
  },
  "trait-objects-vs-generics-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "`ScreenB<T>` is homogeneous: one concrete `T` per instance. `ScreenA` can hold mixed `Draw` implementors via trait objects. Generics use static dispatch (monomorphization); trait objects use dynamic dispatch."
  },
  "trait-dispatch-static-vs-dynamic-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Calls through `dyn Draw` use dynamic dispatch: the vtable selects the concrete `draw` at runtime. Generics would monomorphize instead."
  },
  "trait-blanket-impl-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "A blanket implementation applies to every type that satisfies the bound. The standard library uses this so any `Display` type automatically gets `to_string()`."
  },
  "trait-orphan-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "You may implement a trait for a type if your crate defines the trait or the type (with some allowed coverings). `MyTrait` is yours, so `impl MyTrait for String` is fine. Implementing foreign traits for foreign types (`Display`/`Clone`/`Debug` for std types) is not."
  },
  "trait-supertrait-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "`trait OutlinePrint: Display` means any type that implements `OutlinePrint` must also implement `Display`. The default method can then call `Display`/`ToString` APIs. It does not auto-implement `Display`."
  },
  "trait-associated-type-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "An associated type is a type placeholder fixed by each `impl`. For `Iterator`, `Item` is the element type produced by that iterator. Callers use `I::Item` rather than adding another type parameter on every use."
  },
  "trait-object-safety-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "A trait must be dyn-compatible (formerly called object-safe) to be used as `dyn Trait`. Methods that return `Self` by value are not dyn-compatible, because the concrete size isn’t known for `dyn Bad`. Methods taking `Self` by value are similarly problematic. Use generics, or redesign the API (e.g. return `Box<dyn Bad>`)."
  },
  "trait-from-into-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "The standard library provides `impl<T, U: From<T>> Into<U> for T`. Implement `From`, and `.into()` becomes available. Implementing both manually is unnecessary and can conflict."
  },
  "trait-sized-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "By default, generic `T` has an implicit `Sized` bound. `T: ?Sized` allows unsized types like `str` or `dyn Trait` when they appear behind pointers/references. It does not allocate by itself."
  },
  "trait-drop-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "`Drop::drop` runs automatically at end of scope (and via `mem::drop`). Calling `Drop::drop` yourself is disallowed. `Copy` and `Drop` are mutually exclusive."
  },
  "trait-borrow-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "`Borrow` relates owned and borrowed views that hash/compare consistently. That is why `HashMap<String, _>::get` accepts `&str`. It does not clone the key for the lookup."
  },
  "trait-upcast-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Since Rust 1.86, a trait object can be coerced to a supertrait object (`&dyn Sub` → `&dyn Super`, and similarly for `Box`/`Arc`/raw pointers). Arbitrary unrelated traits do not coerce. An `as_super` helper is no longer required for this case."
  },
  "trait-asref-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "`AsRef<str>` is a cheap reference-to-reference conversion. Both `&str` and `String` implement it, so the generic function accepts either without cloning. `String` is not a subtype of `str`; you typically go through `Deref`/`AsRef`. Prefer `Borrow` when hash/eq must match an owned key."
  },
  "trait-assoc-const-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Traits may declare associated constants. Each impl supplies the value (`const MAX: u32 = 255`). Access it as `<Type as Trait>::MAX` (or `Type::MAX` if unambiguous). It is not a `static` item and does not need a runtime call."
  },
  "trait-gat-associated-lifetime-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "In standard `std::iter::Iterator`, `type Item` has no lifetime parameter, meaning `Item` must be chosen independently of the `&mut self` borrow in `next()`. Generic Associated Types (GATs) allow declaring `type Item<'a>`, so the yielded item type can explicitly borrow from `&mut self` with lifetime `'a`. This enables lending / streaming iterators."
  },
  "trait-disambiguation-inherent-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "When resolving method calls via dot syntax (`u.say()`), Rust checks inherent methods on the concrete type before looking up trait methods in scope. Therefore, `User::say` takes precedence and prints `\"inherent greet\"`. To call the trait implementation, use fully qualified syntax: `Greet::say(&u)` or `<User as Greet>::say(&u)`."
  },
  "trait-supporter-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Modern Rust supports Trait Object Upcasting, allowing a `dyn Sub` (where `trait Sub: Super`) to be implicitly coerced to `dyn Super` by adjusting the vtable pointer."
  },
  "trait-supporter-2": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "Methods taking `self` by value or generic parameters violate object safety. Adding `where Self: Sized` excludes that method from the `dyn Trait` vtable while keeping it available on concrete sized types."
  },
  "trait-supporter-3": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Associated types require each implementing type to specify a concrete type (`type Item = ...;`) in its `impl` block."
  },
  "trait-supporter-4": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "When multiple implemented traits have overlapping method names, Fully Qualified Syntax `<Type as Trait>::method(...)` unambiguously tells the compiler which trait method to call."
  },
  "trait-supporter-5": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "In Rust's method resolution order, inherent methods are checked first before trait methods in scope. Therefore `Person.hello()` calls the inherent implementation."
  },
  "trait-supporter-6": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "The orphan rule ensures coherence: you can implement trait `T` for type `U` only if either `T` or `U` is defined in your local crate, preventing conflicting implementations across independent crates."
  },
  "trait-supporter-7": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "A blanket implementation implements a trait for any type `T` that satisfies certain trait bounds (e.g. `ToString` for any `T: Display`)."
  },
  "trait-supporter-8": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "`Borrow<Borrowed>` requires that `Eq`, `Ord`, and `Hash` for a borrowed value act identically to the owned value, which is crucial for collections like `HashMap` and `BTreeSet`. `AsRef` makes no such behavioral guarantees."
  },
  "trait-supporter-9": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "The clause `where Self: 'a` ensures that `Item<'a>` is only well-formed when `Self` outlives 'a, preventing the associated type from referencing deallocated parts of `Self`."
  },
  "trait-supporter-10": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "A trait object reference `&dyn Trait` is a 16-byte fat pointer containing: 1) a pointer to the concrete data, and 2) a pointer to the virtual method table (vtable) containing function pointers and drop glue."
  },
  "trait-supporter-11": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "`trait Dog: Animal` states that `Animal` is a supertrait of `Dog`. Any concrete type implementing `Dog` is required by the compiler to also provide an `impl Animal` block."
  },
  "trait-supporter-12": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Associated constants in traits are evaluated during compilation and can be used in array size expressions, const generics, and static assertions (e.g. `[u8; Square::ROWS]`)."
  },
  "trait-supporter-13": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "The Rust standard library provides a blanket implementation: `impl<T, U> Into<U> for T where U: From<T>`. Implementing `From` automatically grants the corresponding `Into` implementation."
  },
  "trait-supporter-14": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`TryFrom<T>` defines `type Error;` and returns `Result<Self, Self::Error>`, allowing the implementor to choose a custom error type for fallible conversions."
  },
  "trait-supporter-15": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "`Copy` types are duplicated via simple bitwise memory copies (`memcpy`). If a `Copy` type implemented `Drop`, the compiler would have no way to track how many copies exist or when to run their destructors."
  },
  "trait-supporter-16": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Marker traits contain no methods or associated types. They serve purely to convey compile-time type-level properties and guarantees (e.g. `Send`, `Sync`, `Unpin`, `Copy`) to the compiler and type system."
  },
  "trait-supporter-17": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "`DerefMut` relies on the `type Target` associated type declared in `Deref`. Therefore, any type implementing `DerefMut` must implement `Deref` with a matching target."
  },
  "trait-supporter-18": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`trait Add<Rhs = Self>` uses a generic parameter default. When implementing `Add for Meters` without specifying `<Rhs>`, `Rhs` defaults to `Self` (`Meters`)."
  },
  "trait-supporter-19": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Full trait specialization is unstable because lifetime information is erased at compile time. Specializing based on types can create soundness holes where lifetime subtyping interacts with specialized implementations."
  },
  "trait-supporter-20": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Because `T` could be any type, the compiler cannot generate a finite vtable containing pointers to every possible monomorphized version of `transform<T>`. Thus, generic methods violate object safety unless guarded with `where Self: Sized`."
  },
  "trait-supporter-21": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Auto traits like `Send` and `Sync` are automatically implemented for a struct if and only if all of its fields implement `Send` and `Sync`. Negative `impl !Send` blocks can opt out."
  },
  "trait-supporter-22": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "Raw pointers (`*const T`, `*mut T`) do not implement `Send` or `Sync`. Including `PhantomData<*const ()>` in a struct automatically opts the struct out of `Send` and `Sync`."
  },
  "trait-supporter-23": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "The hierarchy is: `pub trait Fn(): FnMut()`, and `pub trait FnMut(): FnOnce()`. Therefore, any closure implementing `Fn` also implements `FnMut` and `FnOnce`."
  },
  "trait-supporter-24": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "`impl Trait` in return position uses static dispatch: the compiler determines the concrete type at compile time, avoiding the heap allocation and vtable indirection of `Box<dyn Trait>`."
  },
  "trait-supporter-25": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "`impl Trait` hides the concrete type name but still resolves to a single static type at compile time. Because `Range` and `StepBy<Range>` are different types, you must use `Box<dyn Iterator>` or `Either` to return different types."
  },
  "trait-supporter-26": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`CoerceUnsized` is the compiler-supported trait that allows pointer-like structs (such as `Box`, `Rc`, `Arc`) to participate in unsizing coercions (e.g. `Box<T>` to `Box<dyn Trait>` or `Box<[T]>`)."
  },
  "trait-supporter-27": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "`DispatchFromDyn` is the internal trait that allows a type to act as a method receiver for trait objects (like `self: CustomPtr<Self>`)."
  },
  "trait-supporter-28": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "`Unpin` indicates that pinning has no effect: instances can be safely moved out of `Pin` wrappers because they contain no self-referential pointers."
  },
  "trait-supporter-29": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Because `Rc` is `!Send` and `!Sync`, including `PhantomData<Rc<()>>` causes the containing struct to also become `!Send` and `!Sync` by auto trait derivation rules."
  },
  "trait-supporter-30": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`dyn Trait` is a Dynamically Sized Type (DST) with unknown size at compile time. It can only be used behind pointers like `&dyn Trait` (which is 16 bytes). Calling `size_of::<dyn Trait>()` fails to compile because `size_of` requires `T: Sized`."
  },
  "trait-supporter-31": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "The associated type equality constraint `Item = u32` restricts generic type `I` to implementations of `Iterator` where the associated type `Item` matches `u32`."
  },
  "trait-supporter-32": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Default method implementations provide a fallback. Any implementing type can optionally override the default implementation to provide specialized or optimized behavior."
  },
  "trait-supporter-33": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Use an associated type when there should only be one implementation of the trait per type (e.g. a `Vec<i32>` iterator only yields `i32`). Use a generic parameter when multiple implementations are desired (e.g. `From<T>` for multiple `T`)."
  },
  "trait-supporter-34": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "Because `Box<T>` implements `Deref<Target = T>`, `&Box<dyn Worker>` deref-coerces to `&dyn Worker`, allowing seamless function passing."
  },
  "trait-supporter-35": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Placing bounds on `impl` blocks allows constructing and manipulating `Container<T>` for any `T`, unlocking specific methods only when `T` satisfies the necessary trait bounds without restricting the struct itself."
  },
  "trait-supertrait-dyn-upcast-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Rust provides trait upcasting coercion, allowing references to `dyn SubTrait` to be implicitly coerced to `dyn SuperTrait` via embedded supertrait vtables."
  },
  "trait-associated-const-bound-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "An associated constant without a default value must be defined as a compile-time constant by every type implementing the trait."
  },
  "trait-object-safe-sized-self-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Adding `where Self: Sized` to a method excludes it from the `dyn Trait` vtable, allowing the overall trait to remain object safe while providing non-dyn methods for sized types."
  },
  "trait-gats-lifetime-param-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "GATs allow associated types to declare generic type or lifetime parameters (e.g. `type Item<'a>;`), enabling lending iterators and generic collection abstractions."
  },
  "trait-impl-trait-return-pos-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "`impl Trait` in return position hides the concrete type from caller signatures while monomorphizing statically with zero runtime vtable overhead."
  },
  "trait-orphan-rule-blanket-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "The orphan rule (trait coherence) requires that either the trait or the type being implemented must be defined in the current crate, preventing conflicting external implementations."
  },
  "trait-coherence-covered-type-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Under RFC 2451 coherence rules, a type parameter `T` is \"covered\" if it appears as an argument to a local type constructor (like `MyStruct<T>`), allowing trait impls on foreign traits."
  },
  "trait-marker-traits-zero-size-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Marker traits declare no methods or associated items. They serve as zero-overhead compile-time tags indicating structural properties (e.g. thread safety or bitwise copyability)."
  },
  "trait-associated-type-defaults-1": {
    "correctIndex": 1,
    "categorySlug": "traits",
    "explanation": "Associated type defaults allow trait definitions to specify a default type (e.g. `type Output = Self;` in `Add`), which implementors can omit unless customizing."
  },
  "trait-dispatch-vtable-layout-1": {
    "correctIndex": 3,
    "categorySlug": "traits",
    "explanation": "`&dyn Trait` is a two-word fat pointer: the first word points to the concrete instance data, and the second points to the static vtable containing type size, align, drop, and method pointers."
  },
  "trait-auto-traits-negative-impl-1": {
    "correctIndex": 0,
    "categorySlug": "traits",
    "explanation": "Negative trait implementations (`impl !Trait`) explicitly opt a type out of an auto trait (like `Send` or `Sync`), preventing the compiler from automatically deriving it."
  },
  "trait-unsize-coercion-slice-1": {
    "correctIndex": 2,
    "categorySlug": "traits",
    "explanation": "Unsize coercion converts a thin reference to a sized array `&[T; N]` into a fat slice reference `&[T]` by attaching the length `N` into pointer metadata with zero runtime cost."
  }
}
