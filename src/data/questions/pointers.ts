import type { Question } from '../../lib/quiz'

export const pointersQuestions: Question[] = [
  {
    id: 'ptr-box-1',
    categorySlug: 'pointers',
    title: 'Box Ownership',
    prompt: 'What does `Box<T>` primarily provide?',
    tags: ['pointers', 'box'],
    difficulty: 1,
    language: 'rust',
    code: `let b = Box::new(5);
println!("{b}");`,
    options: [
      { label: 'A', text: "Shared ownership with automatic reference counts" },
      { label: 'B', text: "Interior mutability checked only at runtime in code" },
      { label: 'C', text: "Thread-safe mutation without locking primitives" },
      { label: 'D', text: "Exclusive heap ownership of a single `T` value" },
    ],
    correctIndex: 3,
    hint: '`Box` is the simplest owning heap pointer.',
    explanation:
      '`Box<T>` owns a heap-allocated `T` exclusively. When the `Box` is dropped, the value is dropped too. Sharing needs `Rc`/`Arc`; interior mutability needs types like `RefCell`/`Mutex`.',
  },
  {
    id: 'ptr-rc-1',
    categorySlug: 'pointers',
    title: 'Rc Shared Ownership',
    prompt: 'What does cloning an `Rc<T>` do?',
    tags: ['pointers', 'rc'],
    difficulty: 2,
    language: 'rust',
    code: `use std::rc::Rc;
let a = Rc::new(String::from("hi"));
let b = Rc::clone(&a);`,
    options: [
      { label: 'A', text: 'Deep-copies the `String` into a second allocation' },
      { label: 'B', text: 'Increments the strong count for shared ownership' },
      { label: 'C', text: 'Moves ownership out of `a`, leaving it unusable' },
      { label: 'D', text: 'Creates a `Weak` handle that does not keep data alive' },
    ],
    correctIndex: 1,
    hint: '`Rc::clone` shares; it does not clone the inner `T` by default.',
    explanation:
      '`Rc::clone` bumps the strong reference count so multiple owners share one allocation. The inner `String` is not deep-copied. The value is dropped when the last strong `Rc` goes away.',
  },
  {
    id: 'ptr-rc-thread-1',
    categorySlug: 'pointers',
    title: 'Rc and Threads',
    prompt: 'Why can’t you send an `Rc<T>` to another thread?',
    tags: ['pointers', 'rc', 'send'],
    difficulty: 2,
    language: 'rust',
    code: `use std::rc::Rc;
use std::thread;

let data = Rc::new(5);
thread::spawn(move || {
    println!("{data}");
});`,
    options: [
      { label: 'A', text: '`Rc` is not `Send`; its counts aren’t atomic' },
      { label: 'B', text: '`Rc` cannot wrap integer types like `i32`' },
      { label: 'C', text: '`thread::spawn` forbids all heap allocations' },
      { label: 'D', text: '`move` closures cannot capture smart pointers' },
    ],
    correctIndex: 0,
    hint: 'Compare `Rc`’s counters with `Arc`’s.',
    explanation:
      '`Rc` uses non-atomic reference counts and is neither `Send` nor `Sync`. Sharing across threads needs `Arc`, which uses atomic counting.',
  },
  {
    id: 'ptr-arc-1',
    categorySlug: 'pointers',
    title: 'Arc Meaning',
    prompt: 'What is `Arc<T>` primarily for?',
    tags: ['pointers', 'arc'],
    difficulty: 2,
    language: 'rust',
    code: `use std::sync::Arc;
let a = Arc::new(vec![1, 2, 3]);
let b = Arc::clone(&a);`,
    options: [
      { label: 'A', text: 'Faster heap allocation than `Box` for large values' },
      { label: 'B', text: 'Thread-safe shared ownership across threads' },
      { label: 'C', text: 'Exclusive move-only ownership for concurrency' },
      { label: 'D', text: 'Interior mutability without any synchronization' },
    ],
    correctIndex: 1,
    hint: '`Arc` = atomically reference counted.',
    explanation:
      '`Arc<T>` is a thread-safe reference-counted pointer for shared ownership across threads. Mutation of the inner data still needs something like `Mutex`/`RwLock` unless the data is immutable.',
  },
  {
    id: 'ptr-weak-1',
    categorySlug: 'pointers',
    title: 'Weak Upgrade',
    prompt: 'What does `Weak::upgrade` return?',
    tags: ['pointers', 'weak', 'rc'],
    difficulty: 3,
    language: 'rust',
    code: `use std::rc::{Rc, Weak};
let strong = Rc::new(5);
let weak: Weak<i32> = Rc::downgrade(&strong);
let maybe = weak.upgrade();`,
    options: [
      { label: 'A', text: '`Option<Rc<T>>`: `None` if the value is gone' },
      { label: 'B', text: '`Result<Rc<T>, BorrowError>` on contention' },
      { label: 'C', text: 'A bare `&T` borrowed from the weak handle' },
      { label: 'D', text: 'An `Rc<T>` that panics if the allocation died' },
    ],
    correctIndex: 0,
    hint: '`Weak` does not keep the allocation alive by itself.',
    explanation:
      '`Weak<T>` is non-owning. `upgrade()` returns `Some(Rc<T>)` if strong owners still exist, otherwise `None`. It does not panic and does not return a plain reference.',
  },
  {
    id: 'ptr-rc-downgrade-1',
    categorySlug: 'pointers',
    title: 'Creating Weak',
    prompt: 'How do you create a `Weak<T>` from an `Rc<T>`?',
    tags: ['pointers', 'rc', 'weak'],
    difficulty: 2,
    language: 'rust',
    code: `use std::rc::Rc;
let strong = Rc::new("hi".to_string());
// create Weak here`,
    options: [
      { label: 'A', text: '`Rc::weaken(&strong)`' },
      { label: 'B', text: '`Rc::borrow(&strong)`' },
      { label: 'C', text: '`Rc::downgrade(&strong)`' },
      { label: 'D', text: '`Weak::from_rc(&strong)`' },
    ],
    correctIndex: 2,
    hint: 'Look for the std associated function on `Rc`.',
    explanation:
      '`Rc::downgrade(&rc)` produces a `Weak<T>` pointing at the same allocation without increasing the strong count.',
  },
  {
    id: 'ptr-refcell-1',
    categorySlug: 'pointers',
    title: 'RefCell Borrow Rules',
    prompt: 'What happens if you call `borrow_mut` while a `borrow` is active?',
    tags: ['pointers', 'refcell'],
    difficulty: 3,
    language: 'rust',
    code: `use std::cell::RefCell;
let x = RefCell::new(5);
let r = x.borrow();
let m = x.borrow_mut();`,
    options: [
      { label: 'A', text: 'It compiles and both guards alias safely' },
      { label: 'B', text: 'It panics at runtime when borrow rules break' },
      { label: 'C', text: 'It fails at compile time like ordinary `&`/`&mut`' },
      { label: 'D', text: 'It blocks the thread until `r` is dropped' },
    ],
    correctIndex: 1,
    hint: '`RefCell` moves borrow checking to runtime.',
    explanation:
      '`RefCell` enforces the same exclusive/shared rules dynamically. A second conflicting borrow panics (or `try_borrow_mut` returns `Err`). It is not thread-safe and does not block.',
  },
  {
    id: 'ptr-refcell-2',
    categorySlug: 'pointers',
    title: 'RefCell Guarantees',
    prompt: 'Which statement best describes `RefCell<T>`?',
    tags: ['pointers', 'refcell', 'interior-mutability'],
    difficulty: 3,
    language: 'rust',
    code: `use std::cell::RefCell;
let x = RefCell::new(vec![1, 2, 3]);
let r = x.borrow();`,
    options: [
      { label: 'A', text: 'Compile-time borrow checks like ordinary references' },
      { label: 'B', text: 'Thread-safe lock-free shared mutation of `T`' },
      { label: 'C', text: 'Unchecked aliasing with mutation always allowed' },
      { label: 'D', text: 'Runtime enforcement of single-writer borrow rules' },
    ],
    correctIndex: 3,
    hint: 'Interior mutability does not remove the borrow rules.',
    explanation:
      '`RefCell<T>` provides interior mutability with runtime borrow checking: many `borrow`s or one `borrow_mut`, never both. Violations panic. It is not `Sync`.',
  },
  {
    id: 'ptr-rc-refcell-1',
    categorySlug: 'pointers',
    title: 'Rc plus RefCell',
    prompt: 'Why combine `Rc<RefCell<T>>`?',
    tags: ['pointers', 'rc', 'refcell'],
    difficulty: 3,
    language: 'rust',
    code: `use std::cell::RefCell;
use std::rc::Rc;
let shared = Rc::new(RefCell::new(0));
*shared.borrow_mut() += 1;`,
    options: [
      { label: 'A', text: '`Rc` shares ownership; `RefCell` allows mutation' },
      { label: 'B', text: '`RefCell` shares threads; `Rc` counts atomically' },
      { label: 'C', text: '`Rc` provides locks; `RefCell` stores the mutex' },
      { label: 'D', text: 'Together they make `T` automatically `Send + Sync`' },
    ],
    correctIndex: 0,
    hint: 'Separate “who owns it” from “can I mutate through `&`.”',
    explanation:
      '`Rc` enables multiple owners; by itself the inner `T` is immutable through those owners. Wrapping with `RefCell` adds single-threaded interior mutability so owners can `borrow_mut`. The combo is still not thread-safe.',
  },
  {
    id: 'ptr-cell-1',
    categorySlug: 'pointers',
    title: 'Cell Basics',
    prompt: 'What capability does `Cell<T>` provide?',
    tags: ['pointers', 'cell', 'interior-mutability'],
    difficulty: 2,
    language: 'rust',
    code: `use std::cell::Cell;
let c = Cell::new(1);
c.set(2);
let v = c.get();`,
    options: [
      { label: 'A', text: 'Thread-safe mutation without synchronization' },
      { label: 'B', text: 'Multiple simultaneous `&mut` into the cell' },
      { label: 'C', text: 'Immutability guarantees after first `set`' },
      { label: 'D', text: 'Interior mutability via shared `&Cell<T>`' },
    ],
    correctIndex: 3,
    hint: 'You mutate through `&self` methods like `set`/`get`.',
    explanation:
      '`Cell<T>` offers simple interior mutability: `set`/`get`/`replace` work through a shared reference. It is not thread-safe and does not hand out long-lived `&mut T` aliases.',
  },
  {
    id: 'ptr-cell-2',
    categorySlug: 'pointers',
    title: 'Cell::get Constraint',
    prompt: 'Why is `Cell::get` only for `T: Copy`?',
    tags: ['pointers', 'cell'],
    difficulty: 3,
    language: 'rust',
    code: `use std::cell::Cell;
let c = Cell::new(5);
let x = c.get();`,
    options: [
      { label: 'A', text: '`Cell` exists only for integer primitive types' },
      { label: 'B', text: '`get` moves `T` out and leaves the cell empty' },
      { label: 'C', text: '`get` returns `T` by value, so `T` must be `Copy`' },
      { label: 'D', text: '`Copy` is required for `Cell` to be `Send + Sync`' },
    ],
    correctIndex: 2,
    hint: 'Returning `T` through `&self` cannot move a non-`Copy` value.',
    explanation:
      '`get` copies the contained value out. Restricting it to `T: Copy` avoids moving a non-`Copy` value through a shared reference. Non-`Copy` types use `replace`/`take` (when `T: Default`) or `RefCell` instead. `Cell` is not `Sync`.',
  },
  {
    id: 'ptr-mutex-1',
    categorySlug: 'pointers',
    title: 'Mutex Lock Guard',
    prompt: 'Why does `Mutex::lock` return a guard instead of `&mut T`?',
    tags: ['pointers', 'mutex', 'sync'],
    difficulty: 3,
    language: 'rust',
    code: `use std::sync::Mutex;
let m = Mutex::new(0);
let mut guard = m.lock().unwrap();
*guard += 1;`,
    options: [
      { label: 'A', text: 'Safe Rust cannot return references from methods' },
      { label: 'B', text: '`&mut T` is never allowed behind shared owners' },
      { label: 'C', text: 'Locking moves `T` out of the mutex permanently' },
      { label: 'D', text: 'The guard ties unlock to drop (RAII lifetime)' },
    ],
    correctIndex: 3,
    hint: 'When the guard drops, the lock is released.',
    explanation:
      'The mutex guard represents “lock held.” When it drops, it unlocks. Returning a bare `&mut T` would not automatically release the lock or tie the borrow to the critical section.',
  },
  {
    id: 'ptr-arc-mutex-1',
    categorySlug: 'pointers',
    title: 'Arc Mutex Pattern',
    prompt: 'Why is `Arc<Mutex<T>>` a common combo?',
    tags: ['pointers', 'arc', 'mutex'],
    difficulty: 2,
    language: 'rust',
    code: `use std::sync::{Arc, Mutex};
let data = Arc::new(Mutex::new(0));
let d2 = Arc::clone(&data);`,
    options: [
      { label: 'A', text: '`Mutex` shares ownership; `Arc` provides the lock key' },
      { label: 'B', text: '`Arc` shares across threads; `Mutex` guards mutation' },
      { label: 'C', text: 'Together they make interior borrows compile-time only' },
      { label: 'D', text: '`Arc` replaces `Mutex` whenever `T` is already `Sync`' },
    ],
    correctIndex: 1,
    hint: 'Split shared ownership from synchronized mutation.',
    explanation:
      '`Arc` gives thread-safe shared ownership of the mutex. `Mutex` provides synchronized exclusive access to `T`. You need both when multiple threads must mutate shared state.',
  },
  {
    id: 'ptr-cow-1',
    categorySlug: 'pointers',
    title: 'Cow::to_mut',
    prompt: 'What does `to_mut` do on a borrowed `Cow<str>`?',
    tags: ['pointers', 'cow', 'borrow'],
    difficulty: 2,
    language: 'rust',
    code: `use std::borrow::Cow;
let mut c: Cow<str> = Cow::Borrowed("hi");
c.to_mut().push_str("!");`,
    options: [
      { label: 'A', text: 'Mutates the original `&str` in place without copying' },
      { label: 'B', text: 'Panics because borrowed `Cow` values are immutable' },
      { label: 'C', text: 'Clones to owned storage, then yields `&mut String`' },
      { label: 'D', text: 'Converts `c` into `Rc<str>` for shared mutation' },
    ],
    correctIndex: 2,
    hint: '`Cow` means clone-on-write when mutation is needed.',
    explanation:
      'If the `Cow` is `Borrowed`, `to_mut` clones the data into the `Owned` variant, then returns a mutable reference to that owned value. If it is already owned, no clone is needed. Borrowed `&str` data itself is never mutated in place.',
  },
  {
    id: 'ptr-box-deref-1',
    categorySlug: 'pointers',
    title: 'Box Deref Coercion',
    prompt: 'Why does `print_len` accept a `Box<String>` here?',
    tags: ['pointers', 'box', 'deref'],
    difficulty: 2,
    language: 'rust',
    code: `fn print_len(s: &String) {
    println!("{}", s.len());
}

fn main() {
    let b = Box::new(String::from("hi"));
    print_len(&b);
}`,
    options: [
      { label: 'A', text: '`Box<T>` is the same type as `T` at the ABI level' },
      { label: 'B', text: '`&Box<String>` coerces via `Deref` to `&String`' },
      { label: 'C', text: '`print_len` auto-clones the boxed string to borrow' },
      { label: 'D', text: 'All smart pointers implement `Copy` for references' },
    ],
    correctIndex: 1,
    hint: '`Deref` enables ergonomic method and borrow coercion.',
    explanation:
      '`Box<T>` implements `Deref<Target = T>`, so `&Box<String>` can coerce to `&String`. The types are not identical, and no clone of the `String` is required for this call.',
  },
  {
    id: 'ptr-rc-get-mut-1',
    categorySlug: 'pointers',
    title: 'Rc::get_mut',
    prompt: 'When does `Rc::get_mut` return `Some`?',
    tags: ['pointers', 'rc'],
    difficulty: 2,
    language: 'rust',
    code: `use std::rc::Rc;
let mut a = Rc::new(1);
let m = Rc::get_mut(&mut a);`,
    options: [
      { label: 'A', text: 'Only after upgrading a `Weak` into a strong `Rc`' },
      { label: 'B', text: 'Whenever `T` implements `Clone`, regardless of count' },
      { label: 'C', text: 'Never on stable; use `RefCell` for all `Rc` mutation' },
      { label: 'D', text: 'When no other `Rc` or `Weak` shares the allocation' },
    ],
    correctIndex: 3,
    hint: 'Unique ownership unlocks a mutable borrow of `T`.',
    explanation:
      '`Rc::get_mut` yields `&mut T` only when this handle is exclusive: strong count 1 and weak count 0. Another `Rc` or any `Weak` makes it return `None`. Shared mutation otherwise needs interior mutability.',
  },
  {
    id: 'ptr-arc-clone-1',
    categorySlug: 'pointers',
    title: 'Arc Clone Cost',
    prompt: 'What does `Arc::clone` primarily cost?',
    tags: ['pointers', 'arc'],
    difficulty: 2,
    language: 'rust',
    code: `use std::sync::Arc;
let a = Arc::new(vec![1, 2, 3]);
let b = Arc::clone(&a);`,
    options: [
      { label: 'A', text: 'An atomic reference-count increment, not a deep copy' },
      { label: 'B', text: 'A full deep clone of the `Vec` into a new allocation' },
      { label: 'C', text: 'A mutex lock around the entire process address space' },
      { label: 'D', text: 'Moving the vector out of `a`, leaving `a` empty' },
    ],
    correctIndex: 0,
    hint: 'Sharing is cheap; cloning `T` is a separate choice.',
    explanation:
      '`Arc::clone` bumps an atomic strong count so another owner shares the same allocation. It does not clone the inner `Vec`. Use `(*a).clone()` / `T: Clone` when you need a deep copy.',
  },
  {
    id: 'ptr-pin-intro-1',
    categorySlug: 'pointers',
    title: 'Pin Motivation',
    prompt: 'What problem does `Pin` help address?',
    tags: ['pointers', 'pin'],
    difficulty: 2,
    language: 'rust',
    code: `use std::pin::Pin;
// async state machines / self-referential structs`,
    options: [
      { label: 'A', text: 'Making every value `Copy` without deriving traits' },
      { label: 'B', text: 'Replacing `unsafe` with a faster safe allocator API' },
      { label: 'C', text: 'Preventing moves of values that must stay put' },
      { label: 'D', text: 'Turning shared borrows into owned heap allocations' },
    ],
    correctIndex: 2,
    hint: 'Self-referential types break if the struct moves.',
    explanation:
      '`Pin` constrains moving certain values in memory, which is critical for self-referential structs and many `Future` state machines. It is not about making things `Copy` or allocating by itself.',
  },
  {
    id: 'ptr-rc-cycle-1',
    categorySlug: 'pointers',
    title: 'Reference Cycles',
    prompt: 'How can `Rc` leak memory even in safe Rust?',
    tags: ['pointers', 'rc', 'weak'],
    difficulty: 2,
    language: 'rust',
    code: `use std::rc::Rc;
use std::cell::RefCell;

struct Node {
    next: Option<Rc<RefCell<Node>>>,
}`,
    options: [
      { label: 'A', text: 'Safe Rust forbids cycles; this type will not compile' },
      { label: 'B', text: 'Cycles keep strong counts above zero, blocking drop' },
      { label: 'C', text: '`Rc` always leaks unless you call `mem::forget`' },
      { label: 'D', text: '`RefCell` disables destructors for any nested `Rc`' },
    ],
    correctIndex: 1,
    hint: '`Weak` breaks ownership cycles.',
    explanation:
      'If two `Rc` values point at each other (a cycle), strong counts never reach zero and the allocations are never dropped, a memory leak. Use `Weak` for non-owning back-edges (parent/child graphs).',
  },
  {
    id: 'ptr-box-leak-1',
    categorySlug: 'pointers',
    title: 'Box::leak',
    prompt: 'What does `Box::leak` return?',
    tags: ['pointers', 'box', 'static'],
    difficulty: 2,
    language: 'rust',
    code: `let s: &'static mut str = Box::leak(Box::from("hi"));`,
    options: [
      { label: 'A', text: 'An `Rc` that frees memory when the last clone drops' },
      { label: 'B', text: 'A dangling pointer that must never be dereferenced' },
      { label: 'C', text: 'Ownership of `T` moved back onto the stack frame' },
      { label: 'D', text: "A `'static` reference, intentionally never freed" },
    ],
    correctIndex: 3,
    hint: 'Leaking is the point: the allocation outlives forever.',
    explanation:
      '`Box::leak` consumes the `Box` and returns a `&\'static mut T` (or shared) to the heap value, which will not be freed. Useful for certain global tables; misuse is just a deliberate leak.',
  },
  {
    id: 'ptr-into-raw-1',
    categorySlug: 'pointers',
    title: 'Box::into_raw',
    prompt: 'What does `Box::into_raw` require of the caller?',
    tags: ['pointers', 'box', 'ffi'],
    difficulty: 2,
    language: 'rust',
    code: `fn main() {
    let p = Box::into_raw(Box::new(1i32));
    unsafe {
        drop(Box::from_raw(p));
    }
}`,
    options: [
      { label: 'A', text: 'Nothing; dropping the old `Box` still frees it' },
      { label: 'B', text: 'Pass the pointer to `mem::forget` before use' },
      { label: 'C', text: 'Reclaim with `from_raw` (or otherwise free it)' },
      { label: 'D', text: 'Convert it to `&\'static T` before any deref' },
    ],
    correctIndex: 2,
    hint: 'The `Box` is consumed; its destructor will not run.',
    explanation:
      "`Box::into_raw` consumes the `Box` and returns `*mut T` without running the destructor. The caller owns the allocation and should later `Box::from_raw` (typical FFI round-trip) or free it with the matching layout. `Box::leak` instead yields a `'static` reference meant to last for the rest of the program.",
  },
  {
    id: 'ptr-once-cell-1',
    categorySlug: 'pointers',
    title: 'OnceCell Write-Once',
    prompt: 'What happens on the second `set`?',
    tags: ['pointers', 'oncecell'],
    difficulty: 2,
    language: 'rust',
    code: `use std::cell::OnceCell;
fn main() {
    let cell = OnceCell::new();
    assert_eq!(cell.set(92), Ok(()));
    assert_eq!(cell.set(62), Err(62));
    assert_eq!(cell.get(), Some(&92));
}`,
    options: [
      { label: 'A', text: 'It returns `Err` with the unused new value' },
      { label: 'B', text: 'It overwrites 92 with 62 and returns `Ok`' },
      { label: 'C', text: 'It panics because cells cannot be written twice' },
      { label: 'D', text: 'It waits until `get` runs on another thread' },
    ],
    correctIndex: 0,
    hint: 'The cell is write-once; the first value stays.',
    explanation:
      '`OnceCell::set` initializes an empty cell (`Ok(())`) and returns `Err(value)` if it was already set, leaving the original contents. `get` then yields `&T` without `RefCell`-style runtime borrow flags. `OnceCell` is `!Sync`; the thread-safe counterpart is `OnceLock`.',
  },
  {
    id: 'ptr-cow-to-mut-allocation-1',
    categorySlug: 'pointers',
    title: 'Cow::to_mut Allocation',
    prompt: 'What is printed by this program?',
    tags: ['pointers', 'cow', 'clone-on-write'],
    difficulty: 2,
    language: 'rust',
    code: `use std::borrow::Cow;

fn main() {
    let mut item: Cow<str> = Cow::Borrowed("apple");
    item.to_mut().push_str(" pie");
    println!("owned: {}, val: {}", matches!(item, Cow::Owned(_)), item);
}`,
    options: [
      { label: 'A', text: 'owned: false, val: apple because Cow is always immutable' },
      { label: 'B', text: 'owned: false, val: apple pie via in-place slice mutation' },
      { label: 'C', text: 'owned: true, val: apple pie because to_mut clones data' },
      { label: 'D', text: 'Fails to compile because Cow::Borrowed has no to_mut()' },
    ],
    correctIndex: 2,
    hint: 'What does Cow do when you request mutable access to borrowed data?',
    explanation:
      '`Cow::to_mut` returns a mutable reference to the owned value. If the `Cow` is currently `Cow::Borrowed`, `to_mut()` invokes `clone()` on the borrowed data, mutates the `Cow` in-place to `Cow::Owned`, and returns a mutable reference to the newly created owned buffer (here, a `String`). Subsequent mutations modify this owned buffer.',
  },
  {
    id: 'ptr-pin-primitive-unpin-1',
    categorySlug: 'pointers',
    title: 'Pin with Unpin Primitives',
    prompt: 'Why does mutating `*pinned` compile without unsafe code?',
    tags: ['pointers', 'pin', 'unpin'],
    difficulty: 2,
    language: 'rust',
    code: `use std::pin::Pin;

fn main() {
    let mut num = 42;
    let mut pinned = Pin::new(&mut num);
    *pinned = 100;
    println!("{num}");
}`,
    options: [
      { label: 'A', text: 'i32 implements Unpin, so Pin::new and DerefMut are safe' },
      { label: 'B', text: 'Pin only enforces memory immobility for heap types like Box' },
      { label: 'C', text: 'Primitive integers bypass all pin guarantees in compiler' },
      { label: 'D', text: 'DerefMut on Pin is always a safe operation for all types' },
    ],
    correctIndex: 0,
    hint: 'Do primitive integer types implement the Unpin auto trait?',
    explanation:
      '`Pin` prevents values from moving in memory unless their type implements `Unpin`. Primitive types like `i32`, `f64`, `&T`, and most standard types implement the auto trait `Unpin` because they have no self-referential pointers or pinning invariants. When `T: Unpin`, `Pin::new` is safe and `Pin<&mut T>` implements `DerefMut`, allowing safe mutable access to the underlying value.',
  },
  
  {
    id: "ptr-supporter-1",
    categorySlug: "pointers",
    title: "Pin and !Unpin Types",
    prompt: "What guarantee does `Pin<&mut T>` provide when `T` is `!Unpin`?",
    tags: ["pointers","pin","unpin"],
    difficulty: 3,
    language: 'rust',
    code: "use std::marker::PhantomPinned;\nuse std::pin::Pin;\n\nstruct SelfRef {\n    _pin: PhantomPinned,\n}\n\nfn check(p: Pin<&mut SelfRef>) {\n    let _ = p;\n}",
    options: [
      { label: 'A', text: "`T` will never be moved in memory until its destructor completes" },
      { label: 'B', text: "`T` is allocated exclusively in non-pageable physical RAM" },
      { label: 'C', text: "`T` can only be accessed from a single CPU execution thread" },
      { label: 'D', text: "`T` will never be deallocated from the heap memory buffer" },
    ],
    correctIndex: 0,
    hint: "Pin guarantees that !Unpin data will not be moved before being dropped.",
    explanation: "When `T: !Unpin`, `Pin<&mut T>` prevents moving `T` out of its memory location (e.g. via `std::mem::swap` or `replace`) until `T` is dropped, making self-referential structures completely sound.",
  },
  {
    id: "ptr-supporter-2",
    categorySlug: "pointers",
    title: "Strict Provenance APIs (addr vs cast)",
    prompt: "Why is `ptr.addr()` preferred over `ptr as usize` under strict provenance?",
    tags: ["pointers","strict-provenance","unsafe"],
    difficulty: 3,
    language: 'rust',
    code: "fn main() {\n    let x = 42;\n    let ptr: *const i32 = &x;\n    let addr: usize = ptr.addr();\n    println!(\"0x{addr:x}\");\n}",
    options: [
      { label: 'A', text: "It converts the pointer into a 128-bit hardware identifier within local thread memory" },
      { label: 'B', text: "It explicitly reads the memory address without exposing pointer provenance metadata" },
      { label: 'C', text: "It executes in constant CPU time without accessing registers within local thread memory" },
      { label: 'D', text: "It ensures the pointer is allocated on a 64-byte boundary under current compiler safety rules" },
    ],
    correctIndex: 1,
    hint: "Strict provenance separates integer addresses from pointer provenance capabilities.",
    explanation: "Under the Strict Provenance model, `ptr.addr()` inspects the address portion of a pointer without \"exposing\" its provenance to the compiler's alias analysis, preserving optimization opportunities and memory sanitizer checks.",
  },
  {
    id: "ptr-supporter-3",
    categorySlug: "pointers",
    title: "Cow::to_mut Allocation Behavior",
    prompt: "When does `Cow::to_mut` perform a heap allocation?",
    tags: ["pointers","cow","clone-on-write"],
    difficulty: 2,
    language: 'rust',
    code: "use std::borrow::Cow;\n\nfn main() {\n    let mut c: Cow<str> = Cow::Borrowed(\"hello\");\n    let s = c.to_mut();\n    s.push_str(\" world\");\n    println!(\"{c}\");\n}",
    options: [
      { label: 'A', text: "On every call to `to_mut`, creating fresh heap buffers each time in runtime memory" },
      { label: 'B', text: "Never; `Cow::to_mut` modifies borrowed slices directly in place in runtime memory" },
      { label: 'C', text: "Only when `Cow` is currently holding a borrowed `Cow::Borrowed` variant" },
      { label: 'D', text: "Only when the length of the string exceeds 1024 bytes within local thread memory" },
    ],
    correctIndex: 2,
    hint: "If already owned, to_mut returns a mutable reference directly without reallocating.",
    explanation: "`Cow::to_mut` checks the variant: if `Borrowed`, it clones the data into `Owned` (allocating on the heap) and mutates in place; if already `Owned`, it returns `&mut Owned` with zero new allocations.",
  },
  {
    id: "ptr-supporter-4",
    categorySlug: "pointers",
    title: "NonNull vs Raw Pointer",
    prompt: "What guarantee does `std::ptr::NonNull<T>` provide over a raw `*mut T`?",
    tags: ["pointers","non-null","niche-optimization"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ptr::NonNull;\n\nfn main() {\n    let mut x = 10;\n    let ptr = NonNull::new(&mut x).unwrap();\n    assert_eq!(std::mem::size_of::<Option<NonNull<i32>>>(), std::mem::size_of::<*mut i32>());\n    println!(\"{}\", unsafe { *ptr.as_ptr() });\n}",
    options: [
      { label: 'A', text: "Guaranteed valid memory alignment and lifetime safety by the compiler within local thread memory" },
      { label: 'B', text: "Thread-safe atomic read and write access across all cores under current compiler safety rules" },
      { label: 'C', text: "Automatic memory deallocation when the NonNull wrapper drops within local thread memory" },
      { label: 'D', text: "Guaranteed non-null, enabling null-pointer niche optimization in `Option<NonNull<T>>`" },
    ],
    correctIndex: 3,
    hint: "NonNull is guaranteed to never be null, so Option<NonNull<T>> has the same size as *mut T.",
    explanation: "`NonNull<T>` is guaranteed to be non-null and covariant over `T`. This invariant enables the compiler to use `0` as the `None` representation for `Option<NonNull<T>>` (niche optimization).",
  },
  {
    id: "ptr-supporter-5",
    categorySlug: "pointers",
    title: "std::ptr::read vs Dereferencing",
    prompt: "When is `std::ptr::read(ptr)` used instead of `*ptr`?",
    tags: ["pointers","read","ownership"],
    difficulty: 3,
    language: 'rust',
    code: "fn main() {\n    let x = String::from(\"data\");\n    let ptr = &x as *const String;\n    let copy = unsafe { std::ptr::read(ptr) };\n    std::mem::forget(x); // prevent double drop\n    println!(\"{copy}\");\n}",
    options: [
      { label: 'A', text: "To read and move an owned value out of raw memory without dropping the source" },
      { label: 'B', text: "To clone a value across thread boundaries with atomic barriers in runtime memory" },
      { label: 'C', text: "To convert an unaligned pointer into an aligned reference within local thread memory" },
      { label: 'D', text: "To bypass the operating system virtual memory page tables within local thread memory" },
    ],
    correctIndex: 0,
    hint: "ptr::read moves the value out by copying memory bits without running Drop on the source.",
    explanation: "`std::ptr::read(src)` creates an owned `T` by bitwise copying from the raw pointer `src` without running any destructor on `src`. The caller must ensure `src` is not double-dropped.",
  },
  {
    id: "ptr-supporter-6",
    categorySlug: "pointers",
    title: "std::ptr::write Semantics",
    prompt: "Why does `std::ptr::write(dest, val)` not run the destructor of whatever was in `dest`?",
    tags: ["pointers","write","uninitialized"],
    difficulty: 3,
    language: 'rust',
    code: "use std::mem::MaybeUninit;\n\nfn main() {\n    let mut uninit = MaybeUninit::<String>::uninit();\n    unsafe {\n        std::ptr::write(uninit.as_mut_ptr(), String::from(\"init\"));\n        let s = uninit.assume_init();\n        println!(\"{s}\");\n    }\n}",
    options: [
      { label: 'A', text: "It converts the destination value into a static binary constant within local thread memory" },
      { label: 'B', text: "It is designed to initialize uninitialized memory without dropping garbage bits" },
      { label: 'C', text: "Destructors in Rust are only executed when variables leave main within local thread memory" },
      { label: 'D', text: "It delegates deallocation to the kernel background worker within local thread memory" },
    ],
    correctIndex: 1,
    hint: "ptr::write writes to uninitialized memory without evaluating destructors.",
    explanation: "`std::ptr::write(dest, src)` overwrites memory at `dest` with `src` without reading or dropping the previous content. This is essential for initializing `MaybeUninit` memory where dropping garbage would cause UB.",
  },
  {
    id: "ptr-supporter-7",
    categorySlug: "pointers",
    title: "MaybeUninit::assume_init Invariants",
    prompt: "What safety invariant must be satisfied before calling `assume_init()` on `MaybeUninit<T>`?",
    tags: ["pointers","maybe-uninit","safety"],
    difficulty: 3,
    language: 'rust',
    code: "use std::mem::MaybeUninit;\n\nfn main() {\n    let mut val = MaybeUninit::<i32>::uninit();\n    val.write(100);\n    let init_val = unsafe { val.assume_init() };\n    println!(\"{init_val}\");\n}",
    options: [
      { label: 'A', text: "The value must be allocated on the heap through `Box::new` within local thread memory" },
      { label: 'B', text: "The type `T` must implement the `Copy` and `Send` marker traits within local thread memory" },
      { label: 'C', text: "The memory for `T` must be fully and validly initialized with a valid bit pattern" },
      { label: 'D', text: "All other references to the enclosing struct must be dropped within local thread memory" },
    ],
    correctIndex: 2,
    hint: "Calling assume_init on uninitialized memory is immediate undefined behavior.",
    explanation: "`MaybeUninit::assume_init()` asserts to the compiler that the contained `T` is fully initialized with a valid bit representation. Calling it on uninitialized memory is instant Undefined Behavior (UB).",
  },
  {
    id: "ptr-supporter-8",
    categorySlug: "pointers",
    title: "Pin Projection Rules",
    prompt: "What is \"pin projection\" in Rust async / Pin contexts?",
    tags: ["pointers","pin","projection"],
    difficulty: 3,
    language: 'rust',
    code: "// Pin<&mut Struct> -> Pin<&mut Field> for structural fields",
    options: [
      { label: 'A', text: "Allocating pinned memory buffers across distinct CPU cores within local thread memory" },
      { label: 'B', text: "Converting unpinned traits into dynamic vtable trait objects within local thread memory" },
      { label: 'C', text: "Demoting a pinned future into a synchronous blocking loop within local thread memory" },
      { label: 'D', text: "Safely obtaining a `Pin<&mut Field>` from a `Pin<&mut Struct>` for pinned fields" },
    ],
    correctIndex: 3,
    hint: "Pin projection projects a pinned reference to a struct into pinned references to its fields.",
    explanation: "Pin projection is the technique of converting `Pin<&mut Struct>` into `Pin<&mut Field>` (for structurally pinned fields) or `&mut OtherField` (for unpinned fields) while upholding pinning invariants.",
  },
  {
    id: "ptr-supporter-9",
    categorySlug: "pointers",
    title: "Aliasing Rules for Raw Pointers vs References",
    prompt: "Can multiple `*mut T` raw pointers alias the same memory location simultaneously in safe blocks?",
    tags: ["pointers","raw-pointers","aliasing"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut x = 42;\n    let p1: *mut i32 = &mut x;\n    let p2: *mut i32 = &mut x;\n    println!(\"{p1:p} {p2:p}\");\n}",
    options: [
      { label: 'A', text: "Yes; creating and holding multiple raw pointers is safe, but dereferencing requires unsafe" },
      { label: 'B', text: "No; creating multiple `*mut T` pointers is an immediate compiler error within local thread memory" },
      { label: 'C', text: "Only if `x` implements the `Copy` and `Clone` traits during standard program runtime execution" },
      { label: 'D', text: "Only when compiled under debug mode with sanitizers disabled under current compiler safety rules" },
    ],
    correctIndex: 0,
    hint: "Creating raw pointers is safe; dereferencing them is unsafe.",
    explanation: "Creating multiple raw pointers (`*const T` or `*mut T`) that point to the same memory is permitted in safe Rust code. Undefined Behavior only arises if dereferencing violates Rust's aliasing or validity rules in `unsafe`.",
  },
  {
    id: "ptr-supporter-10",
    categorySlug: "pointers",
    title: "Box::into_raw and Box::from_raw",
    prompt: "Why must `Box::from_raw(ptr)` be called on a pointer originally produced by `Box::into_raw`?",
    tags: ["pointers","box","raw-pointers"],
    difficulty: 3,
    language: 'rust',
    code: "fn main() {\n    let b = Box::new(42);\n    let ptr = Box::into_raw(b);\n    let b2 = unsafe { Box::from_raw(ptr) };\n    println!(\"{b2}\");\n}",
    options: [
      { label: 'A', text: "Because `Box::from_raw` converts raw pointers into thread-safe atomics in runtime memory" },
      { label: 'B', text: "To ensure the allocator receives the exact same layout and allocation metadata" },
      { label: 'C', text: "To prevent the operating system from clearing physical RAM pages in runtime memory" },
      { label: 'D', text: "Because raw pointers lose their data types when cast to usize within local thread memory" },
    ],
    correctIndex: 1,
    hint: "Box::from_raw expects a pointer allocated by GlobalAlloc with the same type layout.",
    explanation: "`Box::from_raw` assumes the pointer was allocated by the global allocator with the exact layout of `T`. Calling it on stack pointers or mismatched types causes memory corruption when `Box` drops.",
  },
  {
    id: "ptr-supporter-11",
    categorySlug: "pointers",
    title: "RefCell try_borrow_mut Error Handling",
    prompt: "What does `cell.try_borrow_mut()` return if the `RefCell` is already borrowed?",
    tags: ["pointers","refcell","borrow-error"],
    difficulty: 2,
    language: 'rust',
    code: "use std::cell::RefCell;\n\nfn main() {\n    let cell = RefCell::new(5);\n    let _r = cell.borrow();\n    let res = cell.try_borrow_mut();\n    assert!(res.is_err());\n}",
    options: [
      { label: 'A', text: "Panics immediately with an assertion error" },
      { label: 'B', text: "Blocks the calling thread until the borrow ends" },
      { label: 'C', text: "`Err(BorrowMutError)` without panicking" },
      { label: 'D', text: "`Ok(None)` indicating an empty reference guard" },
    ],
    correctIndex: 2,
    hint: "try_borrow_mut returns a Result instead of panicking on borrow conflicts.",
    explanation: "`RefCell::try_borrow_mut` is the non-panicking counterpart of `borrow_mut()`. If an existing borrow is active, it returns `Err(BorrowMutError)` allowing the caller to handle the conflict gracefully.",
  },
  {
    id: "ptr-supporter-12",
    categorySlug: "pointers",
    title: "std::ptr::copy vs copy_nonoverlapping",
    prompt: "What is the difference between `std::ptr::copy` and `std::ptr::copy_nonoverlapping`?",
    tags: ["pointers","copy","memmove"],
    difficulty: 3,
    language: 'rust',
    code: "use std::ptr;\n\nfn main() {\n    let mut arr = [1, 2, 3, 4, 5];\n    unsafe {\n        ptr::copy(arr.as_ptr(), arr.as_mut_ptr().add(1), 4);\n    }\n    println!(\"{arr:?}\");\n}",
    options: [
      { label: 'A', text: "`copy_nonoverlapping` uses multi-threaded CPU SIMD instructions in runtime memory" },
      { label: 'B', text: "`copy` drops the original values at the destination location in runtime memory" },
      { label: 'C', text: "`copy_nonoverlapping` is safe to call outside of unsafe blocks in runtime memory" },
      { label: 'D', text: "`copy` allows overlapping source and destination buffers (like C memmove)" },
    ],
    correctIndex: 3,
    hint: "copy handles overlapping memory (memmove); copy_nonoverlapping requires disjoint memory (memcpy).",
    explanation: "`std::ptr::copy` is equivalent to C `memmove` (handles overlapping memory regions correctly). `copy_nonoverlapping` is equivalent to `memcpy` and requires source and destination to be non-overlapping for performance.",
  },
  {
    id: "ptr-supporter-13",
    categorySlug: "pointers",
    title: "std::ptr::null vs std::ptr::null_mut",
    prompt: "What are the return types of `std::ptr::null()` and `std::ptr::null_mut()`?",
    tags: ["pointers","null","types"],
    difficulty: 1,
    language: 'rust',
    code: "use std::ptr;\n\nfn main() {\n    let p1: *const i32 = ptr::null();\n    let p2: *mut i32 = ptr::null_mut();\n    assert!(p1.is_null());\n    assert!(p2.is_null());\n}",
    options: [
      { label: 'A', text: "`*const T` for `null()` and `*mut T` for `null_mut()`" },
      { label: 'B', text: "`Option<*const T>` for both functions during runtime execution" },
      { label: 'C', text: "`usize` representing numerical zero during runtime execution" },
      { label: 'D', text: "`NonNull<T>` with null pointer checks during runtime execution" },
    ],
    correctIndex: 0,
    hint: "null() creates an immutable null pointer; null_mut() creates a mutable null pointer.",
    explanation: "`std::ptr::null<T>()` returns a `*const T` null pointer, while `std::ptr::null_mut<T>()` returns a `*mut T` null pointer.",
  },
  {
    id: "ptr-supporter-14",
    categorySlug: "pointers",
    title: "Pointer Offsetting Safety (add vs offset)",
    prompt: "Why is `ptr.add(count)` unsafe?",
    tags: ["pointers","add","bounds"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let arr = [10, 20, 30];\n    let p = arr.as_ptr();\n    let second = unsafe { *p.add(1) };\n    println!(\"{second}\");\n}",
    options: [
      { label: 'A', text: "Pointer arithmetic modifies the underlying array contents directly in runtime memory" },
      { label: 'B', text: "The caller must ensure the offset remains within the allocated object bounds" },
      { label: 'C', text: "The operation disables hardware memory protection on the CPU within local thread memory" },
      { label: 'D', text: "Offsets can only be calculated on heap-allocated vector pointers in runtime memory" },
    ],
    correctIndex: 1,
    hint: "Offsetting out of bounds or wrapping around the address space is undefined behavior.",
    explanation: "`ptr.add(count)` is unsafe because the caller must guarantee that both the starting pointer and the resulting pointer remain within the bounds of the same allocated object (or one byte past the end).",
  },
  {
    id: "ptr-supporter-15",
    categorySlug: "pointers",
    title: "Fat Pointers vs Thin Pointers",
    prompt: "Which of the following types is represented as a fat pointer (two machine words)?",
    tags: ["pointers","fat-pointers","slices"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    println!(\"{}\", std::mem::size_of::<&[u8]>());\n    println!(\"{}\", std::mem::size_of::<&i32>());\n}",
    options: [
      { label: 'A', text: "`&i32` (contains data pointer only) during runtime execution" },
      { label: 'B', text: "`*const f64` (contains memory address only) in code" },
      { label: 'C', text: "`&[u8]` (contains data pointer and length usize)" },
      { label: 'D', text: "`Box<u64>` (contains heap pointer only) during runtime execution" },
    ],
    correctIndex: 2,
    hint: "Slice references and trait object references are fat pointers.",
    explanation: "`&[u8]` is a slice fat pointer consisting of two words: a pointer to the first element and a `usize` length. In contrast, `&i32`, `*const f64`, and `Box<u64>` are thin single-word pointers.",
  },
  {
    id: "ptr-supporter-16",
    categorySlug: "pointers",
    title: "Pin::new_unchecked Invariants",
    prompt: "When calling `unsafe { Pin::new_unchecked(ptr) }`, what must the caller uphold?",
    tags: ["pointers","pin","contracts"],
    difficulty: 3,
    language: 'rust',
    code: "use std::pin::Pin;\n\nstruct Unmovable;\n\nfn pin_it(u: &mut Unmovable) -> Pin<&mut Unmovable> {\n    unsafe { Pin::new_unchecked(u) }\n}",
    options: [
      { label: 'A', text: "Ensure the struct is allocated directly on the thread-local stack in runtime memory" },
      { label: 'B', text: "Verify that the pointee contains no atomic variable fields within local thread memory" },
      { label: 'C', text: "Register the memory address with the global operating system allocator in code" },
      { label: 'D', text: "Guarantee that the pointee will never be moved before its destructor is run" },
    ],
    correctIndex: 3,
    hint: "The core pinning contract is that the pinned object will never be moved until dropped.",
    explanation: "`Pin::new_unchecked` is unsafe because the caller must uphold the Pin contract: once pinned, the memory location occupied by the pointee must not be overwritten, moved, or deallocated without dropping.",
  },
  {
    id: "ptr-supporter-17",
    categorySlug: "pointers",
    title: "Pin::as_mut and Reborrowing",
    prompt: "What does `pin.as_mut()` return for a `Pin<&mut T>`?",
    tags: ["pointers","pin","as-mut"],
    difficulty: 2,
    language: 'rust',
    code: "use std::pin::Pin;\n\nfn modify(mut pin: Pin<&mut i32>) {\n    let reborrow: Pin<&mut i32> = pin.as_mut();\n    println!(\"{}\", *reborrow);\n}",
    options: [
      { label: 'A', text: "The exact same pointer address offset by the element stride" },
      { label: 'B', text: "A newly allocated heap pointer copy with unique metadata" },
      { label: 'C', text: "A null pointer if the offset extends past buffer capacity" },
      { label: 'D', text: "A boxed pointer instance managed by the system allocator" },
    ],
    correctIndex: 0,
    hint: "as_mut reborrows a Pin<&mut T> for a shorter lifetime without unpinning.",
    explanation: "`Pin::as_mut` reborrows the pinned reference as `Pin<&mut T>` for a shorter lifetime, allowing multiple method calls on the pinned object without consuming the original `Pin` handle.",
  },
  {
    id: "ptr-supporter-18",
    categorySlug: "pointers",
    title: "PhantomPinned Role",
    prompt: "What is `std::marker::PhantomPinned` used for in structs?",
    tags: ["pointers","phantom-pinned","unpin"],
    difficulty: 2,
    language: 'rust',
    code: "use std::marker::PhantomPinned;\n\nstruct Struct {\n    _pin: PhantomPinned,\n}",
    options: [
      { label: 'A', text: "Pinning the struct to a specific physical CPU processor core" },
      { label: 'B', text: "Opting the struct out of the auto trait `Unpin` (`!Unpin`)" },
      { label: 'C', text: "Preventing the struct from being dropped at end of scope in code" },
      { label: 'D', text: "Enabling automatic multi-threaded thread synchronization in code" },
    ],
    correctIndex: 1,
    hint: "PhantomPinned implements !Unpin, making any containing struct !Unpin.",
    explanation: "`PhantomPinned` is a marker type that does not implement `Unpin`. Adding a `_pin: PhantomPinned` field makes the enclosing struct `!Unpin`, enabling compile-time pinning guarantees.",
  },
  {
    id: "ptr-supporter-19",
    categorySlug: "pointers",
    title: "Raw Pointer Null Check",
    prompt: "Which method safely checks if a raw pointer `*const T` is null?",
    tags: ["pointers","raw-pointers","null"],
    difficulty: 1,
    language: 'rust',
    code: "fn check(ptr: *const i32) {\n    if ptr.is_null() {\n        println!(\"null pointer\");\n    }\n}",
    options: [
      { label: 'A', text: "`ptr == 0` in code" },
      { label: 'B', text: "`ptr.is_none()`" },
      { label: 'C', text: "`ptr.is_null()`" },
      { label: 'D', text: "`ptr.is_empty()`" },
    ],
    correctIndex: 2,
    hint: "Raw pointers provide the is_null() method in safe Rust.",
    explanation: "`ptr.is_null()` is a safe method on raw pointers (`*const T` and `*mut T`) that returns `true` if the pointer points to address zero.",
  },
  {
    id: "ptr-supporter-20",
    categorySlug: "pointers",
    title: "std::ptr::eq Pointer Comparison",
    prompt: "What does `std::ptr::eq(r1, r2)` test when passed two references `&T`?",
    tags: ["pointers","eq","references"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ptr;\n\nfn main() {\n    let s1 = String::from(\"hi\");\n    let s2 = String::from(\"hi\");\n    assert!(!ptr::eq(&s1, &s2));\n}",
    options: [
      { label: 'A', text: "Whether the contents of both structs are structurally equal in code" },
      { label: 'B', text: "Whether both references share the same generic lifetime in runtime memory" },
      { label: 'C', text: "Whether both values were allocated on the same page in runtime memory" },
      { label: 'D', text: "Whether both references point to the exact same memory address" },
    ],
    correctIndex: 3,
    hint: "ptr::eq compares pointer addresses, not equality of the referenced values.",
    explanation: "`std::ptr::eq` compares the raw memory addresses of two references, returning `true` only if they point to the exact same memory location, bypassing any `PartialEq` implementation.",
  },
  {
    id: "ptr-supporter-21",
    categorySlug: "pointers",
    title: "std::ptr::drop_in_place Execution",
    prompt: "What does `std::ptr::drop_in_place(ptr)` do to the value at `ptr`?",
    tags: ["pointers","drop-in-place","destructors"],
    difficulty: 3,
    language: 'rust',
    code: "use std::ptr;\n\nfn main() {\n    let mut s = String::from(\"custom drop\");\n    let p: *mut String = &mut s;\n    unsafe {\n        ptr::drop_in_place(p);\n        std::mem::forget(s); // avoid double drop\n    }\n}",
    options: [
      { label: 'A', text: "Executes the destructor of the value in-place without freeing the memory container" },
      { label: 'B', text: "Deallocates the heap memory page and zeroes out all registers within local thread memory" },
      { label: 'C', text: "Transmutes the destination value into an uninitialized MaybeUninit within local thread memory" },
      { label: 'D', text: "Clones the value into the thread-local fallback storage under current compiler safety rules" },
    ],
    correctIndex: 0,
    hint: "drop_in_place runs Drop on the referent without freeing the backing allocation.",
    explanation: "`std::ptr::drop_in_place(ptr)` executes the destructor (`Drop::drop`) and field drop glue for the value at `ptr` in-place, without deallocating the memory holding it.",
  },
  {
    id: "ptr-supporter-22",
    categorySlug: "pointers",
    title: "std::mem::size_of_val Dynamically Sized Types",
    prompt: "What does `std::mem::size_of_val(&[1, 2, 3][..])` return on 64-bit systems?",
    tags: ["pointers","size-of-val","dst"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let slice: &[i32] = &[1, 2, 3];\n    let bytes = std::mem::size_of_val(slice);\n    assert_eq!(bytes, 12);\n}",
    options: [
      { label: 'A', text: "16 bytes (size of the fat pointer reference)" },
      { label: 'B', text: "12 bytes (3 elements * 4 bytes per i32)" },
      { label: 'C', text: "8 bytes (size of a single machine pointer word)" },
      { label: 'D', text: "24 bytes (slice overhead plus capacity)" },
    ],
    correctIndex: 1,
    hint: "size_of_val returns the size in bytes of the pointed-to value (the slice data).",
    explanation: "`std::mem::size_of_val(val)` inspects the metadata of the dynamically sized slice and returns the size of the underlying elements: 3 `i32`s * 4 bytes = 12 bytes.",
  },
  {
    id: "ptr-supporter-23",
    categorySlug: "pointers",
    title: "Cell vs RefCell Overhead",
    prompt: "What is the memory and runtime overhead of `std::cell::Cell<u64>` compared to `u64`?",
    tags: ["pointers","cell","overhead"],
    difficulty: 2,
    language: 'rust',
    code: "use std::cell::Cell;\n\nfn main() {\n    assert_eq!(std::mem::size_of::<Cell<u64>>(), std::mem::size_of::<u64>());\n}",
    options: [
      { label: 'A', text: "8 bytes of borrow counter state and atomic CAS overhead in runtime memory" },
      { label: 'B', text: "16 bytes for vtable dispatch and thread identifier metadata in runtime memory" },
      { label: 'C', text: "Zero memory and zero runtime overhead (pure zero-cost abstraction)" },
      { label: 'D', text: "1 byte for runtime lock flags checked on every read in runtime memory" },
    ],
    correctIndex: 2,
    hint: "Cell has zero runtime borrow checks and the exact same memory layout as the inner type.",
    explanation: "`Cell<T>` has identical memory layout and size as `T` with zero runtime check overhead. It achieves interior mutability purely through compile-time ownership rules (`get`/`set` by value).",
  },
  {
    id: "ptr-supporter-24",
    categorySlug: "pointers",
    title: "Raw Pointer Alignment Requirements",
    prompt: "What happens when dereferencing an unaligned raw pointer with `*ptr`?",
    tags: ["pointers","alignment","undefined-behavior"],
    difficulty: 3,
    language: 'rust',
    code: "// let unaligned: *const u32 = ...;\n// let val = unsafe { *unaligned }; // Undefined Behavior if not 4-byte aligned",
    options: [
      { label: 'A', text: "The CPU automatically fixes alignment with a trap handler in runtime memory" },
      { label: 'B', text: "The compiler inserts bit-shift operations transparently in runtime memory" },
      { label: 'C', text: "The operation returns Ok(0) if alignment fails during runtime execution" },
      { label: 'D', text: "Immediate Undefined Behavior; use `std::ptr::read_unaligned` instead" },
    ],
    correctIndex: 3,
    hint: "Directly dereferencing an unaligned pointer violates Rust's safety invariants.",
    explanation: "In Rust, all reference and standard raw pointer dereferences (`*ptr`) require the pointer to be properly aligned for `T`. To read unaligned memory safely, you must use `std::ptr::read_unaligned`.",
  },
  {
    id: "ptr-supporter-25",
    categorySlug: "pointers",
    title: "std::ptr::read_unaligned",
    prompt: "When should `std::ptr::read_unaligned` be used?",
    tags: ["pointers","unaligned","parsing"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ptr;\n\nfn read_u32_from_bytes(bytes: &[u8]) -> u32 {\n    unsafe { ptr::read_unaligned(bytes.as_ptr() as *const u32) }\n}",
    options: [
      { label: 'A', text: "When reading structured binary data from packed or arbitrary byte offsets" },
      { label: 'B', text: "When synchronizing atomic memory across CPU core pipelines in runtime memory" },
      { label: 'C', text: "When allocating dynamic heap buffers inside async tasks within local thread memory" },
      { label: 'D', text: "When converting trait objects into concrete struct types within local thread memory" },
    ],
    correctIndex: 0,
    hint: "read_unaligned copies bytes without requiring alignment to T's boundary.",
    explanation: "`std::ptr::read_unaligned` reads a `T` from a raw pointer that may not be aligned to `align_of::<T>()`. It is common in network packet parsing and binary protocol deserialization.",
  },
  {
    id: "ptr-supporter-26",
    categorySlug: "pointers",
    title: "Box with Custom Allocator",
    prompt: "How does `Box::new_in(val, alloc)` utilize custom allocators in nightly Rust?",
    tags: ["pointers","box","allocators"],
    difficulty: 3,
    language: 'rust',
    code: "// Box::new_in(42, CustomAllocator)",
    options: [
      { label: 'A', text: "Creates an arena in thread stack space for all future allocations in runtime memory" },
      { label: 'B', text: "Allocates memory using the provided allocator instead of the global allocator" },
      { label: 'C', text: "Converts the pointer into an immutable reference in read-only RAM in runtime memory" },
      { label: 'D', text: "Bypasses memory fragmentation by compressing CPU cache lines within local thread memory" },
    ],
    correctIndex: 1,
    hint: "Allocator-aware collections parameterize allocation via the Allocator trait.",
    explanation: "`Box::new_in` (and the `Allocator` API) allows collections and smart pointers to allocate and deallocate their heap memory using custom memory managers (such as arenas or pool allocators).",
  },
  {
    id: "ptr-supporter-27",
    categorySlug: "pointers",
    title: "Rc Weak Cycles and Leaks",
    prompt: "How does `Rc<RefCell<Node>>` prevent memory leaks in cyclic graph structures?",
    tags: ["pointers","rc","cycles"],
    difficulty: 2,
    language: 'rust',
    code: "use std::rc::{Rc, Weak};\nuse std::cell::RefCell;\n\nstruct Node {\n    parent: Option<Weak<RefCell<Node>>>,\n    children: Vec<Rc<RefCell<Node>>>,\n}",
    options: [
      { label: 'A', text: "By running a background garbage collector on thread idle within local thread memory" },
      { label: 'B', text: "By forcing nodes to be allocated on the call stack exclusively within local thread memory" },
      { label: 'C', text: "By storing back-references as `Weak` pointers to break strong reference cycles" },
      { label: 'D', text: "By converting all child pointers into raw C-style pointers within local thread memory" },
    ],
    correctIndex: 2,
    hint: "Child-to-parent pointers should be Weak to avoid reference count cycles.",
    explanation: "Reference cycles (where node A owns node B and node B owns node A via strong `Rc`) prevent reference counts from ever reaching 0, leaking memory. Using `Weak` for back-references breaks the cycle.",
  },
  {
    id: "ptr-supporter-28",
    categorySlug: "pointers",
    title: "Pinning and Unpin Struct Derivation",
    prompt: "When does a struct automatically implement the `Unpin` trait?",
    tags: ["pointers","unpin","auto-trait"],
    difficulty: 2,
    language: 'rust',
    code: "struct Data {\n    x: i32,\n    s: String,\n}\n\nfn check_unpin<T: Unpin>() {}\n\nfn main() {\n    check_unpin::<Data>();\n}",
    options: [
      { label: 'A', text: "Only when explicitly declared with `#[derive(Unpin)]` in code" },
      { label: 'B', text: "Only if the struct contains no heap-allocated pointer fields" },
      { label: 'C', text: "Only if the struct is marked with the public export modifier" },
      { label: 'D', text: "Automatically if all fields of the struct implement `Unpin`" },
    ],
    correctIndex: 3,
    hint: "Unpin is an auto trait that is derived automatically when all fields are Unpin.",
    explanation: "`Unpin` is an auto trait. If all fields in a struct implement `Unpin` (which all normal standard library types do), the struct automatically implements `Unpin`.",
  },
  {
    id: "ptr-supporter-29",
    categorySlug: "pointers",
    title: "Deref vs DerefMut Target Type",
    prompt: "Can `DerefMut` have a different `Target` associated type than `Deref`?",
    tags: ["pointers","deref","target"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ops::{Deref, DerefMut};\n\nstruct MySmart<T>(T);\n\nimpl<T> Deref for MySmart<T> {\n    type Target = T;\n    fn deref(&self) -> &Self::Target { &self.0 }\n}\nimpl<T> DerefMut for MySmart<T> {\n    fn deref_mut(&mut self) -> &mut Self::Target { &mut self.0 }\n}",
    options: [
      { label: 'A', text: "No; `DerefMut` inherits `type Target` directly from `Deref`" },
      { label: 'B', text: "Yes; `DerefMut` defines its own independent `TargetMut` type" },
      { label: 'C', text: "Only if the struct implements the `AsMut` conversion trait in code" },
      { label: 'D', text: "Only when compiled for 32-bit embedded target systems in code" },
    ],
    correctIndex: 0,
    hint: "DerefMut has Deref as a supertrait and reuses Deref::Target.",
    explanation: "`DerefMut` is defined as `pub trait DerefMut: Deref { fn deref_mut(&mut self) -> &mut Self::Target; }`. It does not declare a new associated type; it reuses `Deref::Target`.",
  },
  {
    id: "ptr-supporter-30",
    categorySlug: "pointers",
    title: "Pin and Box::pin",
    prompt: "What does `Box::pin(value)` return?",
    tags: ["pointers","pin","box"],
    difficulty: 2,
    language: 'rust',
    code: "use std::pin::Pin;\n\nfn main() {\n    let pinned: Pin<Box<i32>> = Box::pin(42);\n    println!(\"{pinned}\");\n}",
    options: [
      { label: 'A', text: "`&mut Pin<T>` borrowing directly from the current stack" },
      { label: 'B', text: "`Pin<Box<T>>` with the value pinned in heap memory" },
      { label: 'C', text: "`Pin<&'static T>` pointing to read-only binary text" },
      { label: 'D', text: "`Box<Pin<T>>` with unpinned inner reference capabilities" },
    ],
    correctIndex: 1,
    hint: "Box::pin allocates the value on the heap and wraps the Box in a Pin.",
    explanation: "`Box::pin(x)` allocates `x` on the heap and returns `Pin<Box<T>>`. Because heap allocations have stable addresses that do not move unless explicitly reallocated, heap pinning is safe for any type.",
  },
  {
    id: "ptr-supporter-31",
    categorySlug: "pointers",
    title: "Unsafe Pointer Cast Rules",
    prompt: "Is casting a `*const u8` to `*const u64` valid before checking alignment?",
    tags: ["pointers","casts","alignment"],
    difficulty: 3,
    language: 'rust',
    code: "fn check(ptr: *const u8) {\n    let p_u64 = ptr as *const u64;\n    let _ = p_u64; // casting is safe; dereferencing requires alignment\n}",
    options: [
      { label: 'A', text: "No; casting between mismatched alignments causes a compiler error in runtime memory" },
      { label: 'B', text: "Only if both types implement the `Copy` marker trait within local thread memory" },
      { label: 'C', text: "Yes; raw pointer casts are always valid, but dereferencing requires alignment" },
      { label: 'D', text: "Only inside functions marked with the `#[inline(always)]` attribute in runtime memory" },
    ],
    correctIndex: 2,
    hint: "Casting pointer types with `as` is safe; dereferencing is unsafe.",
    explanation: "Casting pointer types (e.g. `ptr as *const u64`) is allowed in safe code. The pointer only becomes subject to safety requirements (such as alignment and validity) when dereferenced.",
  },
  {
    id: "ptr-supporter-32",
    categorySlug: "pointers",
    title: "std::ptr::slice_from_raw_parts",
    prompt: "What does `std::ptr::slice_from_raw_parts(data, len)` construct?",
    tags: ["pointers","raw-slices","syntax"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ptr;\n\nfn main() {\n    let arr = [1, 2, 3];\n    let p: *const [i32] = ptr::slice_from_raw_parts(arr.as_ptr(), 3);\n    assert_eq!(unsafe { &*p }, &[1, 2, 3]);\n}",
    options: [
      { label: 'A', text: "A safe borrowed slice `&[T]` with lifetime tied to `data` in code" },
      { label: 'B', text: "An owned `Vec<T>` cloned from the raw buffer elements in runtime memory" },
      { label: 'C', text: "A heap-allocated `Box<[T]>` managing the pointer memory in code" },
      { label: 'D', text: "A raw slice pointer `*const [T]` without creating a reference" },
    ],
    correctIndex: 3,
    hint: "slice_from_raw_parts returns a raw slice pointer *const [T], not a reference.",
    explanation: "`std::ptr::slice_from_raw_parts` builds a raw slice pointer (`*const [T]`). Unlike `std::slice::from_raw_parts` (which returns a reference `&[T]`), creating the raw pointer is safe and does not assert reference validity.",
  },
  {
    id: "ptr-supporter-33",
    categorySlug: "pointers",
    title: "Raw Pointer Addr vs Expose Provenance",
    prompt: "How does `ptr::without_provenance::<T>(addr)` differ from casting an integer with `as *const T`?",
    tags: ["pointers","strict-provenance","provenance"],
    difficulty: 3,
    language: 'rust',
    code: "use std::ptr;\n\nfn main() {\n    let p: *const u8 = ptr::without_provenance(0x1000);\n    assert_eq!(p.addr(), 0x1000);\n}",
    options: [
      { label: 'A', text: "It creates a pointer with explicitly no provenance, valid only for address checks" },
      { label: 'B', text: "It registers the address in the kernel memory map table under current compiler safety rules" },
      { label: 'C', text: "It forces the CPU cache line to prefetch the address bytes within local thread memory" },
      { label: 'D', text: "It converts the address into a valid stack reference under current compiler safety rules" },
    ],
    correctIndex: 0,
    hint: "without_provenance creates a pointer with no memory access rights (wildcard/null provenance).",
    explanation: "`ptr::without_provenance(addr)` creates a pointer with zero provenance. It cannot be legally dereferenced for memory access, but is useful for sentinel values and address comparisons.",
  },
  {
    id: "ptr-supporter-34",
    categorySlug: "pointers",
    title: "Pin Set Method in Option Pinning",
    prompt: "How does `Pin::set(&mut pin, value)` work when `T: !Unpin`?",
    tags: ["pointers","pin","set"],
    difficulty: 3,
    language: 'rust',
    code: "use std::pin::Pin;\n\nfn main() {\n    let mut val = 10;\n    let mut p = Pin::new(&mut val);\n    p.set(20);\n    assert_eq!(*p, 20);\n}",
    options: [
      { label: 'A', text: "It allocates a new heap block and updates the pointer address within local thread memory" },
      { label: 'B', text: "It drops the old value in-place and writes the new value into the pinned location" },
      { label: 'C', text: "It panics if the inner type does not implement the Copy trait within local thread memory" },
      { label: 'D', text: "It suspends the active thread until all borrowers release memory within local thread memory" },
    ],
    correctIndex: 1,
    hint: "p.set(val) drops old in-place and writes new without moving the memory location.",
    explanation: "`Pin::set(&mut self, value)` safely overwrites the pinned location: it drops the existing value in place (`drop_in_place`) and writes `value` into the exact same memory address, upholding the pin contract.",
  },
  {
    id: "ptr-supporter-35",
    categorySlug: "pointers",
    title: "UnsafeCell::get Pointer Mutability",
    prompt: "What pointer type is returned by `UnsafeCell::get(&self)`?",
    tags: ["pointers","unsafe-cell","get"],
    difficulty: 2,
    language: 'rust',
    code: "use std::cell::UnsafeCell;\n\nfn main() {\n    let cell = UnsafeCell::new(42);\n    let ptr: *mut i32 = cell.get();\n    unsafe { *ptr += 1; }\n    println!(\"{}\", unsafe { *cell.get() });\n}",
    options: [
      { label: 'A', text: "An exclusive reference `&mut T`" },
      { label: 'B', text: "A shared immutable pointer `*const T`" },
      { label: 'C', text: "A raw mutable pointer `*mut T`" },
      { label: 'D', text: "A non-null pointer wrapper `NonNull<T>`" },
    ],
    correctIndex: 2,
    hint: "UnsafeCell::get takes &self and returns a *mut T raw pointer.",
    explanation: "`UnsafeCell::get(&self)` takes an immutable shared reference `&self` and returns a raw mutable pointer `*mut T` to the wrapped value, providing the foundational primitive for all interior mutability in Rust.",
  },
  {
    id: "point-code-box-list",
    categorySlug: "pointers",
    title: "Boxed Singly-Linked Node",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["pointers", 'coding'],
    prompt: "Define `pub struct Node { pub val: i32, pub next: Option<Box<Node>> }`. Implement `fn new(val: i32) -> Self` and `fn push(&mut self, val: i32)` which appends a node to the tail.",
    code: "pub struct Node {\n    pub val: i32,\n    pub next: Option<Box<Node>>,\n}\n\nimpl Node {\n    pub fn new(val: i32) -> Self {\n        Self { val, next: None }\n    }\n\n    pub fn push(&mut self, val: i32) {\n        let mut curr = self;\n        while let Some(ref mut next) = curr.next {\n            curr = next;\n        }\n        curr.next = Some(Box::new(Node::new(val)));\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let mut head = Node::new(1);\n    head.push(2);\n    head.push(3);\n    assert_eq!(head.val, 1);\n    assert_eq!(head.next.as_ref().unwrap().val, 2);\n    assert_eq!(head.next.as_ref().unwrap().next.as_ref().unwrap().val, 3);\n    println!(\"test passed\");\n}\n",
    explanation: "Define `pub struct Node { pub val: i32, pub next: Option<Box<Node>> }`. Implement `fn new(val: i32) -> Self` and `fn push(&mut self, val: i32)` which appends a node to the tail. Review the test cases to verify all assertions.",
  },
  {
    id: "point-code-rc-shared",
    categorySlug: "pointers",
    title: "Shared Reference Counting with Rc",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["pointers", 'coding'],
    prompt: "Implement `share_and_count(item: String) -> (std::rc::Rc<String>, usize)` creating an `Rc<String>`, cloning it once, and returning `(cloned_rc, Rc::strong_count(&rc))`.",
    code: "use std::rc::Rc;\n\npub fn share_and_count(item: String) -> (Rc<String>, usize) {\n    let original = Rc::new(item);\n    let clone = Rc::clone(&original);\n    (clone, Rc::strong_count(&original))\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let (rc, count) = share_and_count(String::from(\"data\"));\n    assert_eq!(*rc, \"data\");\n    assert_eq!(count, 2);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `share_and_count(item: String) -> (std::rc::Rc<String>, usize)` creating an `Rc<String>`, cloning it once, and returning `(cloned_rc, Rc::strong_count(&rc))`. Review the test cases to verify all assertions.",
  },
  {
    id: "point-code-refcell-memo",
    categorySlug: "pointers",
    title: "Interior Mutability Counter with RefCell",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["pointers", 'coding'],
    prompt: "Define `pub struct SharedCounter { count: std::cell::RefCell<u64> }` with `new(initial: u64)`, `increment(&self)`, and `get(&self) -> u64`.",
    code: "use std::cell::RefCell;\n\npub struct SharedCounter {\n    count: RefCell<u64>,\n}\n\nimpl SharedCounter {\n    pub fn new(initial: u64) -> Self {\n        Self { count: RefCell::new(initial) }\n    }\n\n    pub fn increment(&self) {\n        *self.count.borrow_mut() += 1;\n    }\n\n    pub fn get(&self) -> u64 {\n        *self.count.borrow()\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let c = SharedCounter::new(0);\n    c.increment();\n    c.increment();\n    assert_eq!(c.get(), 2);\n    println!(\"test passed\");\n}\n",
    explanation: "Define `pub struct SharedCounter { count: std::cell::RefCell<u64> }` with `new(initial: u64)`, `increment(&self)`, and `get(&self) -> u64`. Review the test cases to verify all assertions.",
  },
  {
    id: "point-code-arc-clone",
    categorySlug: "pointers",
    title: "Thread-Safe Vector Sharing with Arc",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["pointers", 'coding'],
    prompt: "Implement `spawn_sum_reader(data: std::sync::Arc<Vec<i32>>) -> std::thread::JoinHandle<i32>` that spawns a thread summing the elements and joins to return the sum.",
    code: "use std::sync::Arc;\nuse std::thread;\n\npub fn spawn_sum_reader(data: Arc<Vec<i32>>) -> thread::JoinHandle<i32> {\n    thread::spawn(move || data.iter().sum())\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let numbers = Arc::new(vec![10, 20, 30]);\n    let handle = spawn_sum_reader(Arc::clone(&numbers));\n    assert_eq!(handle.join().unwrap(), 60);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `spawn_sum_reader(data: std::sync::Arc<Vec<i32>>) -> std::thread::JoinHandle<i32>` that spawns a thread summing the elements and joins to return the sum. Review the test cases to verify all assertions.",
  },
  {
    id: "point-code-cow-normalize",
    categorySlug: "pointers",
    title: "Clone-On-Write Normalizer",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["pointers", 'coding'],
    prompt: "Implement `normalize_str<'a>(s: &'a str) -> std::borrow::Cow<'a, str>` returning borrowed slice if no spaces exist, or owned lowercase String if spaces exist.",
    code: "use std::borrow::Cow;\n\npub fn normalize_str<'a>(s: &'a str) -> Cow<'a, str> {\n    if s.contains(' ') {\n        Cow::Owned(s.to_lowercase())\n    } else {\n        Cow::Borrowed(s)\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let b = normalize_str(\"Rust\");\n    assert!(matches!(b, std::borrow::Cow::Borrowed(\"Rust\")));\n\n    let o = normalize_str(\"Hello World\");\n    assert!(matches!(o, std::borrow::Cow::Owned(ref s) if s == \"hello world\"));\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `normalize_str<'a>(s: &'a str) -> std::borrow::Cow<'a, str>` returning borrowed slice if no spaces exist, or owned lowercase String if spaces exist. Review the test cases to verify all assertions.",
  },
  {
    id: "point-code-box-leak",
    categorySlug: "pointers",
    title: "Leak Box to Static Lifetime",
    difficulty: 3,
    language: 'rust',
    kind: 'coding',
    tags: ["pointers", 'coding'],
    prompt: "Implement `make_static_str(s: String) -> &'static str` using `Box::leak(s.into_boxed_str())`.",
    code: "pub fn make_static_str(s: String) -> &'static str {\n    Box::leak(s.into_boxed_str())\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let s = String::from(\"dynamic_config\");\n    let static_ref: &'static str = make_static_str(s);\n    assert_eq!(static_ref, \"dynamic_config\");\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `make_static_str(s: String) -> &'static str` using `Box::leak(s.into_boxed_str())`. Review the test cases to verify all assertions.",
  },
  {
    id: 'ptr-addr-of-mut-uninit-1',
    categorySlug: 'pointers',
    title: 'Safe Raw Pointer Creation',
    prompt: 'Why is `std::ptr::addr_of_mut!` preferred over `&mut place as *mut _` for uninitialized data?',
    tags: ['pointers', 'raw-pointers', 'addr-of'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'It asserts the referenced data is initialized on stack' },
      { label: 'B', text: 'It creates a raw pointer without producing invalid &mut' },
      { label: 'C', text: 'It allocates memory dynamically using system allocator' },
      { label: 'D', text: 'It casts uninitialized integers into valid zero bytes' },
    ],
    correctIndex: 1,
    hint: 'Creating an intermediate &mut to uninitialized memory is instant UB.',
    explanation: '`addr_of_mut!` directly computes the raw pointer to a memory location without creating an intermediate `&mut T` reference, which requires validity.',
  },
  {
    id: 'ptr-null-mut-cast-1',
    categorySlug: 'pointers',
    title: 'Null Pointer Dereference',
    prompt: 'What happens when dereferencing `std::ptr::null_mut::<i32>()` inside an `unsafe` block?',
    tags: ['pointers', 'null', 'ub'],
    difficulty: 1,
    language: 'rust',
    options: [
      { label: 'A', text: 'Dereferencing returns a default unit value safely' },
      { label: 'B', text: 'Dereferencing executes a clean hardware interrupt' },
      { label: 'C', text: 'Dereferencing automatically catches unwinding panic' },
      { label: 'D', text: 'Dereferencing triggers immediate undefined behavior' },
    ],
    correctIndex: 3,
    hint: 'Dereferencing null in unsafe Rust is undefined behavior.',
    explanation: 'Dereferencing a null pointer is undefined behavior (UB) in Rust, which can cause segfaults, compiler miscompilations, or crashes.',
  },
  {
    id: 'ptr-nonnull-dangling-1',
    categorySlug: 'pointers',
    title: 'NonNull Dangling Pointer',
    prompt: 'What does `std::ptr::NonNull::dangling()` return?',
    tags: ['pointers', 'non-null', 'dangling'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It returns a well-aligned non-null sentinel pointer' },
      { label: 'B', text: 'It allocates one byte from OS virtual memory pool' },
      { label: 'C', text: 'It panics immediately if the generic type is zero' },
      { label: 'D', text: 'It points directly to the start of program binary' },
    ],
    correctIndex: 0,
    hint: 'NonNull::dangling produces a properly aligned sentinel pointer for empty containers.',
    explanation: '`NonNull::dangling()` returns a properly aligned, non-null pointer without allocating memory, widely used by collections (like empty `Vec`) as a sentinel.',
  },
  {
    id: 'ptr-offset-from-isize-1',
    categorySlug: 'pointers',
    title: 'Pointer Offset From Invariant',
    prompt: 'What condition is required for `ptr.offset_from(origin)` to be sound?',
    tags: ['pointers', 'offset', 'safety'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'Both pointers can point to separate global static arrays' },
      { label: 'B', text: 'The operation ignores byte padding between array elements' },
      { label: 'C', text: 'Both pointers must originate from same memory allocation' },
      { label: 'D', text: 'Pointers must be aligned to 64-byte hardware cache lines' },
    ],
    correctIndex: 2,
    hint: 'Pointer subtraction requires both pointers to point within the same allocated object.',
    explanation: '`offset_from` requires that both pointers point to or one past the end of the same allocated object and that their distance in bytes fits in `isize`.',
  },
  {
    id: 'ptr-provenance-expose-addr-1',
    categorySlug: 'pointers',
    title: 'Pointer Provenance and expose_addr',
    prompt: 'What is the role of `expose_addr` in Rust pointer provenance models?',
    tags: ['pointers', 'provenance'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'It clears the pointer provenance and frees heap blocks' },
      { label: 'B', text: 'It exposes address integer while preserving provenance' },
      { label: 'C', text: 'It converts pointers into random 64-bit integer values' },
      { label: 'D', text: 'It turns raw addresses into compile-time const literals' },
    ],
    correctIndex: 1,
    hint: 'Exposing address informs compiler analysis that integer casts may recover provenance.',
    explanation: '`expose_addr` converts a pointer to `usize` while exposing its provenance, signaling to compiler optimization passes that this address may later be cast back to a pointer.',
  },
  {
    id: 'ptr-align-of-val-raw-1',
    categorySlug: 'pointers',
    title: 'Raw Pointer Alignment Query',
    prompt: 'What safety advantage does `std::mem::align_of_val_raw` offer over `align_of_val`?',
    tags: ['pointers', 'alignment', 'raw'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It inspects memory alignment without creating a reference' },
      { label: 'B', text: 'It dereferences the target memory to read runtime header' },
      { label: 'C', text: 'It adjusts pointer alignment by rounding down to zero byte' },
      { label: 'D', text: 'It enforces runtime alignment assertions inside debug mode' },
    ],
    correctIndex: 0,
    hint: 'align_of_val_raw operates directly on *const T without requiring a valid reference.',
    explanation: '`align_of_val_raw` computes type alignment from a raw pointer without requiring a valid reference, making it sound even for uninitialized or unaligned memory.',
  },
  {
    id: 'ptr-copy-nonoverlapping-memcpy-1',
    categorySlug: 'pointers',
    title: 'ptr::copy_nonoverlapping Invariant',
    prompt: 'What condition must hold when calling `std::ptr::copy_nonoverlapping`?',
    tags: ['pointers', 'copy', 'memcpy', 'safety'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'Source and destination memory regions are allowed to overlap' },
      { label: 'B', text: 'Source and destination memory regions must not overlap at all' },
      { label: 'C', text: 'It allocates an intermediate heap buffer during memory copy' },
      { label: 'D', text: 'It runs destructors for existing values at the target memory' },
    ],
    correctIndex: 1,
    hint: 'copy_nonoverlapping corresponds to C\'s memcpy and forbids overlapping memory.',
    explanation: '`copy_nonoverlapping` requires that the source and destination memory regions do not overlap (equivalent to `memcpy`). For overlapping memory, use `ptr::copy` (memmove).',
  },
  {
    id: 'ptr-read-unaligned-soundness-1',
    categorySlug: 'pointers',
    title: 'Unaligned Memory Reading',
    prompt: 'How does `std::ptr::read_unaligned` safely read a value of type `T`?',
    tags: ['pointers', 'unaligned', 'safety'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It asserts that the pointer address is a power-of-two number' },
      { label: 'B', text: 'It allocates a 64-byte aligned stack frame to copy bytes' },
      { label: 'C', text: 'It converts unaligned pointers into heap-allocated boxes' },
      { label: 'D', text: 'It reads values from memory without requiring type alignment' },
    ],
    correctIndex: 3,
    hint: 'read_unaligned copies bytes directly without enforcing T\'s alignment requirements.',
    explanation: '`read_unaligned` reads a value `T` from memory without requiring the pointer to be aligned to `align_of::<T>()`, preventing hardware alignment faults.',
  },
  {
    id: 'ptr-write-bytes-zeroing-1',
    categorySlug: 'pointers',
    title: 'ptr::write_bytes (memset)',
    prompt: 'What is the behavior of `std::ptr::write_bytes(dst, byte, count)`?',
    tags: ['pointers', 'memset', 'write-bytes'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It sets count * size_of::<T>() bytes without dropping' },
      { label: 'B', text: 'It executes drop_in_place on every overwritten element' },
      { label: 'C', text: 'It allocates fresh zero-filled pages from OS virtual mem' },
      { label: 'D', text: 'It only works on pointers to unsigned integer primitives' },
    ],
    correctIndex: 0,
    hint: 'write_bytes is Rust\'s equivalent of memset and does not drop overwritten values.',
    explanation: '`write_bytes` sets `count * size_of::<T>()` contiguous bytes to the given byte value (like `memset`), without invoking `Drop` on existing contents.',
  },
  {
    id: 'ptr-slice-from-raw-parts-len-1',
    categorySlug: 'pointers',
    title: 'Raw Slice Pointer Construction',
    prompt: 'Why is `std::ptr::slice_from_raw_parts` a safe (non-unsafe) function?',
    tags: ['pointers', 'slice-from-raw-parts', 'raw'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It allocates an owned slice vector on the system heap' },
      { label: 'B', text: 'It panics at runtime if the pointer address is null' },
      { label: 'C', text: 'It constructs a raw fat pointer *const [T] safely' },
      { label: 'D', text: 'It dereferences the pointer to verify element bounds' },
    ],
    correctIndex: 2,
    hint: 'Constructing a raw fat pointer (*const [T]) does not dereference memory or create references.',
    explanation: '`slice_from_raw_parts` merely packages a data pointer and length into a fat raw pointer `*const [T]`. Because raw pointers are not dereferenced, the constructor itself is safe.',
  },
  {
    id: 'ptr-strict-provenance-with-addr-1',
    categorySlug: 'pointers',
    title: 'Strict Provenance with_addr',
    prompt: 'What does `ptr.with_addr(new_addr)` do under strict provenance rules?',
    tags: ['pointers', 'provenance', 'strict-provenance'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'It replaces pointer address while retaining provenance' },
      { label: 'B', text: 'It discards all provenance tags from the raw pointer' },
      { label: 'C', text: 'It converts pointers into arbitrary 64-bit integers' },
      { label: 'D', text: 'It allocates fresh provenance identifiers dynamically' },
    ],
    correctIndex: 0,
    hint: 'with_addr creates a pointer with the new address but copies the provenance of the receiver.',
    explanation: '`with_addr` creates a new raw pointer with the address `new_addr` while preserving the exact provenance (allocation permission) of `self`.',
  },
  {
    id: 'ptr-null-dangling-zst-compare-1',
    categorySlug: 'pointers',
    title: 'Raw Pointers to Zero-Sized Types',
    prompt: 'What is true about dereferencing dangling, properly aligned raw pointers to zero-sized types (ZST)?',
    tags: ['pointers', 'zst', 'dangling'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'Zero-sized types cannot have raw pointer addresses' },
      { label: 'B', text: 'Dangling non-null pointers are valid for ZST reads' },
      { label: 'C', text: 'Reading from a dangling ZST pointer is instant UB' },
      { label: 'D', text: 'ZST pointers must always be cast to null pointers' },
    ],
    correctIndex: 1,
    hint: 'ZST reads/writes access 0 bytes of memory, so any aligned non-null pointer is valid.',
    explanation: 'Because zero-sized types occupy 0 bytes, reading or writing a ZST through a properly aligned, non-null (even dangling) pointer performs zero memory accesses and is sound.',
  },
]
