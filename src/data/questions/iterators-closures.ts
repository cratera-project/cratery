import type { Question } from '../../lib/quiz'

export const iteratorsClosuresQuestions: Question[] = [
  {
    id: 'iter-vs-loop-1',
    categorySlug: 'iterators-closures',
    title: 'Iterators vs Loops',
    prompt: 'When is an iterator chain usually preferred over a manual `for` loop?',
    tags: ['iterators', 'loops'],
    difficulty: 2,
    language: 'rust',
    code: `// A
let evens: Vec<_> = nums.iter().filter(|n| *n % 2 == 0).collect();

// B
let mut evens = Vec::new();
for n in &nums {
    if n % 2 == 0 {
        evens.push(*n);
    }
}`,
    options: [
      { label: 'A', text: "For clear map/filter-style transforms over data" },
      { label: 'B', text: "Always; explicit loops are obsolete in Rust in code" },
      { label: 'C', text: "Only when you need identical machine code always" },
      { label: 'D', text: "Never; hand-written loops are always faster in code" },
    ],
    correctIndex: 0,
    hint: 'Prefer expressing the transformation, not micro-optimizing by default.',
    explanation:
      'Iterator adapters read as the transformation you want and compose well. LLVM often optimizes them similarly to loops. Prefer a loop when control flow is complex (early exits, multi-step state) rather than from a blanket speed rule.',
  },
  {
    id: 'iter-closure-ownership-1',
    categorySlug: 'iterators-closures',
    title: 'Closure Ownership',
    prompt: 'How does this closure capture `prefix` by default?',
    tags: ['closures', 'ownership'],
    difficulty: 2,
    language: 'rust',
    code: `let prefix = "Item: ";
let items = vec!["A", "B"];
let labeled: Vec<_> = items
    .iter()
    .map(|item| format!("{prefix}{item}"))
    .collect();`,
    options: [
      { label: 'A', text: 'It always moves captures; `move` is only for threads' },
      { label: 'B', text: 'It copies `prefix` because every `&str` is heap data' },
      { label: 'C', text: 'It fails unless you write `move` on the closure' },
      { label: 'D', text: 'It borrows `prefix`; `move` would capture by value' },
    ],
    correctIndex: 3,
    hint: 'Closures capture the least they need unless you force `move`.',
    explanation:
      'By default the closure immutably borrows `prefix`. `move` forces capture by value. Here `prefix: &str` is `Copy`, so `move` copies the reference, not the string bytes.',
  },
  {
    id: 'iter-lazy-1',
    categorySlug: 'iterators-closures',
    title: 'Lazy Evaluation',
    prompt: 'What does this program print?',
    tags: ['iterators', 'lazy'],
    difficulty: 3,
    language: 'rust',
    code: `fn main() {
    let v = vec![1, 2, 3];
    let _iter = v.iter().map(|x| {
        println!("Processing {x}");
        x * 2
    });
    println!("Iterator created");
}`,
    options: [
      { label: 'A', text: '`Processing` lines first, then `Iterator created`' },
      { label: 'B', text: 'A compile error: closures cannot call `println!`' },
      { label: 'C', text: 'Only `Iterator created` (the map never runs)' },
      { label: 'D', text: 'A panic because the iterator was not consumed' },
    ],
    correctIndex: 2,
    hint: 'Adapters are lazy until a consumer pulls values.',
    explanation:
      'Creating a `map` adapter does not run the closure. Work happens when something consumes the iterator (`collect`, `for`, `next`, …). Leaving it unused is fine; there is no panic.',
  },
  {
    id: 'iter-fold-1',
    categorySlug: 'iterators-closures',
    title: 'Fold Accumulation',
    prompt: 'What does `.fold` do that plain `.map` does not?',
    tags: ['iterators', 'fold'],
    difficulty: 2,
    language: 'rust',
    code: `let sum = nums.iter().fold(0, |acc, x| acc + x);`,
    options: [
      { label: 'A', text: 'Keep only elements that match a predicate' },
      { label: 'B', text: 'Reduce many items into one accumulated value' },
      { label: 'C', text: 'Transform each element into another element' },
      { label: 'D', text: 'Reverse the order of the underlying iterator' },
    ],
    correctIndex: 1,
    hint: '`fold` carries state from one step into the next.',
    explanation:
      '`fold` starts from an initial accumulator and combines each item into it, producing one final value. `map` transforms items one-by-one without reducing them.',
  },
  {
    id: 'iter-idiomatic-1',
    categorySlug: 'iterators-closures',
    title: 'Idiomatic Iterator Chains',
    prompt: 'Which style is more idiomatic for this search?',
    tags: ['iterators', 'idiomatic'],
    difficulty: 2,
    language: 'rust',
    code: `// A
nums.iter().filter(|n| **n > 10).find(|n| **n % 2 == 0);

// B
for n in &nums {
    if *n > 10 && *n % 2 == 0 {
        return Some(n);
    }
}
None`,
    options: [
      { label: 'A', text: 'B only; iterator adapters are rarely idiomatic' },
      { label: 'B', text: 'Neither; this search must use recursion instead' },
      { label: 'C', text: 'A; the chain states filter-then-find intent clearly' },
      { label: 'D', text: 'B only; `find` cannot express compound conditions' },
    ],
    correctIndex: 2,
    hint: 'Idiomatic Rust often reads the query as a chain of adapters.',
    explanation:
      'For a simple search/transform, adapters like `filter`/`find` (or a single `find` with a compound predicate) are idiomatic. Loops are fine for messier control flow, but not required here.',
  },
  {
    id: 'iter-collect-type-1',
    categorySlug: 'iterators-closures',
    title: 'Collect Type Inference',
    prompt: 'Why does this fail to compile?',
    tags: ['iterators', 'type-inference'],
    difficulty: 2,
    language: 'rust',
    code: `let nums = vec![1, 2, 3];
let doubled = nums.iter().map(|x| x * 2).collect();`,
    options: [
      { label: 'A', text: '`collect` needs a target type; annotate `Vec<_>`' },
      { label: 'B', text: '`collect` is invalid; use `for_each` to consume' },
      { label: 'C', text: '`map` cannot multiply `&i32`; the chain is illegal' },
      { label: 'D', text: '`collect` demands mutable ownership of `nums`' },
    ],
    correctIndex: 0,
    hint: '`collect` is generic over many collection types.',
    explanation:
      '`FromIterator` is implemented for many types (`Vec`, `HashSet`, …), so Rust needs a concrete target. Write `.collect::<Vec<_>>()` or `let doubled: Vec<_> = ...`. Multiplying `&i32` by `2` is fine via auto-deref/`Mul`.',
  },
  {
    id: 'closure-fn-traits-1',
    categorySlug: 'iterators-closures',
    title: 'Fn vs FnMut vs FnOnce',
    prompt: 'Which trait best describes calling this closure repeatedly?',
    tags: ['closures', 'traits'],
    difficulty: 3,
    language: 'rust',
    code: `let mut count = 0;
let mut closure = || {
    count += 1;
    count
};`,
    options: [
      { label: 'A', text: '`Fn` only, because it never takes ownership of data' },
      { label: 'B', text: '`FnOnce` only, because each call consumes `count`' },
      { label: 'C', text: 'No Fn traits; mutable captures are unsupported' },
      { label: 'D', text: '`FnMut`, because it mutably borrows `count`' },
    ],
    correctIndex: 3,
    hint: 'Ask whether the call needs `&`, `&mut`, or ownership of the capture.',
    explanation:
      'Mutating a captured variable requires `FnMut`. All closures implement `FnOnce`; this one is not limited to a single call. It does not implement `Fn`, which allows calling through a shared `&self`.',
  },
  {
    id: 'iter-chain-efficiency-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator Chain Performance',
    prompt: 'Where do allocations happen in this chain?',
    tags: ['iterators', 'performance'],
    difficulty: 2,
    language: 'rust',
    code: `let result: Vec<_> = nums
    .iter()
    .filter(|n| **n > 0)
    .map(|n| n * 2)
    .take(5)
    .collect();`,
    options: [
      { label: 'A', text: 'Each adapter builds a temporary `Vec` of results' },
      { label: 'B', text: 'Only `collect` allocates; adapters stay lazy wrappers' },
      { label: 'C', text: 'Only `filter` allocates; later steps mutate in place' },
      { label: 'D', text: 'The parser rewrites the chain into raw C-style loops' },
    ],
    correctIndex: 1,
    hint: 'Adapters return iterator types; consumers force work.',
    explanation:
      '`filter`/`map`/`take` are lazy zero-cost wrappers. The closure work runs as `collect` pulls items, and that is when the output `Vec` is allocated. LLVM may optimize further, but that is not a parse-time rewrite.',
  },
  {
    id: 'closure-move-copy-1',
    categorySlug: 'iterators-closures',
    title: 'Move Closures with Copy Types',
    prompt: 'Is `x` still usable after creating this `move` closure?',
    tags: ['closures', 'ownership', 'copy'],
    difficulty: 2,
    language: 'rust',
    code: `let x = 42;
let closure = move || println!("{x}");
closure();
println!("x is {x}");`,
    options: [
      { label: 'A', text: 'No; `move` always invalidates the original binding' },
      { label: 'B', text: 'Yes, but only because `move` still borrows mutably' },
      { label: 'C', text: 'Yes; `i32: Copy`, so capture copies the value' },
      { label: 'D', text: 'No; using `x` afterward is undefined behavior' },
    ],
    correctIndex: 2,
    hint: '`move` means capture by value; `Copy` types still copy.',
    explanation:
      '`move` forces by-value capture. For `i32`, that value is copied, so `x` remains valid. A non-`Copy` type like `String` would be moved and unusable afterward.',
  },
  {
    id: 'iter-filter-map-1',
    categorySlug: 'iterators-closures',
    title: 'filter_map vs filter + map',
    prompt: 'When is `.filter_map` the better fit?',
    tags: ['iterators', 'combinators'],
    difficulty: 2,
    language: 'rust',
    code: `// A
strs.iter().filter_map(|s| s.parse::<i32>().ok())

// B
strs.iter()
    .map(|s| s.parse::<i32>())
    .filter(|r| r.is_ok())
    .map(|r| r.unwrap())`,
    options: [
      { label: 'A', text: 'When mapping can both transform and drop failures' },
      { label: 'B', text: 'Only when separating filter and map is required' },
      { label: 'C', text: 'Only for `Result`; it cannot work with `Option`' },
      { label: 'D', text: 'Never; the two-step form is always preferred' },
    ],
    correctIndex: 0,
    hint: '`filter_map` expects an `Option` from the closure.',
    explanation:
      '`filter_map` maps to `Option<U>`, keeps `Some`, and discards `None`, ideal for parse-or-skip. It works with any `Option`, not only `Result` (often via `.ok()`). The longer chain is noisier and uses `unwrap` after filtering.',
  },
  {
    id: 'iter-into-iter-1',
    categorySlug: 'iterators-closures',
    title: 'into_iter vs iter',
    prompt: 'What is the key difference between these methods?',
    tags: ['iterators', 'ownership'],
    difficulty: 2,
    language: 'rust',
    code: `let v = vec![1, 2, 3];
for _ in v.iter() {}
for _ in v.into_iter() {}`,
    options: [
      { label: 'A', text: 'They are identical for every collection type' },
      { label: 'B', text: '`into_iter` is deprecated; always prefer `iter`' },
      { label: 'C', text: '`iter` moves items out; `into_iter` only borrows' },
      { label: 'D', text: '`into_iter` consumes `v`; `iter` only borrows it' },
    ],
    correctIndex: 3,
    hint: 'After which call is `v` still usable?',
    explanation:
      '`iter()` yields `&T` and leaves the collection owned. `into_iter()` consumes the collection and yields owned `T`. A bare `for x in v` desugars to `into_iter()`.',
  },
  {
    id: 'closure-lifetime-1',
    categorySlug: 'iterators-closures',
    title: 'Closure Lifetimes',
    prompt: 'Does this function compile?',
    tags: ['closures', 'lifetimes'],
    difficulty: 3,
    language: 'rust',
    code: `fn make_closure<'a>(s: &'a str) -> impl Fn() -> &'a str + 'a {
    move || s
}`,
    options: [
      { label: 'A', text: 'No; closures are unsized and cannot return refs' },
      { label: 'B', text: 'Yes; it captures `s` and returns that borrow' },
      { label: 'C', text: 'No; `impl Fn` cannot appear in return position' },
      { label: 'D', text: 'No; only `Box<dyn Fn()>` may return references' },
    ],
    correctIndex: 1,
    hint: 'The returned closure\'s lifetime is tied to `s`.',
    explanation:
      'This compiles: the closure captures `s: &\'a str` and returns it. Returning `impl Fn() -> &\'a str + \'a` (or an equivalent bound) is valid. Boxing is only needed for type erasure or mixed concrete closure types.',
  },
  {
    id: 'iter-flat-map-1',
    categorySlug: 'iterators-closures',
    title: 'flat_map Usage',
    prompt: 'What does `.flat_map` add over plain `.map`?',
    tags: ['iterators', 'combinators'],
    difficulty: 2,
    language: 'rust',
    code: `let nested = vec![vec![1, 2], vec![3, 4]];
let flat: Vec<_> = nested.iter().flat_map(|v| v.iter()).collect();`,
    options: [
      { label: 'A', text: 'Nothing; `.map` already flattens nested iterators' },
      { label: 'B', text: 'It deduplicates mapped values like a set insert' },
      { label: 'C', text: 'It maps each item to an iterator, then flattens' },
      { label: 'D', text: 'It only accepts `Vec<Vec<_>>`, unlike general `.map`' },
    ],
    correctIndex: 2,
    hint: 'Think `.map(f).flatten()`.',
    explanation:
      '`flat_map(f)` is `.map(f).flatten()`: each element produces an iterable (`IntoIterator`), and one level of nesting is removed. It works with many iterable outputs, including `Option`/`Result` as 0-or-1 iterators.',
  },
  {
    id: 'iter-zip-1',
    categorySlug: 'iterators-closures',
    title: 'Zipping Iterators',
    prompt: 'What happens when zipped iterators have different lengths?',
    tags: ['iterators', 'combinators'],
    difficulty: 2,
    language: 'rust',
    code: `let a = vec![1, 2, 3];
let b = vec!["x", "y"];
let zipped: Vec<_> = a.iter().zip(b.iter()).collect();`,
    options: [
      { label: 'A', text: 'It stops at the shorter length (two pairs here)' },
      { label: 'B', text: 'It fails to compile because element types differ' },
      { label: 'C', text: 'It pads the shorter side with default values' },
      { label: 'D', text: 'It panics unless both iterators have equal length' },
    ],
    correctIndex: 0,
    hint: '`zip` ends when either side is exhausted.',
    explanation:
      '`Iterator::zip` yields pairs until one side ends. Different payload types are fine (`(&i32, &&str)`). No padding and no length panic in std.',
  },
  {
    id: 'closure-capture-scope-1',
    categorySlug: 'iterators-closures',
    title: 'Closure Capture Scope',
    prompt: 'Which bindings do these closures capture?',
    tags: ['closures', 'capture'],
    difficulty: 2,
    language: 'rust',
    code: `fn process(data: &[i32], threshold: i32) {
    let factor = 2;
    let _result: Vec<_> = data
        .iter()
        .filter(|&&x| x > threshold)
        .map(|&x| x * factor)
        .collect();
}`,
    options: [
      { label: 'A', text: 'None; adapter closures never capture outer names' },
      { label: 'B', text: 'Only `factor`; parameters are never captures' },
      { label: 'C', text: '`data`, `threshold`, and `factor` all get captured' },
      { label: 'D', text: '`threshold` and `factor` from the enclosing scope' },
    ],
    correctIndex: 3,
    hint: 'Capture means the closure body names that binding.',
    explanation:
      'The `filter` closure reads `threshold`; the `map` closure reads `factor`. `data` is used to build the iterator, not inside those closures, so it is not captured by them.',
  },
  {
    id: 'iter-take-while-1',
    categorySlug: 'iterators-closures',
    title: 'take_while vs take',
    prompt: 'How do `.take` and `.take_while` differ?',
    tags: ['iterators', 'combinators'],
    difficulty: 2,
    language: 'rust',
    code: `nums.iter().take(3);
nums.iter().take_while(|&&x| x < 10);`,
    options: [
      { label: 'A', text: '`take` consumes; `take_while` only borrows items' },
      { label: 'B', text: '`take` uses a count; `take_while` uses a predicate' },
      { label: 'C', text: '`take_while` is only for infinite iterators' },
      { label: 'D', text: 'They match; `take_while` is just an older name' },
    ],
    correctIndex: 1,
    hint: 'One stops after N items; the other stops on a failed test.',
    explanation:
      '`take(n)` yields at most `n` items. `take_while` yields while the predicate holds and stops at the first failure (even if later items would pass). Both are lazy adapters.',
  },
  {
    id: 'iter-enumerate-1',
    categorySlug: 'iterators-closures',
    title: 'Enumerate Pattern',
    prompt: 'What does `.enumerate` add to each item?',
    tags: ['iterators', 'combinators'],
    difficulty: 1,
    language: 'rust',
    code: `for (i, val) in [10, 20, 30].iter().enumerate() {
    println!("{i}: {val}");
}`,
    options: [
      { label: 'A', text: 'The memory address of the element being visited' },
      { label: 'B', text: 'A countdown of how many items remain ahead' },
      { label: 'C', text: 'A 0-based index paired with the element' },
      { label: 'D', text: 'A stable hash used for keyed lookups later' },
    ],
    correctIndex: 2,
    hint: 'It is the idiomatic replacement for a manual counter.',
    explanation:
      '`enumerate` yields `(usize, Item)` starting at index `0`. It does not expose addresses, remaining counts, or hashes.',
  },
  {
    id: 'closure-return-1',
    categorySlug: 'iterators-closures',
    title: 'Returning Closures',
    prompt: 'Why return `Box<dyn Fn(i32) -> i32>` here?',
    tags: ['closures', 'return-types'],
    difficulty: 3,
    language: 'rust',
    code: `fn make_op(add: bool) -> Box<dyn Fn(i32) -> i32> {
    if add {
        Box::new(|x| x + 1)
    } else {
        Box::new(|x| x - 1)
    }
}`,
    options: [
      { label: 'A', text: 'Every closure is unsized and must live on the heap' },
      { label: 'B', text: '`impl Fn` already unifies both branches without boxing' },
      { label: 'C', text: 'Modern Rust removed `impl Trait` in return types' },
      { label: 'D', text: 'The branches are different closure types at compile time' },
    ],
    correctIndex: 3,
    hint: 'Each closure expression has its own unique anonymous type.',
    explanation:
      '`impl Fn` works for one concrete closure type. Here the `if` arms produce two distinct types, so they cannot unify. `Box<dyn Fn...>` erases them behind one trait object. A single-branch function can return `impl Fn` without boxing.',
  },
  {
    id: 'iter-partition-1',
    categorySlug: 'iterators-closures',
    title: 'Partition Method',
    prompt: 'What does `.partition` return?',
    tags: ['iterators', 'combinators'],
    difficulty: 2,
    language: 'rust',
    code: `let nums = vec![1, 2, 3, 4, 5, 6];
let (evens, odds): (Vec<_>, Vec<_>) =
    nums.into_iter().partition(|&x| x % 2 == 0);`,
    options: [
      { label: 'A', text: 'Two collections split by a boolean predicate' },
      { label: 'B', text: 'Fixed-size chunks meant for parallel workers' },
      { label: 'C', text: 'Only the rejected elements; matches are dropped' },
      { label: 'D', text: 'Runs of equal consecutive values as sub-vectors' },
    ],
    correctIndex: 0,
    hint: 'True goes left; false goes right.',
    explanation:
      '`partition` consumes the iterator and builds two collections: predicate-true first, false second. It keeps both sides, unlike `filter`. Chunking/grouping consecutive equals are different adapters/crates.',
  },
  {
    id: 'iter-scan-1',
    categorySlug: 'iterators-closures',
    title: 'Scan Accumulator',
    prompt: 'How does `.scan` differ from `.map`?',
    tags: ['iterators', 'stateful'],
    difficulty: 3,
    language: 'rust',
    code: `let running_sum: Vec<_> = [1, 2, 3, 4]
    .into_iter()
    .scan(0, |acc, x| {
        *acc += x;
        Some(*acc)
    })
    .collect();`,
    options: [
      { label: 'A', text: 'No difference; `scan` is only an alias of `map`' },
      { label: 'B', text: '`scan` keeps mutable state across yielded items' },
      { label: 'C', text: '`scan` works only for numbers; `map` is general' },
      { label: 'D', text: '`scan` always parallelizes; `map` stays sequential' },
    ],
    correctIndex: 1,
    hint: 'Notice the accumulator argument threaded through calls.',
    explanation:
      '`scan` is a stateful adapter: the closure gets `&mut State` and may yield `Some`/`None` (ending early). `map` is stateless per element. Use `scan` for running totals and similar pipelines.',
  },
  {
    id: 'iter-by-ref-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator by_ref',
    prompt: 'What does `.by_ref()` let you do?',
    tags: ['iterators', 'by-ref'],
    difficulty: 2,
    language: 'rust',
    code: `let mut it = [1, 2, 3, 4].into_iter();
let first_two: Vec<_> = it.by_ref().take(2).collect();
let rest: Vec<_> = it.collect();`,
    options: [
      { label: 'A', text: 'Clone every remaining item before further adapters run' },
      { label: 'B', text: 'Convert the iterator into a parallel `rayon` pipeline' },
      { label: 'C', text: 'Borrow the iterator so later code can keep using it' },
      { label: 'D', text: 'Force the iterator to restart from the first element' },
    ],
    correctIndex: 2,
    hint: 'Adapters often take `self`; `by_ref` peels that off.',
    explanation:
      'Many iterator adapters consume `self`. `.by_ref()` borrows the iterator so you can apply adapters (like `take`) and still use the original afterward for the remainder.',
  },
  {
    id: 'iter-fuse-1',
    categorySlug: 'iterators-closures',
    title: 'Fused Iterators',
    prompt: 'What guarantee does `.fuse()` add?',
    tags: ['iterators', 'fuse'],
    difficulty: 2,
    language: 'rust',
    code: `let mut it = [1].into_iter().fuse();
assert_eq!(it.next(), Some(1));
assert_eq!(it.next(), None);
assert_eq!(it.next(), None);`,
    options: [
      { label: 'A', text: 'After the first `None`, further `next` calls stay `None`' },
      { label: 'B', text: 'The iterator panics if `next` is called after exhaustion' },
      { label: 'C', text: 'Items are returned in reverse order until empty' },
      { label: 'D', text: '`fuse` makes iteration run across OS threads automatically' },
    ],
    correctIndex: 0,
    hint: 'Some iterators can yield values again after `None`.',
    explanation:
      'A fused iterator promises that once `next` returns `None`, all later `next` calls also return `None`. `.fuse()` wraps iterators that might not uphold that, which matters for combinators that assume fusion.',
  },
  {
    id: 'closure-fnonce-1',
    categorySlug: 'iterators-closures',
    title: 'FnOnce Consumption',
    prompt: 'Why can this closure implement only `FnOnce`?',
    tags: ['closures', 'fnonce'],
    difficulty: 2,
    language: 'rust',
    code: `fn consume<F: FnOnce()>(f: F) {
    f();
}

fn main() {
    let s = String::from("hi");
    consume(|| drop(s));
}`,
    options: [
      { label: 'A', text: 'All closures that print text are limited to `FnOnce`' },
      { label: 'B', text: '`drop(s)` moves `s`, so the closure can run only once' },
      { label: 'C', text: '`FnOnce` means the closure captures nothing at all' },
      { label: 'D', text: '`String` captures always require `FnMut`, never `FnOnce`' },
    ],
    correctIndex: 1,
    hint: 'Moved captures cannot be used on a second call.',
    explanation:
      'Calling the closure moves `s` into `drop`. A second call would have nothing left to move, so the closure is `FnOnce` only, not `FnMut`/`Fn`. Closures that only borrow can be called repeatedly.',
  },
  {
    id: 'iter-copied-1',
    categorySlug: 'iterators-closures',
    title: 'copied vs cloned',
    prompt: 'What does `.copied()` require and do?',
    tags: ['iterators', 'copied'],
    difficulty: 2,
    language: 'rust',
    code: `fn main() {
    let v = [1, 2, 3];
    let s: Vec<i32> = v.iter().copied().collect();
    assert_eq!(s, vec![1, 2, 3]);
}`,
    options: [
      { label: 'A', text: '`T: Copy`; it copies `&T` items into owned `T`' },
      { label: 'B', text: '`T: Clone`; it always heap-allocates each item' },
      { label: 'C', text: 'No bounds; it converts the iterator into a `Vec`' },
      { label: 'D', text: '`T: Copy`; it moves elements out of the slice' },
    ],
    correctIndex: 0,
    hint: '`iter()` yields `&T`; `copied` turns that into `T`.',
    explanation:
      '`.copied()` is for iterators of `&T` where `T: Copy`: it bitwise-copies each item. `.cloned()` needs `T: Clone` and calls `clone()` (which may allocate). Neither consumes the slice; `v.iter()` still only borrows. `collect` is a separate adapter.',
  },
  {
    id: 'iter-peekable-1',
    categorySlug: 'iterators-closures',
    title: 'Peekable Lookahead',
    prompt: 'After `peek()` returns `Some(&10)`, what does `next()` return?',
    tags: ['iterators', 'peekable'],
    difficulty: 2,
    language: 'rust',
    code: `fn main() {
    let mut it = [10, 20].into_iter().peekable();
    assert_eq!(it.peek(), Some(&10));
    assert_eq!(it.next(), Some(10));
}`,
    options: [
      { label: 'A', text: '`Some(20)`, because `peek` already consumed 10' },
      { label: 'B', text: '`None`, because `peek` exhausted the iterator' },
      { label: 'C', text: '`Some(&10)`, leaving the cursor where it was' },
      { label: 'D', text: '`Some(10)`, the item `peek` only inspected' },
    ],
    correctIndex: 3,
    hint: '`peek` looks ahead; `next` is what advances.',
    explanation:
      '`Peekable::peek` returns `Option<&Item>` without advancing. The following `next()` still yields that same item (`Some(10)`). A common mix-up is thinking `peek` consumes, or that `next` would keep returning a reference.',
  },
  {
    id: 'iter-rev-1',
    categorySlug: 'iterators-closures',
    title: 'Double-Ended rev',
    prompt: 'Why does `(1..=3).rev()` produce `[3, 2, 1]`?',
    tags: ['iterators', 'rev'],
    difficulty: 2,
    language: 'rust',
    code: `fn main() {
    let v: Vec<_> = (1..=3).rev().collect();
    assert_eq!(v, vec![3, 2, 1]);
}`,
    options: [
      { label: 'A', text: 'Every iterator can reverse by buffering in a `Vec`' },
      { label: 'B', text: '`RangeInclusive` implements `DoubleEndedIterator`' },
      { label: 'C', text: '`rev` always walks the range twice to count first' },
      { label: 'D', text: 'Inclusive ranges are stored backwards by rustc' },
    ],
    correctIndex: 1,
    hint: '`rev` needs a back as well as a front.',
    explanation:
      '`Iterator::rev` requires `DoubleEndedIterator` so it can pull from the back. `RangeInclusive<i32>` implements that, yielding 3, then 2, then 1, without collecting first. Many iterators (e.g. most `Map` over a one-way source) are not double-ended and cannot `.rev()`.',
  },
  {
    id: 'iter-by-ref-partial-consumption-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator by_ref Adapter',
    prompt: 'What is printed by this program?',
    tags: ['iterators', 'by-ref', 'take'],
    difficulty: 2,
    language: 'rust',
    code: `fn main() {
    let mut numbers = 1..=6;
    let first_two: Vec<_> = numbers.by_ref().take(2).collect();
    let next_val = numbers.next();
    println!("{:?}, {:?}", first_two, next_val);
}`,
    options: [
      { label: 'A', text: '[1, 2], Some(3) because by_ref borrows the iterator' },
      { label: 'B', text: '[1, 2], None because take(2) consumes base iterator' },
      { label: 'C', text: '[1, 2], Some(1) because by_ref restarts the sequence' },
      { label: 'D', text: 'Compile error because numbers is moved into take(2)' },
    ],
    correctIndex: 0,
    hint: 'What does by_ref do to avoid consuming the iterator by value?',
    explanation:
      '`Iterator::by_ref` creates an adapter that borrows the underlying iterator mutably (`&mut I`). When `take(2).collect()` runs, it advances `numbers` by two elements and drops the `take` adapter without consuming `numbers` by value. Subsequent calls to `numbers.next()` resume directly at the third element (`Some(3)`).',
  },
  {
    id: 'iter-closure-fn-cell-capture-1',
    categorySlug: 'iterators-closures',
    title: 'Closure Fn via Interior Mutability',
    prompt: 'Why does the closure `bump` implement `Fn` instead of only `FnMut`?',
    tags: ['closures', 'fn', 'cell'],
    difficulty: 2,
    language: 'rust',
    code: `use std::cell::Cell;

fn call_twice<F: Fn()>(f: F) {
    f();
    f();
}

fn main() {
    let counter = Cell::new(0);
    let bump = || counter.set(counter.get() + 1);
    call_twice(bump);
    println!("{}", counter.get());
}`,
    options: [
      { label: 'A', text: 'Closures with trivial body expressions always implement Fn' },
      { label: 'B', text: 'It captures &Cell, mutating state by interior mutability' },
      { label: 'C', text: 'The compiler promotes all FnMut closures to Fn in main()' },
      { label: 'D', text: 'Cell instances force closure environments to copy by value' },
    ],
    correctIndex: 1,
    hint: 'How is the Cell reference captured in the closure environment?',
    explanation:
      "The closure trait implemented by a closure depends on how it accesses its captured variables. Here, `bump` captures `counter` by shared reference (`&Cell<i32>`). Because `Cell::set` operates on `&self` via interior mutability, the closure's call operator only requires `&self` (`&bump`), allowing it to implement `Fn`.",
  },
  {
    id: 'iter-inspect-laziness-side-effect-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator inspect Laziness',
    prompt: 'What value is printed when this program runs?',
    tags: ['iterators', 'laziness', 'inspect'],
    difficulty: 2,
    language: 'rust',
    code: `fn main() {
    let mut count = 0;
    let iter = vec![1, 2, 3].into_iter().inspect(|_| {
        count += 1;
    });
    let _ = iter;
    println!("{count}");
}`,
    options: [
      { label: 'A', text: '3 because into_iter eagerly drives all iterator elements' },
      { label: 'B', text: '1 because inspect executes closure on the first item only' },
      { label: 'C', text: 'Compile error because inspect closures cannot mutate state' },
      { label: 'D', text: '0 because iterator adaptors are lazy and require consumers' },
    ],
    correctIndex: 3,
    hint: 'Do iterator adapters perform work before elements are pulled?',
    explanation:
      'Iterators in Rust are completely lazy. Adapter methods like `inspect`, `map`, and `filter` do not perform any work until a consuming method (like `next()`, `collect()`, or `for_each()`) actively pulls elements through the iterator chain. Because `iter` is dropped without being polled, `count` remains 0.',
  },
  
  {
    id: "iter-supporter-1",
    categorySlug: "iterators-closures",
    title: "Iterator::by_ref Adapter",
    prompt: "How does `iter.by_ref()` allow partially consuming an iterator without losing ownership?",
    tags: ["iterators-closures","by-ref","adapters"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut iter = 1..=10;\n    let head: Vec<_> = iter.by_ref().take(3).collect();\n    assert_eq!(head, vec![1, 2, 3]);\n    assert_eq!(iter.next(), Some(4));\n}",
    options: [
      { label: 'A', text: "It creates a mutable reference adapter `&mut I` that implements `Iterator`" },
      { label: 'B', text: "It duplicates the iterator state onto the thread-local stack memory buffer" },
      { label: 'C', text: "It converts the iterator into a clone-on-write Cow buffer during execution" },
      { label: 'D', text: "It freezes the iterator until all borrowed elements are dropped in scope" },
    ],
    correctIndex: 3,
    hint: "by_ref borrows the iterator mutably (&mut iter) so adapters like take() do not consume the iterator.",
    explanation: "`Iterator::by_ref` borrows the iterator mutably as `&mut I` (which also implements `Iterator`). When consuming adapters like `.take(3)` drop, the underlying iterator `iter` remains valid and ready for further items.",
  },
  {
    id: "iter-supporter-2",
    categorySlug: "iterators-closures",
    title: "Rust 2021/2024 Disjoint Closure Capture",
    prompt: "Why can two closures capture disjoint fields of the same struct simultaneously in Rust 2024?",
    tags: ["iterators-closures","disjoint-capture","closures"],
    difficulty: 2,
    language: 'rust',
    code: "struct State {\n    a: i32,\n    b: String,\n}\n\nfn main() {\n    let mut s = State { a: 1, b: String::from(\"hi\") };\n    let mut c1 = || s.a += 1;\n    let mut c2 = || s.b.push_str(\"!\");\n    c1();\n    c2();\n    println!(\"{} {}\", s.a, s.b);\n}",
    options: [
      { label: 'A', text: "Closures capture individual struct fields rather than the entire enclosing struct" },
      { label: 'B', text: "Closures clone all captured fields into heap memory automatically within local thread memory" },
      { label: 'C', text: "The compiler demotes closure calls into atomic compare-and-swap loops in runtime memory" },
      { label: 'D', text: "Field mutation inside closures is deferred until the main block exits in runtime memory" },
    ],
    correctIndex: 0,
    hint: "Since Rust 2021, closures only capture the specific fields they touch.",
    explanation: "Starting in Rust 2021 and continuing in 2024, closures capture precise field paths (`s.a`, `s.b`) rather than the entire struct `s`, enabling independent simultaneous borrows of disjoint struct fields.",
  },
  {
    id: "iter-supporter-3",
    categorySlug: "iterators-closures",
    title: "DoubleEndedIterator::next_back",
    prompt: "What does `iter.next_back()` do on a `DoubleEndedIterator`?",
    tags: ["iterators-closures","double-ended","rev"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut iter = [10, 20, 30].into_iter();\n    assert_eq!(iter.next_back(), Some(30));\n    assert_eq!(iter.next(), Some(10));\n    assert_eq!(iter.next_back(), Some(20));\n    assert_eq!(iter.next(), None);\n}",
    options: [
      { label: 'A', text: "Reverses the internal array order in memory before returning in runtime memory" },
      { label: 'B', text: "Yields the next element from the end of the collection towards the front" },
      { label: 'C', text: "Returns the last element without advancing the iterator cursor in runtime memory" },
      { label: 'D', text: "Panics if called after `next()` has already been called within local thread memory" },
    ],
    correctIndex: 1,
    hint: "DoubleEndedIterator can produce elements from either end until meeting in the middle.",
    explanation: "`DoubleEndedIterator::next_back()` removes and returns an element from the rear of the iterator. Elements can be drawn from either end until the iteration converges and yields `None`.",
  },
  {
    id: "iter-supporter-4",
    categorySlug: "iterators-closures",
    title: "ExactSizeIterator::len vs Iterator::size_hint",
    prompt: "What guarantee does `ExactSizeIterator` provide over `size_hint()`?",
    tags: ["iterators-closures","exact-size","size-hint"],
    difficulty: 2,
    language: 'rust',
    code: "fn print_len<I: ExactSizeIterator>(iter: I) {\n    println!(\"Exact remaining elements: {}\", iter.len());\n}\n\nfn main() {\n    let v = vec![1, 2, 3];\n    print_len(v.into_iter());\n}",
    options: [
      { label: 'A', text: "`len()` is evaluated at compile time as a constant expression in runtime memory" },
      { label: 'B', text: "The collection is guaranteed to fit within a single CPU cache line in code" },
      { label: 'C', text: "`iter.len()` is guaranteed to be exact and equal to the remaining items" },
      { label: 'D', text: "Elements can be accessed by numerical index in O(1) time in runtime memory" },
    ],
    correctIndex: 2,
    hint: "ExactSizeIterator guarantees that size_hint().0 == size_hint().1 and provides .len().",
    explanation: "`ExactSizeIterator` guarantees that `iter.len()` accurately reports the exact number of elements remaining in the iteration (`(len, Some(len))`), allowing callers to preallocate exact capacity.",
  },
  {
    id: "iter-supporter-5",
    categorySlug: "iterators-closures",
    title: "Iterator Laziness and Side Effects",
    prompt: "Why does `vec.iter().map(|x| println!(\"{x}\"));` print nothing when executed alone?",
    tags: ["iterators-closures","laziness","side-effects"],
    difficulty: 1,
    language: 'rust',
    code: "fn main() {\n    let numbers = vec![1, 2, 3];\n    let _ = numbers.iter().map(|x| println!(\"{x}\"));\n    // Nothing is printed!\n}",
    options: [
      { label: 'A', text: "The compiler elides printing calls in release optimization mode in runtime memory" },
      { label: 'B', text: "Println statements are buffered and output only on program termination in code" },
      { label: 'C', text: "Vector iterators cannot capture output formatting arguments in runtime memory" },
      { label: 'D', text: "Iterator adapters are lazy and do not execute until driven by a consumer" },
    ],
    correctIndex: 3,
    hint: "Iterator adapters like map, filter, and inspect do nothing until next() or collect() is called.",
    explanation: "Rust iterators are completely lazy: calling `.map(...)` simply constructs an iterator adapter struct. No closure code executes until the iterator is driven by calling `.next()`, `.collect()`, or a `for` loop.",
  },
  {
    id: "iter-supporter-6",
    categorySlug: "iterators-closures",
    title: "Iterator::fold vs Iterator::reduce",
    prompt: "How does `reduce(f)` differ from `fold(init, f)`?",
    tags: ["iterators-closures","fold","reduce"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let empty: Vec<i32> = vec![];\n    let res = empty.into_iter().reduce(|a, b| a + b);\n    assert_eq!(res, None);\n}",
    options: [
      { label: 'A', text: "`reduce` uses the first element as initial accumulator and returns `Option<T>`" },
      { label: 'B', text: "`reduce` executes across multiple background worker threads concurrently in code" },
      { label: 'C', text: "`fold` requires all elements to implement the `Copy` marker trait in runtime memory" },
      { label: 'D', text: "`reduce` only works on non-negative integer primitives within local thread memory" },
    ],
    correctIndex: 0,
    hint: "reduce uses the first element as accumulator and returns None on empty iterators.",
    explanation: "`Iterator::reduce` uses the first element of the iterator as the initial accumulator value and returns `Option<T>` (yielding `None` if the iterator is empty), whereas `fold` takes an explicit initial value.",
  },
  {
    id: "iter-supporter-7",
    categorySlug: "iterators-closures",
    title: "Iterator::try_fold Early Exit",
    prompt: "What is the primary advantage of `try_fold` over standard `fold`?",
    tags: ["iterators-closures","try-fold","short-circuit"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![1, 2, -3, 4];\n    let res = nums.into_iter().try_fold(0, |acc, x| {\n        if x < 0 { Err(\"negative\") } else { Ok(acc + x) }\n    });\n    assert_eq!(res, Err(\"negative\"));\n}",
    options: [
      { label: 'A', text: "Catches thread panics and converts them into Result values in runtime memory" },
      { label: 'B', text: "Short-circuits immediately upon encountering an error (`Err` or `None`)" },
      { label: 'C', text: "Executes closure operations in parallel across Rayon pools in runtime memory" },
      { label: 'D', text: "Allocates the accumulator variable on heap page memory within local thread memory" },
    ],
    correctIndex: 1,
    hint: "try_fold short-circuits early when the closure returns Err or Break.",
    explanation: "`Iterator::try_fold` is the foundational primitive for all fallible/short-circuiting iterator consumers. If the accumulator closure returns `Err(e)` or `Break(b)`, `try_fold` stops immediately without processing remaining items.",
  },
  {
    id: "iter-supporter-8",
    categorySlug: "iterators-closures",
    title: "FnMut Closure Mutation",
    prompt: "Why does calling an `FnMut` closure require the closure variable to be declared `mut`?",
    tags: ["iterators-closures","fn-mut","closures"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut count = 0;\n    let mut inc = || { count += 1; count };\n    assert_eq!(inc(), 1);\n    assert_eq!(inc(), 2);\n}",
    options: [
      { label: 'A', text: "Closures are allocated on the heap and require mutable write locks in runtime memory" },
      { label: 'B', text: "The compiler must allocate a new stack frame on each invocation in runtime memory" },
      { label: 'C', text: "Invoking `FnMut::call_mut(&mut self)` requires an exclusive mutable reference" },
      { label: 'D', text: "Variables captured inside closures lose their type information in runtime memory" },
    ],
    correctIndex: 2,
    hint: "FnMut is defined as fn call_mut(&mut self, ...) which needs a mutable binding.",
    explanation: "`FnMut` defines `extern \"rust-call\" fn call_mut(&mut self, ...)`. Because the call takes `&mut self` to modify its captured environment, the binding `inc` must be declared with `let mut`.",
  },
  {
    id: "iter-supporter-9",
    categorySlug: "iterators-closures",
    title: "Iterator::scan Stateful Adapter",
    prompt: "How does `Iterator::scan` maintain internal state across iteration steps?",
    tags: ["iterators-closures","scan","stateful"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![1, 2, 3, 4];\n    let cumulative: Vec<i32> = nums.into_iter()\n        .scan(0, |state, x| {\n            *state += x;\n            Some(*state)\n        })\n        .collect();\n    assert_eq!(cumulative, vec![1, 3, 6, 10]);\n}",
    options: [
      { label: 'A', text: "Stores state inside an atomic thread-local variable within local thread memory" },
      { label: 'B', text: "Clones the state object for every element in the collection in runtime memory" },
      { label: 'C', text: "Delegates state persistence to the operating system memory in runtime memory" },
      { label: 'D', text: "Passes a mutable reference `&mut State` to the closure on each step" },
    ],
    correctIndex: 3,
    hint: "scan passes &mut State and returns Option<B>, stopping early if None is returned.",
    explanation: "`Iterator::scan(initial_state, f)` holds a state value and passes `&mut state` to the closure `f` on each iteration. The closure returns `Option<B>`, allowing stateful transformations and early termination.",
  },
  {
    id: "iter-supporter-10",
    categorySlug: "iterators-closures",
    title: "Iterator::partition Splitting",
    prompt: "What does `iter.partition(predicate)` produce from an iterator?",
    tags: ["iterators-closures","partition","collections"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let numbers = vec![1, 2, 3, 4, 5, 6];\n    let (even, odd): (Vec<i32>, Vec<i32>) = numbers.into_iter().partition(|&n| n % 2 == 0);\n    assert_eq!(even, vec![2, 4, 6]);\n    assert_eq!(odd, vec![1, 3, 5]);\n}",
    options: [
      { label: 'A', text: "A pair of collections `(TrueCollection, FalseCollection)` split by the predicate" },
      { label: 'B', text: "A single interleaved vector containing even indices first within local thread memory" },
      { label: 'C', text: "A HashMap mapping boolean keys to collection slices under current compiler safety rules" },
      { label: 'D', text: "Two asynchronous stream handles running on background threads within local thread memory" },
    ],
    correctIndex: 0,
    hint: "partition splits an iterator into two collections based on a boolean predicate.",
    explanation: "`Iterator::partition(predicate)` consumes the iterator and collects elements into a tuple `(A, B)` of collections: `A` receives elements where the predicate returned `true`, and `B` receives `false`.",
  },
  {
    id: "iter-supporter-11",
    categorySlug: "iterators-closures",
    title: "FnOnce Cannot Be Called Twice",
    prompt: "Why does calling an `FnOnce` closure a second time produce a compile-time error?",
    tags: ["iterators-closures","fn-once","ownership"],
    difficulty: 2,
    language: 'rust',
    code: "fn call_twice<F: FnOnce()>(f: F) {\n    f();\n    // f(); // compile error: use of moved value\n}",
    options: [
      { label: 'A', text: "The closure destructor runs in a separate thread asynchronously in code" },
      { label: 'B', text: "`FnOnce::call_once(self)` consumes the closure by value on invocation" },
      { label: 'C', text: "The compiler zero-fills the closure memory after first execution in code" },
      { label: 'D', text: "Calling closures more than once is restricted to unsafe code in runtime memory" },
    ],
    correctIndex: 1,
    hint: "FnOnce takes self by value, consuming ownership of captured variables.",
    explanation: "`FnOnce` defines `fn call_once(self, ...) -> Self::Output`. Because it takes `self` by value, the closure (and any owned resources it captured) is consumed on the first call and cannot be called again.",
  },
  {
    id: "iter-supporter-12",
    categorySlug: "iterators-closures",
    title: "Iterator::peekable and Peek Mutability",
    prompt: "Can you mutably modify the peeked value via `peek_mut()` on `Peekable`?",
    tags: ["iterators-closures","peekable","peek_mut"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut iter = vec![1, 2, 3].into_iter().peekable();\n    if let Some(val) = iter.peek_mut() {\n        *val = 42;\n    }\n    assert_eq!(iter.next(), Some(42));\n}",
    options: [
      { label: 'A', text: "No, peekable values are strictly read-only immutable references in runtime memory" },
      { label: 'B', text: "Only if the collection elements implement the `Copy` trait in runtime memory" },
      { label: 'C', text: "Yes, `peek_mut()` returns `Option<&mut Item>` allowing in-place mutation" },
      { label: 'D', text: "Only inside unsafe blocks with raw pointer dereferencing within local thread memory" },
    ],
    correctIndex: 2,
    hint: "peek_mut() yields a mutable reference &mut T to the buffered next element.",
    explanation: "`Peekable::peek_mut` returns `Option<&mut I::Item>`, enabling callers to inspect and modify the buffered next item in place before extracting it with `next()`.",
  },
  {
    id: "iter-supporter-13",
    categorySlug: "iterators-closures",
    title: "Iterator::zip Shortest Length Termination",
    prompt: "When does `iter_a.zip(iter_b)` terminate?",
    tags: ["iterators-closures","zip","termination"],
    difficulty: 1,
    language: 'rust',
    code: "fn main() {\n    let a = [1, 2, 3, 4];\n    let b = [\"a\", \"b\"];\n    let pairs: Vec<_> = a.into_iter().zip(b).collect();\n    assert_eq!(pairs.len(), 2);\n}",
    options: [
      { label: 'A', text: "Only after both iterators have completely returned `None` in runtime memory" },
      { label: 'B', text: "When the longer iterator runs out of buffer memory within local thread memory" },
      { label: 'C', text: "After exactly 1024 iterations in debug mode during runtime execution" },
      { label: 'D', text: "As soon as either iterator returns `None` (shorter iterator length)" },
    ],
    correctIndex: 3,
    hint: "zip stops yielding as soon as the first iterator yields None.",
    explanation: "`Iterator::zip` pairs elements until either underlying iterator yields `None`, terminating at the length of the shorter iterator.",
  },
  {
    id: "iter-supporter-14",
    categorySlug: "iterators-closures",
    title: "Iterator::fuse Guarantee",
    prompt: "What does `iter.fuse()` guarantee about calls to `next()` after the first `None`?",
    tags: ["iterators-closures","fuse","fused-iterator"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut iter = (1..=2).fuse();\n    assert_eq!(iter.next(), Some(1));\n    assert_eq!(iter.next(), Some(2));\n    assert_eq!(iter.next(), None);\n    assert_eq!(iter.next(), None); // Guaranteed to stay None forever\n}",
    options: [
      { label: 'A', text: "Guarantees that all subsequent calls to `next()` will continue returning `None`" },
      { label: 'B', text: "Re-evaluates the iterator from the beginning upon reaching `None` in runtime memory" },
      { label: 'C', text: "Panics if `next()` is called after `None` has been returned within local thread memory" },
      { label: 'D', text: "Merges multiple iterator streams into an asynchronous channel within local thread memory" },
    ],
    correctIndex: 0,
    hint: "fuse creates an iterator that continues to return None forever once None is yielded.",
    explanation: "Standard `Iterator` contracts do not specify behavior if `next()` is called after returning `None`. `fuse()` wraps the iterator so that once `None` is yielded, all future `next()` calls are guaranteed to return `None`.",
  },
  {
    id: "iter-supporter-15",
    categorySlug: "iterators-closures",
    title: "FromIterator for Result (Collection of Results)",
    prompt: "What does `items.into_iter().collect::<Result<Vec<T>, E>>()` do on error?",
    tags: ["iterators-closures","collect","result-transposition"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let strings = vec![\"1\", \"two\", \"3\"];\n    let parsed: Result<Vec<i32>, _> = strings.into_iter().map(|s| s.parse::<i32>()).collect();\n    assert!(parsed.is_err());\n}",
    options: [
      { label: 'A', text: "Collects all successful elements, discarding any errors silently in code" },
      { label: 'B', text: "Short-circuits on the first `Err` and returns that `Err` immediately" },
      { label: 'C', text: "Panics with an unwrap error when the first failure occurs in runtime memory" },
      { label: 'D', text: "Accumulates all errors into a composite multi-error vector in runtime memory" },
    ],
    correctIndex: 1,
    hint: "collecting an Iterator of Results into a Result of Collection stops at the first Err.",
    explanation: "`FromIterator` for `Result<V, E>` collects an iterator of `Result<T, E>`: if all are `Ok(x)`, it returns `Ok(Vec<T>)`; on the first `Err(e)`, iteration stops and `Err(e)` is returned immediately.",
  },
  {
    id: "iter-supporter-16",
    categorySlug: "iterators-closures",
    title: "Iterator::flat_map vs map",
    prompt: "How does `flat_map` differ from calling `map` on an iterator?",
    tags: ["iterators-closures","flat-map","combinators"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let words = vec![\"hi\", \"world\"];\n    let chars: Vec<char> = words.into_iter().flat_map(|w| w.chars()).collect();\n    assert_eq!(chars, vec!['h', 'i', 'w', 'o', 'r', 'l', 'd']);\n}",
    options: [
      { label: 'A', text: "`flat_map` executes the mapping closure concurrently in Rayon within local thread memory" },
      { label: 'B', text: "`flat_map` removes duplicate items from the resulting collection in runtime memory" },
      { label: 'C', text: "`flat_map` flattens each yielded sub-iterator into a continuous single stream" },
      { label: 'D', text: "`flat_map` converts all yielded elements into static slice views in runtime memory" },
    ],
    correctIndex: 2,
    hint: "flat_map is equivalent to .map(f).flatten().",
    explanation: "`Iterator::flat_map(f)` maps each item to an `IntoIterator` and flattens the resulting iterators into a single linear sequence.",
  },
  {
    id: "iter-supporter-17",
    categorySlug: "iterators-closures",
    title: "Closure Coercion to Function Pointer",
    prompt: "When can a closure be coerced to a plain function pointer `fn(T) -> U`?",
    tags: ["iterators-closures","fn-pointer","coercion"],
    difficulty: 2,
    language: 'rust',
    code: "fn apply(f: fn(i32) -> i32, x: i32) -> i32 { f(x) }\n\nfn main() {\n    let c = |x| x * 2;\n    assert_eq!(apply(c, 5), 10);\n}",
    options: [
      { label: 'A', text: "Whenever the closure is annotated with the `#[inline]` attribute in code" },
      { label: 'B', text: "Only when the closure argument and return types implement `Copy` in code" },
      { label: 'C', text: "Any closure can be converted to a function pointer via transmutation in code" },
      { label: 'D', text: "Only when the closure captures no variables from its outer environment" },
    ],
    correctIndex: 3,
    hint: "Non-capturing closures have a zero-sized environment and can coerce to fn pointers.",
    explanation: "A closure that captures no variables from its environment has an empty environment struct (zero-sized) and can be implicitly coerced to a plain function pointer `fn(...) -> ...`.",
  },
  {
    id: "iter-supporter-18",
    categorySlug: "iterators-closures",
    title: "Iterator::take_while vs filter",
    prompt: "How does `take_while` differ from `filter`?",
    tags: ["iterators-closures","take-while","filter"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![1, 2, 5, 2, 1];\n    let taken: Vec<_> = nums.into_iter().take_while(|&x| x < 4).collect();\n    assert_eq!(taken, vec![1, 2]);\n}",
    options: [
      { label: 'A', text: "`take_while` terminates the iterator completely at the first `false` item" },
      { label: 'B', text: "`take_while` evaluates the predicate across all items in parallel in runtime memory" },
      { label: 'C', text: "`filter` stops iteration immediately when `false` is encountered in runtime memory" },
      { label: 'D', text: "`take_while` collects items into an array of fixed capacity in runtime memory" },
    ],
    correctIndex: 0,
    hint: "take_while stops at the first false; filter checks every item in the iterator.",
    explanation: "`take_while` yields items only until the predicate returns `false` for the first time, at which point it terminates. `filter` tests every item in the iterator and keeps all matching ones.",
  },
  {
    id: "iter-supporter-19",
    categorySlug: "iterators-closures",
    title: "Iterator::cycle Infinite Iteration",
    prompt: "What happens when an iterator produced by `iter.cycle()` reaches the end?",
    tags: ["iterators-closures","cycle","infinite"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut cycle = vec![1, 2].into_iter().cycle();\n    assert_eq!(cycle.next(), Some(1));\n    assert_eq!(cycle.next(), Some(2));\n    assert_eq!(cycle.next(), Some(1));\n}",
    options: [
      { label: 'A', text: "Reverses the sequence direction and iterates backwards within local thread memory" },
      { label: 'B', text: "Restarts iteration from the beginning repeatedly, iterating infinitely" },
      { label: 'C', text: "Returns `None` after exactly two full cycles during runtime execution in code" },
      { label: 'D', text: "Panics with a circular reference error on loop completion in runtime memory" },
    ],
    correctIndex: 1,
    hint: "cycle repeats the iterator endlessly by cloning the initial iterator state (requires Clone).",
    explanation: "`Iterator::cycle` repeats the elements endlessly: when the end is reached, it starts again from the beginning (requires `Self: Clone`).",
  },
  {
    id: "iter-supporter-20",
    categorySlug: "iterators-closures",
    title: "Iterator::chain Sequence Concatenation",
    prompt: "What does `iter_a.chain(iter_b)` do?",
    tags: ["iterators-closures","chain","adapters"],
    difficulty: 1,
    language: 'rust',
    code: "fn main() {\n    let a = vec![1, 2];\n    let b = vec![3, 4];\n    let chained: Vec<_> = a.into_iter().chain(b).collect();\n    assert_eq!(chained, vec![1, 2, 3, 4]);\n}",
    options: [
      { label: 'A', text: "Pairs corresponding elements into tuples like `zip` under current compiler safety rules" },
      { label: 'B', text: "Executes both iterators in parallel threads concurrently within local thread memory" },
      { label: 'C', text: "Yields all elements from `iter_a`, and then yields all elements from `iter_b`" },
      { label: 'D', text: "Interleaves elements alternately one by one from each iterator in runtime memory" },
    ],
    correctIndex: 2,
    hint: "chain concatenates two iterators sequentially.",
    explanation: "`Iterator::chain` yields elements from `iter_a` until exhaustion, and then seamlessly proceeds to yield elements from `iter_b`.",
  },
  {
    id: "iter-supporter-21",
    categorySlug: "iterators-closures",
    title: "Closure Struct Size and Captures",
    prompt: "What determines `std::mem::size_of_val(&closure)` for a closure instance?",
    tags: ["iterators-closures","closures","memory-layout"],
    difficulty: 3,
    language: 'rust',
    code: "fn main() {\n    let a: u64 = 1;\n    let b: u64 = 2;\n    let c = move || a + b;\n    assert_eq!(std::mem::size_of_val(&c), 16);\n}",
    options: [
      { label: 'A', text: "A fixed size of 16 bytes representing fat pointer vtables within local thread memory" },
      { label: 'B', text: "The size of the compiled machine code instructions in RAM within local thread memory" },
      { label: 'C', text: "Zero bytes because closures are evaluated purely at compile time in runtime memory" },
      { label: 'D', text: "The combined memory sizes and alignment padding of all captured variables" },
    ],
    correctIndex: 3,
    hint: "Under the hood, a closure is an anonymous compiler-generated struct holding its captured fields.",
    explanation: "In Rust, a closure is desugared into an anonymous struct containing all captured variables (either as references or by-value fields). Its size is the struct layout size of those captured fields.",
  },
  {
    id: "iter-supporter-22",
    categorySlug: "iterators-closures",
    title: "Iterator::enumerate Counter Type",
    prompt: "What type is used for the index in `iter.enumerate()`?",
    tags: ["iterators-closures","enumerate","types"],
    difficulty: 1,
    language: 'rust',
    code: "fn main() {\n    for (i, val) in [\"a\", \"b\"].into_iter().enumerate() {\n        let _idx: usize = i;\n        let _ = val;\n    }\n}",
    options: [
      { label: 'A', text: "The pointer-width integer `usize`" },
      { label: 'B', text: "The 32-bit unsigned integer `u32`" },
      { label: 'C', text: "The 64-bit unsigned integer `u64`" },
      { label: 'D', text: "The pointer-width integer `isize`" },
    ],
    correctIndex: 0,
    hint: "enumerate yields (usize, Item) tuples starting from 0.",
    explanation: "`Iterator::enumerate` yields `(usize, Item)` pairs, with the zero-based index typed as `usize` matching pointer widths.",
  },
  {
    id: "iter-supporter-23",
    categorySlug: "iterators-closures",
    title: "Iterator::position vs find",
    prompt: "What does `iter.position(predicate)` return?",
    tags: ["iterators-closures","position","searching"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![10, 20, 30, 40];\n    let pos = nums.into_iter().position(|x| x == 30);\n    assert_eq!(pos, Some(2));\n}",
    options: [
      { label: 'A', text: "`Option<T>` containing a reference to the matched element within local thread memory" },
      { label: 'B', text: "`Option<usize>` containing the 0-based index of the first matching element" },
      { label: 'C', text: "`bool` indicating whether the element was found during runtime execution in code" },
      { label: 'D', text: "A raw memory pointer to the element in the heap during runtime execution in code" },
    ],
    correctIndex: 1,
    hint: "position returns Some(index) of the first element satisfying the predicate.",
    explanation: "`Iterator::position` searches from left to right and returns `Some(index)` (`Option<usize>`) for the first element where `predicate` returns `true`, or `None` if not found.",
  },
  {
    id: "iter-supporter-24",
    categorySlug: "iterators-closures",
    title: "Iterator::rposition in DoubleEndedIterator",
    prompt: "What does `iter.rposition(predicate)` do on an `ExactSizeIterator + DoubleEndedIterator`?",
    tags: ["iterators-closures","rposition","search"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![1, 2, 3, 2, 1];\n    let pos = nums.into_iter().rposition(|x| x == 2);\n    assert_eq!(pos, Some(3));\n}",
    options: [
      { label: 'A', text: "Returns the index counted backwards starting with 0 at the end in runtime memory" },
      { label: 'B', text: "Reverses the vector and returns the first matching element value in runtime memory" },
      { label: 'C', text: "Searches from right to left and returns the 0-based index from the start" },
      { label: 'D', text: "Panics if multiple matching elements exist in the collection in runtime memory" },
    ],
    correctIndex: 2,
    hint: "rposition searches from right to left, returning the 0-based index from the left.",
    explanation: "`Iterator::rposition` searches from right to left, returning `Some(index)` (`usize`) representing the index from the front of the collection.",
  },
  {
    id: "iter-supporter-25",
    categorySlug: "iterators-closures",
    title: "Iterator::skip_while Behavior",
    prompt: "What does `iter.skip_while(predicate)` do once the predicate returns `false`?",
    tags: ["iterators-closures","skip-while","combinators"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![1, 2, 5, 2, 1];\n    let skipped: Vec<_> = nums.into_iter().skip_while(|&x| x < 4).collect();\n    assert_eq!(skipped, vec![5, 2, 1]);\n}",
    options: [
      { label: 'A', text: "Terminates iteration and immediately returns `None` under current compiler safety rules" },
      { label: 'B', text: "Continues evaluating the predicate on subsequent elements like `filter` in runtime memory" },
      { label: 'C', text: "Reverses the collection order and searches from the rear under current compiler safety rules" },
      { label: 'D', text: "Yields that element and all remaining elements without checking the predicate again" },
    ],
    correctIndex: 3,
    hint: "skip_while ignores items until the first false, then yields all remaining items unchecked.",
    explanation: "`Iterator::skip_while` ignores items as long as `predicate` is `true`. As soon as it encounters the first `false`, it yields that element and all subsequent elements without ever calling the predicate again.",
  },
  {
    id: "iter-supporter-26",
    categorySlug: "iterators-closures",
    title: "Iterator::step_by Stride",
    prompt: "What happens if `step_by(0)` is called on an iterator?",
    tags: ["iterators-closures","step-by","panics"],
    difficulty: 2,
    language: 'rust',
    code: "// let _ = (0..10).step_by(0); // Panics!",
    options: [
      { label: 'A', text: "Panics immediately because step size must be non-zero" },
      { label: 'B', text: "Yields the first element infinitely in a loop during runtime execution" },
      { label: 'C', text: "Returns an empty iterator yielding `None` during runtime execution" },
      { label: 'D', text: "Defaults to a step size of 1 automatically during runtime execution" },
    ],
    correctIndex: 0,
    hint: "step_by(step) requires step > 0 and panics if step == 0.",
    explanation: "`Iterator::step_by(step)` requires `step >= 1`. Calling `step_by(0)` panics with an assertion error because a step size of 0 would loop infinitely on the first element.",
  },
  {
    id: "iter-supporter-27",
    categorySlug: "iterators-closures",
    title: "Iterator::rfold Reverse Accumulation",
    prompt: "How does `iter.rfold(init, f)` process elements compared to `fold`?",
    tags: ["iterators-closures","intersperse","adapters"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let numbers = vec![1, 2, 3];\n    let res = numbers.into_iter().rfold(0, |acc, x| acc * 10 + x);\n    assert_eq!(res, 321);\n}",
    options: [
      { label: 'A', text: "Executes accumulation concurrently across worker threads in code" },
      { label: 'B', text: "Processes elements from right to left starting from the end" },
      { label: 'C', text: "Reverses the internal heap allocation buffer in place in code" },
      { label: 'D', text: "Short-circuits immediately when a zero value is found in code" },
    ],
    correctIndex: 1,
    hint: "intersperse places a separator clone between every pair of elements.",
    explanation: "`Iterator::rfold` operates on `DoubleEndedIterator` by consuming elements in reverse order (from back to front).",
  },
  {
    id: "iter-supporter-28",
    categorySlug: "iterators-closures",
    title: "Extend Trait on Collections",
    prompt: "What does the `Extend` trait do when implemented on collections?",
    tags: ["iterators-closures","extend","collections"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut v = vec![1, 2];\n    v.extend([3, 4]);\n    assert_eq!(v, vec![1, 2, 3, 4]);\n}",
    options: [
      { label: 'A', text: "Reallocates the collection in thread-local storage within local thread memory" },
      { label: 'B', text: "Extends the lifetime of references stored in the collection in runtime memory" },
      { label: 'C', text: "Appends all elements from an `IntoIterator` into the existing collection" },
      { label: 'D', text: "Merges two collections by sorting their combined elements in runtime memory" },
    ],
    correctIndex: 2,
    hint: "Extend::extend consumes an iterator and inserts its elements into self.",
    explanation: "`std::iter::Extend` defines `fn extend<T: IntoIterator>(&mut self, iter: T)`. It extends an existing collection by iterating through `iter` and appending each element.",
  },
  {
    id: "iter-supporter-29",
    categorySlug: "iterators-closures",
    title: "Iterator::all vs Iterator::any Short-Circuiting",
    prompt: "When does `iter.all(predicate)` short-circuit?",
    tags: ["iterators-closures","all","short-circuit"],
    difficulty: 1,
    language: 'rust',
    code: "fn main() {\n    let nums = vec![2, 4, 5, 6];\n    assert!(!nums.into_iter().all(|x| x % 2 == 0));\n}",
    options: [
      { label: 'A', text: "Only after inspecting all elements in the entire collection" },
      { label: 'B', text: "As soon as the predicate returns `true` for any element in code" },
      { label: 'C', text: "When the iterator produces a `None` terminal value in code" },
      { label: 'D', text: "As soon as the predicate returns `false` for any element" },
    ],
    correctIndex: 3,
    hint: "all stops immediately as soon as a false is encountered, returning false.",
    explanation: "`Iterator::all` short-circuits immediately upon the first element for which `predicate` returns `false`, returning `false` without inspecting the rest.",
  },
  {
    id: "iter-supporter-30",
    categorySlug: "iterators-closures",
    title: "IntoIterator for Slices (&[T] and &mut [T])",
    prompt: "What element types are yielded when iterating `(&slice).into_iter()` vs `(&mut slice).into_iter()`?",
    tags: ["iterators-closures","slices","into-iterator"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let mut arr = [1, 2];\n    for _x in (&arr).into_iter() {}     // yields &i32\n    for _x in (&mut arr).into_iter() {} // yields &mut i32\n}",
    options: [
      { label: 'A', text: "`&T` for shared slices and `&mut T` for mutable slices" },
      { label: 'B', text: "Owned `T` values for both slice variations during runtime execution" },
      { label: 'C', text: "`Option<&T>` and `Option<&mut T>` during runtime execution" },
      { label: 'D', text: "Raw memory pointers `*const T` and `*mut T` during runtime execution" },
    ],
    correctIndex: 0,
    hint: "&[T] yields &T; &mut [T] yields &mut T.",
    explanation: "`IntoIterator` for `&[T]` yields shared references `&T`, while `IntoIterator` for `&mut [T]` yields exclusive mutable references `&mut T`.",
  },
  {
    id: "iter-supporter-31",
    categorySlug: "iterators-closures",
    title: "Iterator::unzip Tuple Split",
    prompt: "What does `iter.unzip()` do on an iterator yielding pairs `(A, B)`?",
    tags: ["iterators-closures","unzip","tuples"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let pairs = vec![(1, \"a\"), (2, \"b\")];\n    let (nums, letters): (Vec<i32>, Vec<&str>) = pairs.into_iter().unzip();\n    assert_eq!(nums, vec![1, 2]);\n    assert_eq!(letters, vec![\"a\", \"b\"]);\n}",
    options: [
      { label: 'A', text: "Decompresses a compressed binary byte stream in memory within local thread memory" },
      { label: 'B', text: "Splits an iterator of pairs into two separate collections `(ColA, ColB)`" },
      { label: 'C', text: "Interleaves two collections into a single sorted vector within local thread memory" },
      { label: 'D', text: "Converts pairs into a hash table key-value map during runtime execution in code" },
    ],
    correctIndex: 1,
    hint: "unzip converts an iterator of tuples (A, B) into a tuple of collections (Vec<A>, Vec<B>).",
    explanation: "`Iterator::unzip` takes an iterator of tuples `(A, B)` and separates them into two collections, extending the first collection with `a` and the second with `b`.",
  },
  {
    id: "iter-supporter-32",
    categorySlug: "iterators-closures",
    title: "Iterator::flatten Flattening Semantics",
    prompt: "What requirement must elements of `I` meet to use `iter.flatten()`?",
    tags: ["iterators-closures","flatten","into-iterator"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let nested = vec![vec![1, 2], vec![3, 4]];\n    let flat: Vec<i32> = nested.into_iter().flatten().collect();\n    assert_eq!(flat, vec![1, 2, 3, 4]);\n}",
    options: [
      { label: 'A', text: "All elements must implement the `Copy` marker trait in code" },
      { label: 'B', text: "The outer collection must have a fixed compile-time size" },
      { label: 'C', text: "The element type `I::Item` must implement `IntoIterator`" },
      { label: 'D', text: "Elements must be stored contiguously in physical RAM in code" },
    ],
    correctIndex: 2,
    hint: "flatten requires each element Item to implement IntoIterator (e.g. Vec, Option, array).",
    explanation: "`Iterator::flatten` requires `I::Item: IntoIterator`. It flattens one level of nested structure (e.g. `Vec<Vec<T>>` or `Vec<Option<T>>`).",
  },
  {
    id: "iter-supporter-33",
    categorySlug: "iterators-closures",
    title: "Fn Trait as Function Parameter",
    prompt: "Why is `impl Fn(i32) -> i32` preferred over `fn(i32) -> i32` for generic callbacks?",
    tags: ["iterators-closures","fn-traits","generics"],
    difficulty: 2,
    language: 'rust',
    code: "fn apply<F: Fn(i32) -> i32>(f: F, x: i32) -> i32 { f(x) }",
    options: [
      { label: 'A', text: "It forces dynamic dispatch through virtual function vtables in code" },
      { label: 'B', text: "It allocates the callback closure on the thread-local heap in code" },
      { label: 'C', text: "It allows callbacks to execute asynchronously in the background" },
      { label: 'D', text: "It accepts both capturing closures and plain function pointers" },
    ],
    correctIndex: 3,
    hint: "Generic Fn trait bounds accept capturing closures and function pointers with zero overhead.",
    explanation: "`impl Fn(...) -> ...` accepts any callable type (including closures with captured state and plain `fn` pointers), monomorphizing the call with zero heap or indirection overhead.",
  },
  {
    id: "iter-supporter-34",
    categorySlug: "iterators-closures",
    title: "Iterator::count vs len Efficiency",
    prompt: "Why is `iter.count()` O(N) while `slice.len()` is O(1)?",
    tags: ["iterators-closures","count","complexity"],
    difficulty: 2,
    language: 'rust',
    code: "fn main() {\n    let count = (0..10).filter(|x| x % 2 == 0).count();\n    assert_eq!(count, 5);\n}",
    options: [
      { label: 'A', text: "`count()` must consume and advance the entire iterator stream to count items" },
      { label: 'B', text: "`count()` performs memory allocation checks on every element within local thread memory" },
      { label: 'C', text: "`slice.len()` executes a hardware assembly trap on each read within local thread memory" },
      { label: 'D', text: "`count()` runs in a separate thread requiring sync barriers within local thread memory" },
    ],
    correctIndex: 0,
    hint: "count() consumes the iterator element by element unless specialized.",
    explanation: "`Iterator::count()` iterates through every element by repeatedly calling `next()` until exhaustion. `slice.len()` is an O(1) field read from the slice's fat pointer metadata.",
  },
  {
    id: "iter-supporter-35",
    categorySlug: "iterators-closures",
    title: "Iterator::collect Type Inference with Turbofish",
    prompt: "Why is the turbofish `collect::<Vec<_>>()` often needed when collecting iterators?",
    tags: ["iterators-closures","turbofish","type-inference"],
    difficulty: 1,
    language: 'rust',
    code: "fn main() {\n    let items = (0..5).collect::<Vec<_>>();\n    assert_eq!(items.len(), 5);\n}",
    options: [
      { label: 'A', text: "The compiler cannot deduce the size of primitive integer types in runtime memory" },
      { label: 'B', text: "`collect()` can construct many different collection types via `FromIterator`" },
      { label: 'C', text: "Vector collections require explicit heap allocation permits within local thread memory" },
      { label: 'D', text: "Turbofish syntax disables runtime bounds checking on arrays within local thread memory" },
    ],
    correctIndex: 1,
    hint: "FromIterator is implemented for Vec, HashSet, BTreeSet, LinkedList, Result, etc.",
    explanation: "Because `collect()` can produce any type implementing `FromIterator` (such as `Vec`, `HashSet`, `BTreeSet`, `String`, `Result`), type inference cannot guess the target container without annotations or turbofish syntax `::<Vec<_>>()`.",
  },
  {
    id: "iter-code-sum-squares",
    categorySlug: "iterators-closures",
    title: "Sum of Even Squares",
    difficulty: 1,
    language: 'rust',
    kind: 'coding',
    tags: ["iterators-closures", 'coding'],
    prompt: "Implement `sum_even_squares(nums: &[i32]) -> i32` that filters even numbers, squares them, and returns their sum.",
    code: "pub fn sum_even_squares(nums: &[i32]) -> i32 {\n    nums.iter().filter(|&&x| x % 2 == 0).map(|&x| x * x).sum()\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    assert_eq!(sum_even_squares(&[1, 2, 3, 4]), 20);\n    assert_eq!(sum_even_squares(&[1, 3, 5]), 0);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `sum_even_squares(nums: &[i32]) -> i32` that filters even numbers, squares them, and returns their sum. Review the test cases to verify all assertions.",
  },
  {
    id: "iter-code-chunk-windows",
    categorySlug: "iterators-closures",
    title: "Window Triplet Maxima",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["iterators-closures", 'coding'],
    prompt: "Implement `triplet_maxima(nums: &[i32]) -> Vec<i32>` that uses `.windows(3)` and returns the maximum element of each 3-element sliding window.",
    code: "pub fn triplet_maxima(nums: &[i32]) -> Vec<i32> {\n    nums.windows(3).map(|w| *w.iter().max().unwrap()).collect()\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let res = triplet_maxima(&[1, 3, 2, 5, 4]);\n    assert_eq!(res, vec![3, 5, 5]);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `triplet_maxima(nums: &[i32]) -> Vec<i32>` that uses `.windows(3)` and returns the maximum element of each 3-element sliding window. Review the test cases to verify all assertions.",
  },
  {
    id: "iter-code-custom-counter",
    categorySlug: "iterators-closures",
    title: "Custom Range Counter Iterator",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["iterators-closures", 'coding'],
    prompt: "Implement `Iterator` for `pub struct Counter { pub current: u32, pub end: u32 }` where `next(&mut self)` yields numbers from `current` up to (exclusive) `end`.",
    code: "pub struct Counter {\n    pub current: u32,\n    pub end: u32,\n}\n\nimpl Iterator for Counter {\n    type Item = u32;\n\n    fn next(&mut self) -> Option<Self::Item> {\n        if self.current < self.end {\n            let val = self.current;\n            self.current += 1;\n            Some(val)\n        } else {\n            None\n        }\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let mut c = Counter { current: 1, end: 4 };\n    assert_eq!(c.next(), Some(1));\n    assert_eq!(c.next(), Some(2));\n    assert_eq!(c.next(), Some(3));\n    assert_eq!(c.next(), None);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `Iterator` for `pub struct Counter { pub current: u32, pub end: u32 }` where `next(&mut self)` yields numbers from `current` up to (exclusive) `end`. Review the test cases to verify all assertions.",
  },
  {
    id: "iter-code-flatten-options",
    categorySlug: "iterators-closures",
    title: "Flatten Optional Values",
    difficulty: 1,
    language: 'rust',
    kind: 'coding',
    tags: ["iterators-closures", 'coding'],
    prompt: "Implement `flatten_options(opts: Vec<Option<i32>>) -> Vec<i32>` using iterator `.into_iter().flatten().collect()`.",
    code: "pub fn flatten_options(opts: Vec<Option<i32>>) -> Vec<i32> {\n    opts.into_iter().flatten().collect()\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let data = vec![Some(1), None, Some(3), None, Some(5)];\n    assert_eq!(flatten_options(data), vec![1, 3, 5]);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `flatten_options(opts: Vec<Option<i32>>) -> Vec<i32>` using iterator `.into_iter().flatten().collect()`. Review the test cases to verify all assertions.",
  },
  {
    id: "iter-code-closure-fnmut",
    categorySlug: "iterators-closures",
    title: "Apply Mutating Closure",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["iterators-closures", 'coding'],
    prompt: "Implement `apply_each<F: FnMut(&mut i32)>(slice: &mut [i32], mut f: F)` applying the closure in-place to each element of the slice.",
    code: "pub fn apply_each<F: FnMut(&mut i32)>(slice: &mut [i32], mut f: F) {\n    for x in slice.iter_mut() {\n        f(x);\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let mut arr = [1, 2, 3];\n    apply_each(&mut arr, |x| *x *= 10);\n    assert_eq!(arr, [10, 20, 30]);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `apply_each<F: FnMut(&mut i32)>(slice: &mut [i32], mut f: F)` applying the closure in-place to each element of the slice. Review the test cases to verify all assertions.",
  },
  {
    id: "iter-code-zip-indices",
    categorySlug: "iterators-closures",
    title: "Dot Product of Vectors",
    difficulty: 1,
    language: 'rust',
    kind: 'coding',
    tags: ["iterators-closures", 'coding'],
    prompt: "Implement `dot_product(a: &[i32], b: &[i32]) -> i32` calculating the sum of `a[i] * b[i]` for corresponding elements using `.iter().zip().map().sum()`.",
    code: "pub fn dot_product(a: &[i32], b: &[i32]) -> i32 {\n    a.iter().zip(b.iter()).map(|(x, y)| x * y).sum()\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let a = [1, 2, 3];\n    let b = [4, 5, 6];\n    assert_eq!(dot_product(&a, &b), 4 + 10 + 18);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `dot_product(a: &[i32], b: &[i32]) -> i32` calculating the sum of `a[i] * b[i]` for corresponding elements using `.iter().zip().map().sum()`. Review the test cases to verify all assertions.",
  },
  {
    id: 'iter-fuse-once-none-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator Fuse Adapter',
    prompt: 'What guarantee does `.fuse()` provide on an arbitrary iterator?',
    tags: ['iterators-closures', 'fuse'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It wraps each yielded element into an Option variant' },
      { label: 'B', text: 'It allocates an internal backing ring buffer on heap' },
      { label: 'C', text: 'It guarantees that once None is returned it stays None' },
      { label: 'D', text: 'It resets the iterator pointer back to beginning node' },
    ],
    correctIndex: 2,
    hint: 'A fused iterator will never yield Some after returning None.',
    explanation: '`fuse()` adapts any iterator so that once `next()` returns `None`, all subsequent calls to `next()` are guaranteed to keep returning `None`.',
  },
  {
    id: 'iter-by-ref-adapter-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator Borrowing with by_ref',
    prompt: 'What is the primary use case of calling `iter.by_ref()` in an iterator pipeline?',
    tags: ['iterators-closures', 'by-ref'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It allows chaining adapters without consuming the iterator' },
      { label: 'B', text: 'It converts value-yielding iterators to reference streams' },
      { label: 'C', text: 'It enforces single-threaded ownership during loop pipeline' },
      { label: 'D', text: 'It automatically caches consumed items into memory vector' },
    ],
    correctIndex: 0,
    hint: 'by_ref borrows the iterator mutably so the remaining items can be used later.',
    explanation: '`by_ref()` creates a mutable borrow of an iterator, allowing you to apply consumer adapters (like `take(5)`) without moving or consuming the original iterator.',
  },
  {
    id: 'iter-inspect-side-effects-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator inspect Debugging',
    prompt: 'What is the role of `.inspect(|item| ...)` in an iterator pipeline?',
    tags: ['iterators-closures', 'inspect'],
    difficulty: 1,
    language: 'rust',
    options: [
      { label: 'A', text: 'It filters elements that return false from the inspect fn' },
      { label: 'B', text: 'It mutates the yielded elements in place without allocating' },
      { label: 'C', text: 'It transforms items into arbitrary output types dynamically' },
      { label: 'D', text: 'It executes a closure on each item without modifying values' },
    ],
    correctIndex: 3,
    hint: 'inspect allows observing items passing through an iterator chain without modifying them.',
    explanation: '`inspect()` takes a closure `&FnMut(&Item)` to perform side-effects (such as logging or debugging) on elements as they pass through without altering the sequence.',
  },
  {
    id: 'iter-flat-map-option-1',
    categorySlug: 'iterators-closures',
    title: 'FlatMap with Option Returns',
    prompt: 'What happens when `.flat_map(|x| ...)` returns an `Option<T>` for each element?',
    tags: ['iterators-closures', 'flat-map', 'option'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It requires all Option items to be unwrapped with panic' },
      { label: 'B', text: 'It automatically skips None and yields only Some values' },
      { label: 'C', text: 'It combines all items into a single heap vector buffer' },
      { label: 'D', text: 'It stops iterator evaluation at the very first None item' },
    ],
    correctIndex: 1,
    hint: 'Option implements IntoIterator yielding either 0 or 1 item.',
    explanation: 'Because `Option<T>` implements `IntoIterator`, `flat_map` flattens `Some(v)` into `v` and silently ignores `None`, functioning similarly to `filter_map`.',
  },
  {
    id: 'iter-rchunks-exact-remainder-1',
    categorySlug: 'iterators-closures',
    title: 'Reverse Slicing with rchunks_exact',
    prompt: 'How does `slice.rchunks_exact(n)` partition a slice?',
    tags: ['iterators-closures', 'rchunks', 'slice'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It yields non-overlapping chunks from the slice tail' },
      { label: 'B', text: 'It allocates copies of slice segments in reverse order' },
      { label: 'C', text: 'It panics if the slice length is not an exact multiple' },
      { label: 'D', text: 'It sorts the chunks in descending order automatically' },
    ],
    correctIndex: 0,
    hint: 'rchunks_exact starts chunking from the end of the slice towards the front.',
    explanation: '`rchunks_exact(n)` yields exact chunks of length `n` starting from the end of the slice, leaving any remainder at the start available via `.remainder()`.',
  },
  {
    id: 'iter-advance-by-n-efficiency-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator advance_by Optimization',
    prompt: 'What performance benefit does `Iterator::advance_by(n)` provide over repeated `.next()` calls?',
    tags: ['iterators-closures', 'advance-by', 'performance'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It calls next() in a dynamic busy loop inside CPU cache' },
      { label: 'B', text: 'It skips elements in O(1) time when optimized for slices' },
      { label: 'C', text: 'It converts the iterator stream into a static linked list' },
      { label: 'D', text: 'It panics immediately if the iterator length is unknown' },
    ],
    correctIndex: 1,
    hint: 'advance_by allows iterators over contiguous slices to advance internal pointers in O(1).',
    explanation: '`advance_by(n)` allows specialized iterators (like slices and ranges) to jump forward `n` elements in O(1) pointer arithmetic rather than executing an O(n) stepping loop.',
  },
  {
    id: 'iter-collect-result-short-circuit-1',
    categorySlug: 'iterators-closures',
    title: 'Collecting Iterator of Results into Result<Vec<T>, E>',
    prompt: 'What happens when `iter.collect::<Result<Vec<T>, E>>()` encounters an `Err`?',
    tags: ['iterators-closures', 'collect', 'result'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It replaces all Err variants with default zero values' },
      { label: 'B', text: 'It returns a tuple of successful values and error lists' },
      { label: 'C', text: 'It continues iteration and collects all errors to vector' },
      { label: 'D', text: 'It stops on the first Err and returns that Err directly' },
    ],
    correctIndex: 3,
    hint: 'Collecting into Result short-circuits on the first error.',
    explanation: '`collect()` into `Result<Vec<T>, E>` short-circuits: upon encountering the first `Err(e)`, iteration immediately stops and returns `Err(e)`.',
  },
  {
    id: 'iter-zip-shortest-termination-1',
    categorySlug: 'iterators-closures',
    title: 'Iterator zip Termination Condition',
    prompt: 'When does `iter_a.zip(iter_b)` stop yielding elements?',
    tags: ['iterators-closures', 'zip'],
    difficulty: 1,
    language: 'rust',
    options: [
      { label: 'A', text: 'It terminates immediately as soon as either iterator ends' },
      { label: 'B', text: 'It pads the shorter iterator with default None values' },
      { label: 'C', text: 'It panics if the two zipped iterators have unequal sizes' },
      { label: 'D', text: 'It resets the shorter iterator back to the first element' },
    ],
    correctIndex: 0,
    hint: 'zip stops as soon as either underlying iterator yields None.',
    explanation: '`zip()` terminates as soon as either iterator returns `None`. Remaining elements in the longer iterator are not consumed.',
  },
  {
    id: 'iter-scan-internal-state-1',
    categorySlug: 'iterators-closures',
    title: 'Stateful Iteration with scan',
    prompt: 'What is the primary role of `.scan(initial_state, |state, item| ...)` in Rust?',
    tags: ['iterators-closures', 'scan', 'state'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It stores historical items in a global static array pool' },
      { label: 'B', text: 'It parses items into token streams using compiler syntax' },
      { label: 'C', text: 'It maintains internal mutable state across closure calls' },
      { label: 'D', text: 'It scans system memory pages for matching byte sequences' },
    ],
    correctIndex: 2,
    hint: 'scan is like fold/reduce but yields an iterator while maintaining internal mutable state.',
    explanation: '`scan()` maintains internal mutable state across iteration steps, allowing stateful transformations and early termination by returning `None` from the closure.',
  },
  {
    id: 'iter-peekable-peek-mut-1',
    categorySlug: 'iterators-closures',
    title: 'In-Place Mutation with peek_mut',
    prompt: 'What does `Peekable::peek_mut(&mut self)` allow on an iterator?',
    tags: ['iterators-closures', 'peekable', 'peek_mut'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It allows in-place mutation of the upcoming next element' },
      { label: 'B', text: 'It consumes the next item and stores it in thread heap' },
      { label: 'C', text: 'It clones the next element into an immutable shared box' },
      { label: 'D', text: 'It reverses the iteration direction towards collection head' },
    ],
    correctIndex: 0,
    hint: 'peek_mut returns a mutable reference &mut to the peeked item without consuming it.',
    explanation: '`peek_mut()` returns a `PeekMut` guard holding a mutable reference to the next element, allowing you to modify the peeked value before it is consumed.',
  },
]
