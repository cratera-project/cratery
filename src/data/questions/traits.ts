import type { Question } from '../../lib/quiz'

export const traitsQuestions: Question[] = [
  {
    id: 'trait-def-1',
    categorySlug: 'traits',
    title: 'Trait Definition',
    prompt: 'What is the primary role of a `trait` in Rust?',
    tags: ['traits', 'impl'],
    difficulty: 1,
    language: 'rust',
    code: `trait Summary {
    fn summarize(&self) -> String;
}`,
    options: [
      { label: 'A', text: "Describe shared behavior types can implement" },
      { label: 'B', text: "Define a class hierarchy with inheritance of fields" },
      { label: 'C', text: "Limit variable scope inside a single module in code" },
      { label: 'D', text: "Expand macros into concrete method bodies in code" },
    ],
    correctIndex: 0,
    hint: 'Think “interface for behavior,” not OOP inheritance.',
    explanation:
      'A trait declares method signatures (and optional defaults) that describe shared behavior. Types `impl` the trait to provide those methods. Traits do not define class hierarchies or field inheritance.',
  },
  {
    id: 'trait-bound-1',
    categorySlug: 'traits',
    title: 'Trait Bounds',
    prompt: 'What does this bound require of `T`?',
    tags: ['traits', 'bounds'],
    difficulty: 2,
    language: 'rust',
    code: `pub fn notify<T: Display + Clone>(item: &T) { /* ... */ }`,
    options: [
      { label: 'A', text: '`T` must implement `Display` or else `Clone`' },
      { label: 'B', text: '`T` must convert into both `Display` and `Clone`' },
      { label: 'C', text: '`T` must return `Display` and `Clone` values' },
      { label: 'D', text: '`T` must implement both `Display` and `Clone`' },
    ],
    correctIndex: 3,
    hint: 'In bounds, `+` means “and,” not “or.”',
    explanation:
      '`T: Display + Clone` means `T` must implement every listed trait. The caller must supply a type that is both `Display` and `Clone`.',
  },
  {
    id: 'trait-default-1',
    categorySlug: 'traits',
    title: 'Default Implementations',
    prompt: 'What happens with this empty `impl`?',
    tags: ['traits', 'default'],
    difficulty: 2,
    language: 'rust',
    code: `trait Summary {
    fn summarize(&self) -> String {
        String::from("(Read more...)")
    }
}

struct NewsArticle;
impl Summary for NewsArticle {}`,
    options: [
      { label: 'A', text: 'It fails because no methods were written in the impl' },
      { label: 'B', text: 'It panics if `summarize` is called at runtime' },
      { label: 'C', text: 'It uses the trait’s default `summarize` body' },
      { label: 'D', text: 'It generates an empty method that returns `()`' },
    ],
    correctIndex: 2,
    hint: 'Defaults fill in methods you choose not to override.',
    explanation:
      'Traits may supply default method bodies. An empty `impl Summary for NewsArticle {}` is valid and inherits that default `summarize` unless you override it.',
  },
  {
    id: 'trait-derive-1',
    categorySlug: 'traits',
    title: 'Derive Attribute',
    prompt: 'What does `#[derive(Debug)]` do?',
    tags: ['traits', 'derive'],
    difficulty: 1,
    language: 'rust',
    code: `#[derive(Debug)]
struct Point { x: i32, y: i32 }`,
    options: [
      { label: 'A', text: 'Mark every field as mutable for debugging sessions' },
      { label: 'B', text: 'Disable optimizations so values stay inspectable' },
      { label: 'C', text: 'Generate an `impl Debug for Point` via derive' },
      { label: 'D', text: 'Turn on compiler logging for this type only' },
    ],
    correctIndex: 2,
    hint: '`derive` is a macro that writes a trait impl for you.',
    explanation:
      '`#[derive(Debug)]` runs a derive macro that generates an `impl Debug for Point`, enabling formatting with `{:?}`. It does not change mutability or compiler logging.',
  },
  {
    id: 'trait-dyn-1',
    categorySlug: 'traits',
    title: 'Trait Objects',
    prompt: 'Why use `Vec<Box<dyn Animal>>` here?',
    tags: ['traits', 'dyn'],
    difficulty: 3,
    language: 'rust',
    code: `let animals: Vec<Box<dyn Animal>> = vec![
    Box::new(Dog {}),
    Box::new(Cat {}),
];`,
    options: [
      { label: 'A', text: 'So the vector itself becomes immutable forever after' },
      { label: 'B', text: 'Because `Vec` can only hold pointer-sized element types' },
      { label: 'C', text: 'To grow the vector without ever needing reallocation' },
      { label: 'D', text: 'To store different `Animal` implementors together' },
    ],
    correctIndex: 3,
    hint: '`Vec` needs one element layout; trait objects unify that layout.',
    explanation:
      '`Dog` and `Cat` have different sizes. Boxing them as `dyn Animal` gives a uniform fat-pointer element type so one `Vec` can hold mixed implementors behind dynamic dispatch.',
  },
  {
    id: 'trait-impltrait-two-args-1',
    categorySlug: 'traits',
    title: '`impl Trait` Parameters',
    prompt: 'What is true about these parameters?',
    tags: ['traits', 'impl-trait', 'bounds'],
    difficulty: 3,
    language: 'rust',
    code: `pub fn notify(item1: &impl Summary, item2: &impl Summary) {}`,
    options: [
      { label: 'A', text: 'Each arg may be a different `Summary` type' },
      { label: 'B', text: 'Both arguments must share one concrete type' },
      { label: 'C', text: 'Both arguments must be `dyn Summary` objects' },
      { label: 'D', text: 'This form works only for stdlib traits' },
    ],
    correctIndex: 0,
    hint: 'Each `impl Trait` argument is an independent anonymous generic.',
    explanation:
      'Each `impl Summary` in argument position is its own type parameter. The two arguments may therefore be different concrete types, as long as both implement `Summary`.',
  },
  {
    id: 'trait-bound-same-type-1',
    categorySlug: 'traits',
    title: 'One `T` Means One Type',
    prompt: 'What does this signature force about the two items?',
    tags: ['traits', 'bounds', 'generics'],
    difficulty: 3,
    language: 'rust',
    code: `pub fn notify<T: Summary>(item1: &T, item2: &T) {}`,
    options: [
      { label: 'A', text: 'Both must be stored as `Box<dyn Summary>`' },
      { label: 'B', text: 'Both must share one lifetime, not one type' },
      { label: 'C', text: 'Both must be owned values, never references' },
      { label: 'D', text: 'Both must be the same concrete type `T`' },
    ],
    correctIndex: 3,
    hint: 'A single type parameter is one type per call site.',
    explanation:
      'One `T` means one concrete type for that call. Both parameters must therefore be the same type that implements `Summary`, unlike two separate `impl Summary` parameters.',
  },
  {
    id: 'trait-multi-bounds-plus-1',
    categorySlug: 'traits',
    title: 'Multiple Trait Bounds',
    prompt: 'What does `T: Summary + Display` require?',
    tags: ['traits', 'bounds'],
    difficulty: 2,
    language: 'rust',
    code: `fn f<T: Summary + Display>(t: &T) { /* ... */ }`,
    options: [
      { label: 'A', text: '`T` implements `Summary` or else `Display`' },
      { label: 'B', text: '`T` is a subtype of both trait objects' },
      { label: 'C', text: '`T` returns values of both trait types' },
      { label: 'D', text: '`T` implements `Summary` and `Display`' },
    ],
    correctIndex: 3,
    hint: '`+` stacks requirements that all must hold.',
    explanation:
      'The `+` syntax combines bounds. The caller must provide a type that implements both `Summary` and `Display`. Rust has no trait subtyping hierarchy like classical OOP.',
  },
  {
    id: 'trait-where-clause-1',
    categorySlug: 'traits',
    title: '`where` Clause',
    prompt: 'What does a `where` clause change here?',
    tags: ['traits', 'bounds', 'where'],
    difficulty: 2,
    language: 'rust',
    code: `fn some_function<T, U>(t: &T, u: &U) -> i32
where
    T: Display + Clone,
    U: Clone + Debug,
{
    0
}`,
    options: [
      { label: 'A', text: 'It makes the generic function faster at runtime' },
      { label: 'B', text: 'It switches every call site to dynamic dispatch' },
      { label: 'C', text: 'Nothing semantic, just clearer bound placement' },
      { label: 'D', text: 'It forces both `T` and `U` to be the same type' },
    ],
    correctIndex: 2,
    hint: 'These bounds could also be written next to `T` and `U`.',
    explanation:
      'Here `where` only relocates bounds that could also be written as `T: Display + Clone` and `U: Clone + Debug` after each type parameter. That inline form is a shorthand for simple parameter bounds. `where` is the general place for bounds and does not change dispatch, performance, or force `T` and `U` to match.',
  },
  {
    id: 'trait-where-needed-1',
    categorySlug: 'traits',
    title: 'When `where` Is Required',
    prompt: 'Why is this `impl` written with a `where` clause?',
    tags: ['traits', 'bounds', 'where'],
    difficulty: 3,
    language: 'rust',
    code: `use std::fmt::Debug;

trait PrintInOption {
    fn print_in_option(self);
}

impl<T> PrintInOption for T
where
    Option<T>: Debug,
{
    fn print_in_option(self) {
        println!("{:?}", Some(self));
    }
}`,
    options: [
      { label: 'A', text: 'Blanket `impl`s are illegal unless they use `where`' },
      { label: 'B', text: '`Debug` cannot appear as any kind of trait bound here' },
      { label: 'C', text: 'The bound is on `Option<T>`, not only on the parameter `T`' },
      { label: 'D', text: '`Option` forces dynamic dispatch unless written with `where`' },
    ],
    correctIndex: 2,
    hint: 'Look at which type appears on the left of the colon in the bound.',
    explanation:
      'Inline `T: Trait` after a type parameter is only a shorthand for bounds on that parameter. A `where` clause can bound arbitrary types, such as `Option<T>: Debug` here (the value being printed). That form cannot be written as a plain `T: …` parameter shorthand. `where` is the general bound syntax; the angle-bracket form is the limited shorthand.',
  },
  {
    id: 'trait-return-impl-single-type-1',
    categorySlug: 'traits',
    title: '`impl Trait` Return Restriction',
    prompt: 'Why can’t both branches return different types here?',
    tags: ['traits', 'impl-trait'],
    difficulty: 3,
    language: 'rust',
    code: `fn returns_summarizable(switch: bool) -> impl Summary {
    if switch {
        NewsArticle {}
    } else {
        SocialPost {}
    }
}`,
    options: [
      { label: 'A', text: '`impl Trait` is illegal in return position' },
      { label: 'B', text: 'Structs cannot implement user-defined traits' },
      { label: 'C', text: '`if` expressions cannot return trait implementors' },
      { label: 'D', text: '`impl Summary` hides exactly one concrete type' },
    ],
    correctIndex: 3,
    hint: 'Opaque return types still name one underlying type.',
    explanation:
      '`-> impl Summary` means “some single concrete type that implements `Summary`.” Returning `NewsArticle` or `SocialPost` depending on `switch` needs a trait object (or an enum) instead.',
  },
  {
    id: 'trait-object-unsized-1',
    categorySlug: 'traits',
    title: 'Why `Box<dyn Trait>`?',
    prompt: 'Why doesn’t this work, and what’s the usual fix?',
    tags: ['traits', 'dyn', 'sized'],
    difficulty: 3,
    language: 'rust',
    code: `struct Screen {
    components: Vec<dyn Draw>,
}`,
    options: [
      { label: 'A', text: 'Traits can’t appear in structs; switch to an enum' },
      { label: 'B', text: '`dyn Draw` is unsized; use `Vec<Box<dyn Draw>>`' },
      { label: 'C', text: '`Vec` requires `Copy`; bound `Draw: Copy` instead' },
      { label: 'D', text: 'Write `Vec<impl Draw>`; sizedness follows automatically' },
    ],
    correctIndex: 1,
    hint: '`Vec` elements must have a known size at compile time.',
    explanation:
      '`dyn Draw` is a DST (dynamically sized). Put it behind a pointer such as `Box<dyn Draw>` (or `&dyn Draw`) so `Vec` has a sized element type.',
  },
  {
    id: 'trait-object-vtable-1',
    categorySlug: 'traits',
    title: 'Pointers to Trait Objects',
    prompt: 'What does a pointer to a trait object (`Box<dyn Draw>`) carry?',
    tags: ['traits', 'dyn', 'dispatch'],
    difficulty: 3,
    language: 'rust',
    code: `let x: Box<dyn Draw> = /* ... */;`,
    options: [
      { label: 'A', text: 'Only method pointers, with no concrete data' },
      { label: 'B', text: 'Only the concrete value, with no method table' },
      { label: 'C', text: 'A copy of every `impl` for every type in the crate' },
      { label: 'D', text: 'A data pointer plus a vtable for trait methods' },
    ],
    correctIndex: 3,
    hint: 'Fat pointers pair data with how to call the trait.',
    explanation:
      'Per the Reference, `dyn Draw` is the trait object (an unsized opaque value). Pointers to it (`&dyn Draw`, `Box<dyn Draw>`) are fat: a pointer to the concrete value plus a vtable for dynamic dispatch.',
  },
  {
    id: 'trait-objects-vs-generics-1',
    categorySlug: 'traits',
    title: 'Trait Objects vs Generics',
    prompt: 'What is the key difference between these designs?',
    tags: ['traits', 'dyn', 'generics'],
    difficulty: 3,
    language: 'rust',
    code: `struct ScreenA { components: Vec<Box<dyn Draw>> }
struct ScreenB<T: Draw> { components: Vec<T> }`,
    options: [
      { label: 'A', text: 'Only generics can call methods from `Draw`' },
      { label: 'B', text: 'Trait objects mix types; generics keep one `T`' },
      { label: 'C', text: 'Trait objects avoid heap; generics require `Box`' },
      { label: 'D', text: 'Generics always dispatch methods at runtime' },
    ],
    correctIndex: 1,
    hint: 'Homogeneous vs heterogeneous collections.',
    explanation:
      '`ScreenB<T>` is homogeneous: one concrete `T` per instance. `ScreenA` can hold mixed `Draw` implementors via trait objects. Generics use static dispatch (monomorphization); trait objects use dynamic dispatch.',
  },
  {
    id: 'trait-dispatch-static-vs-dynamic-1',
    categorySlug: 'traits',
    title: 'Static vs Dynamic Dispatch',
    prompt: 'How are `draw` calls resolved through a trait object?',
    tags: ['traits', 'dyn', 'dispatch'],
    difficulty: 3,
    language: 'rust',
    code: `fn run(xs: Vec<Box<dyn Draw>>) {
    for x in xs {
        x.draw();
    }
}`,
    options: [
      { label: 'A', text: 'Monomorphize `draw` for each concrete type used' },
      { label: 'B', text: 'Look up the method via dynamic dispatch' },
      { label: 'C', text: 'Reject unless `Draw` also requires `Copy`' },
      { label: 'D', text: 'Rewrite each call into a macro expansion' },
    ],
    correctIndex: 1,
    hint: '`dyn` means the concrete type is known only at runtime.',
    explanation:
      'Calls through `dyn Draw` use dynamic dispatch: the vtable selects the concrete `draw` at runtime. Generics would monomorphize instead.',
  },
  {
    id: 'trait-blanket-impl-1',
    categorySlug: 'traits',
    title: 'Blanket Implementations',
    prompt: 'What does this style of `impl` represent?',
    tags: ['traits', 'blanket-impl'],
    difficulty: 3,
    language: 'rust',
    code: `impl<T: Display> ToString for T {
    // ...
}`,
    options: [
      { label: 'A', text: 'An impl that applies only to the `String` type' },
      { label: 'B', text: 'A blanket impl: any `T: Display` gets `ToString`' },
      { label: 'C', text: 'An impl that disables all other `ToString` impls' },
      { label: 'D', text: 'An impl that forces heap allocation for every `T`' },
    ],
    correctIndex: 1,
    hint: 'The impl is parameterized over all types meeting a bound.',
    explanation:
      'A blanket implementation applies to every type that satisfies the bound. The standard library uses this so any `Display` type automatically gets `to_string()`.',
  },
  {
    id: 'trait-orphan-1',
    categorySlug: 'traits',
    title: 'Orphan Rule',
    prompt: 'Which impl is allowed under the orphan rules?',
    tags: ['traits', 'coherence'],
    difficulty: 3,
    language: 'rust',
    code: `// In your crate, which is legal?
// impl Display for Vec<i32> { ... }
// impl MyTrait for String { ... }
// impl Clone for i32 { ... }
// impl Debug for &str { ... }`,
    options: [
      { label: 'A', text: '`impl Display for Vec<i32>` in your crate' },
      { label: 'B', text: '`impl MyTrait for String` in your crate' },
      { label: 'C', text: '`impl Clone for i32` in your crate' },
      { label: 'D', text: '`impl Debug for &str` in your crate' },
    ],
    correctIndex: 1,
    hint: 'You need to own either the trait or the type.',
    explanation:
      'You may implement a trait for a type if your crate defines the trait or the type (with some allowed coverings). `MyTrait` is yours, so `impl MyTrait for String` is fine. Implementing foreign traits for foreign types (`Display`/`Clone`/`Debug` for std types) is not.',
  },
  {
    id: 'trait-supertrait-1',
    categorySlug: 'traits',
    title: 'Supertraits',
    prompt: 'What does this supertrait bound mean?',
    tags: ['traits', 'supertraits'],
    difficulty: 2,
    language: 'rust',
    code: `trait OutlinePrint: Display {
    fn outline_print(&self) {
        let output = self.to_string();
        println!("{output}");
    }
}`,
    options: [
      { label: 'A', text: 'Implementors must also implement `Display`' },
      { label: 'B', text: '`OutlinePrint` automatically impls `Display`' },
      { label: 'C', text: '`Display` methods become `unsafe` to call' },
      { label: 'D', text: 'Only `Display` types may define any new traits' },
    ],
    correctIndex: 0,
    hint: 'Supertraits are extra requirements on implementors.',
    explanation:
      '`trait OutlinePrint: Display` means any type that implements `OutlinePrint` must also implement `Display`. The default method can then call `Display`/`ToString` APIs. It does not auto-implement `Display`.',
  },
  {
    id: 'trait-associated-type-1',
    categorySlug: 'traits',
    title: 'Associated Types',
    prompt: 'What does `type Item` mean in this trait?',
    tags: ['traits', 'associated-types'],
    difficulty: 2,
    language: 'rust',
    code: `trait Iterator {
    type Item;
    fn next(&mut self) -> Option<Self::Item>;
}`,
    options: [
      { label: 'A', text: 'A type alias chosen once per implementing type' },
      { label: 'B', text: 'A second generic parameter callers must always pass' },
      { label: 'C', text: 'A runtime tag identifying the iterator’s element' },
      { label: 'D', text: 'A lifetime tied to every call to `next`' },
    ],
    correctIndex: 0,
    hint: 'Associated types are outputs of the impl, not inputs at each call.',
    explanation:
      'An associated type is a type placeholder fixed by each `impl`. For `Iterator`, `Item` is the element type produced by that iterator. Callers use `I::Item` rather than adding another type parameter on every use.',
  },
  {
    id: 'trait-object-safety-1',
    categorySlug: 'traits',
    title: 'Dyn Compatibility',
    prompt: 'Why can’t this trait be a trait object?',
    tags: ['traits', 'dyn', 'dyn-compatibility'],
    difficulty: 3,
    language: 'rust',
    code: `trait Bad {
    fn clone_me(&self) -> Self;
}

// let x: Box<dyn Bad> = ...;  // error`,
    options: [
      { label: 'A', text: 'Returning `Self` by value isn’t dyn-compatible' },
      { label: 'B', text: 'Traits with methods can never become `dyn`' },
      { label: 'C', text: '`Box` cannot hold unsized trait objects at all' },
      { label: 'D', text: 'Dyn compatibility requires every method to be `async`' },
    ],
    correctIndex: 0,
    hint: 'The compiler must know the returned size for `dyn Trait`.',
    explanation:
      'A trait must be dyn-compatible (formerly called object-safe) to be used as `dyn Trait`. Methods that return `Self` by value are not dyn-compatible, because the concrete size isn’t known for `dyn Bad`. Methods taking `Self` by value are similarly problematic. Use generics, or redesign the API (e.g. return `Box<dyn Bad>`).',
  },
  {
    id: 'trait-from-into-1',
    categorySlug: 'traits',
    title: 'From and Into',
    prompt: 'Why does implementing `From<A> for B` give you `Into`?',
    tags: ['traits', 'from', 'into'],
    difficulty: 2,
    language: 'rust',
    code: `struct Id(u32);

impl From<u32> for Id {
    fn from(v: u32) -> Self {
        Id(v)
    }
}

fn main() {
    let id: Id = 7u32.into();
}`,
    options: [
      { label: 'A', text: '`into` is a keyword that ignores trait impls entirely' },
      { label: 'B', text: 'A blanket `Into` impl is provided for all `From` impls' },
      { label: 'C', text: '`From` and `Into` are identical traits with two names' },
      { label: 'D', text: 'Only `u32` gets `Into` automatically; others need both' },
    ],
    correctIndex: 1,
    hint: 'Prefer implementing `From`; callers can still use `.into()`.',
    explanation:
      'The standard library provides `impl<T, U: From<T>> Into<U> for T`. Implement `From`, and `.into()` becomes available. Implementing both manually is unnecessary and can conflict.',
  },
  {
    id: 'trait-sized-1',
    categorySlug: 'traits',
    title: 'Implicit Sized Bound',
    prompt: 'Why does this generic need `T: ?Sized`?',
    tags: ['traits', 'sized'],
    difficulty: 2,
    language: 'rust',
    code: `fn print_len<T: ?Sized + AsRef<str>>(x: &T) {
    println!("{}", x.as_ref().len());
}

fn main() {
    print_len("hi"); // unsized str behind a reference
}`,
    options: [
      { label: 'A', text: 'Generics default to `T: Sized`; `?Sized` relaxes that' },
      { label: 'B', text: '`?Sized` makes `T` allocate on the heap automatically' },
      { label: 'C', text: 'Without `?Sized`, references to `T` are always illegal' },
      { label: 'D', text: '`AsRef` only works when `T` is marked `?Sized`' },
    ],
    correctIndex: 0,
    hint: 'Type parameters are `Sized` unless you opt out.',
    explanation:
      'By default, generic `T` has an implicit `Sized` bound. `T: ?Sized` allows unsized types like `str` or `dyn Trait` when they appear behind pointers/references. It does not allocate by itself.',
  },
  {
    id: 'trait-drop-1',
    categorySlug: 'traits',
    title: 'Drop Trait Limits',
    prompt: 'What is true about implementing `Drop`?',
    tags: ['traits', 'drop'],
    difficulty: 2,
    language: 'rust',
    code: `struct Guard;

impl Drop for Guard {
    fn drop(&mut self) {
        println!("bye");
    }
}`,
    options: [
      { label: 'A', text: 'You may call `Drop::drop` directly to run cleanup early' },
      { label: 'B', text: '`Drop` types can also implement `Copy` for cheap moves' },
      { label: 'C', text: 'The compiler invokes `drop` at the end of scope' },
      { label: 'D', text: '`drop` must be `async` to flush I/O before exiting' },
    ],
    correctIndex: 2,
    hint: 'Use `std::mem::drop` to drop early; don’t call the method.',
    explanation:
      '`Drop::drop` runs automatically at end of scope (and via `mem::drop`). Calling `Drop::drop` yourself is disallowed. `Copy` and `Drop` are mutually exclusive.',
  },
  {
    id: 'trait-borrow-1',
    categorySlug: 'traits',
    title: 'Borrow Trait in APIs',
    prompt: 'Why do map lookups take `Q: ?Sized where K: Borrow<Q>`?',
    tags: ['traits', 'borrow'],
    difficulty: 2,
    language: 'rust',
    code: `use std::collections::HashMap;
let mut m: HashMap<String, i32> = HashMap::new();
m.insert("a".into(), 1);
let v = m.get("a"); // &str query, String keys`,
    options: [
      { label: 'A', text: '`Borrow` lets queries use equivalent borrowed forms' },
      { label: 'B', text: '`get` always clones keys into owned `String` values' },
      { label: 'C', text: '`HashMap` can only look up with the exact key type' },
      { label: 'D', text: '`Borrow` converts every `&str` into a mutable key' },
    ],
    correctIndex: 0,
    hint: '`String` borrows as `str` with matching hash/eq.',
    explanation:
      '`Borrow` relates owned and borrowed views that hash/compare consistently. That is why `HashMap<String, _>::get` accepts `&str`. It does not clone the key for the lookup.',
  },
  {
    id: 'trait-upcast-1',
    categorySlug: 'traits',
    title: 'Trait Object Upcasting',
    prompt: 'Why does `up` compile on stable Rust?',
    tags: ['traits', 'dyn', 'upcasting'],
    difficulty: 2,
    language: 'rust',
    code: `trait Super {
    fn tag(&self) -> i32;
}
trait Sub: Super {}

struct S;
impl Super for S {
    fn tag(&self) -> i32 { 7 }
}
impl Sub for S {}

fn up(x: &dyn Sub) -> &dyn Super {
    x
}`,
    options: [
      { label: 'A', text: 'Any `dyn Trait` coerces to every other trait' },
      { label: 'B', text: '`dyn Sub` coerces to a supertrait object' },
      { label: 'C', text: '`up` clones `S` into a fresh vtable `Box`' },
      { label: 'D', text: 'You still must write an `as_super` method' },
    ],
    correctIndex: 1,
    hint: 'Subtrait objects know how to view their supertraits.',
    explanation:
      'Since Rust 1.86, a trait object can be coerced to a supertrait object (`&dyn Sub` → `&dyn Super`, and similarly for `Box`/`Arc`/raw pointers). Arbitrary unrelated traits do not coerce. An `as_super` helper is no longer required for this case.',
  },
  {
    id: 'trait-asref-1',
    categorySlug: 'traits',
    title: 'AsRef Generic APIs',
    prompt: 'Why can `is_hello` take both `&str` and `String`?',
    tags: ['traits', 'asref'],
    difficulty: 2,
    language: 'rust',
    code: `fn is_hello<T: AsRef<str>>(s: T) {
    assert_eq!("hello", s.as_ref());
}

fn main() {
    is_hello("hello");
    is_hello("hello".to_string());
}`,
    options: [
      { label: 'A', text: '`AsRef` clones every argument into a `String`' },
      { label: 'B', text: 'Generic functions ignore the argument’s type' },
      { label: 'C', text: 'Both types implement `AsRef<str>` cheaply' },
      { label: 'D', text: '`String` is a subtype of `str` in the type system' },
    ],
    correctIndex: 2,
    hint: 'The bound asks for a cheap `&str` view, not ownership.',
    explanation:
      '`AsRef<str>` is a cheap reference-to-reference conversion. Both `&str` and `String` implement it, so the generic function accepts either without cloning. `String` is not a subtype of `str`; you typically go through `Deref`/`AsRef`. Prefer `Borrow` when hash/eq must match an owned key.',
  },
  {
    id: 'trait-assoc-const-1',
    categorySlug: 'traits',
    title: 'Associated Constants',
    prompt: 'How is `MAX` provided on `u8` here?',
    tags: ['traits', 'associated-const'],
    difficulty: 2,
    language: 'rust',
    code: `trait Max {
    const MAX: u32;
}

impl Max for u8 {
    const MAX: u32 = 255;
}

fn main() {
    assert_eq!(<u8 as Max>::MAX, 255);
}`,
    options: [
      { label: 'A', text: 'As an associated constant on the trait impl' },
      { label: 'B', text: 'As a `static` the trait imports automatically' },
      { label: 'C', text: 'As a default type parameter on `Max` itself' },
      { label: 'D', text: 'As a `const fn` that must run inside `main`' },
    ],
    correctIndex: 0,
    hint: 'Traits can carry `const` items, not only methods.',
    explanation:
      'Traits may declare associated constants. Each impl supplies the value (`const MAX: u32 = 255`). Access it as `<Type as Trait>::MAX` (or `Type::MAX` if unambiguous). It is not a `static` item and does not need a runtime call.',
  },
  {
    id: 'trait-gat-associated-lifetime-1',
    categorySlug: 'traits',
    title: 'Generic Associated Types (GATs)',
    prompt: 'Why can `LendingIterator` yield items that borrow from `&mut self`?',
    tags: ['traits', 'gat', 'generics'],
    difficulty: 3,
    language: 'rust',
    code: `trait LendingIterator {
    type Item<'a> where Self: 'a;
    fn next<'a>(&'a mut self) -> Option<Self::Item<'a>>;
}`,
    options: [
      { label: 'A', text: 'It circumvents borrow checking using compiler magic' },
      { label: 'B', text: "It forces all yielded items to have 'static lifetime" },
      { label: 'C', text: "Its associated type Item takes a generic lifetime 'a" },
      { label: 'D', text: 'It converts &mut self into shared references on call' },
    ],
    correctIndex: 2,
    hint: 'Notice the parameter list on the associated Item declaration.',
    explanation:
      "In standard `std::iter::Iterator`, `type Item` has no lifetime parameter, meaning `Item` must be chosen independently of the `&mut self` borrow in `next()`. Generic Associated Types (GATs) allow declaring `type Item<'a>`, so the yielded item type can explicitly borrow from `&mut self` with lifetime `'a`. This enables lending / streaming iterators.",
  },
  {
    id: 'trait-disambiguation-inherent-1',
    categorySlug: 'traits',
    title: 'Inherent Method Precedence',
    prompt: 'What is the output of running this program?',
    tags: ['traits', 'methods', 'inherent'],
    difficulty: 2,
    language: 'rust',
    code: `trait Greet {
    fn say(&self) -> &'static str { "trait greet" }
}

struct User;

impl User {
    fn say(&self) -> &'static str { "inherent greet" }
}

impl Greet for User {}

fn main() {
    let u = User;
    println!("{}", u.say());
}`,
    options: [
      { label: 'A', text: 'trait greet because trait methods have higher priority' },
      { label: 'B', text: 'inherent greet because inherent methods take precedence' },
      { label: 'C', text: 'Fails to compile due to ambiguous duplicate method names' },
      { label: 'D', text: 'Fails to compile because Greet::say is not implemented' },
    ],
    correctIndex: 1,
    hint: 'Dot syntax looks for inherent methods before searching traits.',
    explanation:
      'When resolving method calls via dot syntax (`u.say()`), Rust checks inherent methods on the concrete type before looking up trait methods in scope. Therefore, `User::say` takes precedence and prints `"inherent greet"`. To call the trait implementation, use fully qualified syntax: `Greet::say(&u)` or `<User as Greet>::say(&u)`.',
  },
  
  {
    id: "trait-supporter-1",
    categorySlug: "traits",
    title: "Trait Object Upcasting",
    prompt: "How does modern Rust support casting a sub-trait object `dyn Sub` to its supertrait `dyn Super`?",
    tags: ["traits","upcasting","dyn"],
    difficulty: 2,
    language: 'rust',
    code: "trait Super {}\ntrait Sub: Super {}\n\nstruct Node;\nimpl Super for Node {}\nimpl Sub for Node {}\n\nfn upcast(s: Box<dyn Sub>) -> Box<dyn Super> {\n    s\n}",
    options: [
      { label: 'A', text: "Explicit clone and reconstruction of the box" },
      { label: 'B', text: "Unsafe memory transmutation with std::mem" },
      { label: 'C', text: "Direct coercion via trait object upcasting" },
      { label: 'D', text: "Runtime reflection lookup in the metadata" },
    ],
    correctIndex: 2,
    hint: "Trait object upcasting allows coercing dyn Sub to dyn Super directly.",
    explanation: "Modern Rust supports Trait Object Upcasting, allowing a `dyn Sub` (where `trait Sub: Super`) to be implicitly coerced to `dyn Super` by adjusting the vtable pointer.",
  },
  {
    id: "trait-supporter-2",
    categorySlug: "traits",
    title: "Object Safety and Sized Bound",
    prompt: "Why does adding `where Self: Sized` on a trait method preserve object safety for the trait?",
    tags: ["traits","object-safety","sized"],
    difficulty: 3,
    language: 'rust',
    code: "trait Builder {\n    fn build(self) -> Vec<u8> where Self: Sized;\n    fn name(&self) -> &str;\n}",
    options: [
      { label: 'A', text: "It forces all implementing structs to be dynamically sized in code" },
      { label: 'B', text: "It converts the method into a macro during compilation in code" },
      { label: 'C', text: "It enables multithreaded dispatch for dynamic calls in runtime memory" },
      { label: 'D', text: "It removes the non-object-safe method from the trait vtable" },
    ],
    correctIndex: 3,
    hint: "Methods with Self: Sized bounds are omitted from dynamic dispatch vtables.",
    explanation: "Methods taking `self` by value or generic parameters violate object safety. Adding `where Self: Sized` excludes that method from the `dyn Trait` vtable while keeping it available on concrete sized types.",
  },
  {
    id: "trait-supporter-3",
    categorySlug: "traits",
    title: "Associated Type in Trait Implementation",
    prompt: "What does defining `type Item;` in a trait require of implementing structs?",
    tags: ["traits","associated-types","defaults"],
    difficulty: 2,
    language: 'rust',
    code: "trait Container {\n    type Item;\n    fn get(&self) -> Self::Item;\n}\n\nstruct ByteBox;\nimpl Container for ByteBox {\n    type Item = u8;\n    fn get(&self) -> u8 { 42 }\n}",
    options: [
      { label: 'A', text: "Implementors must specify the concrete `type Item` in their `impl` block" },
      { label: 'B', text: "All implementors are strictly forced to use 64-bit integer types in runtime memory" },
      { label: 'C', text: "The associated type is automatically allocated on the heap buffer in runtime memory" },
      { label: 'D', text: "The compiler converts the trait into an abstract virtual class in runtime memory" },
    ],
    correctIndex: 0,
    hint: "Associated type defaults provide a fallback type when omitted in impl blocks.",
    explanation: "Associated types require each implementing type to specify a concrete type (`type Item = ...;`) in its `impl` block.",
  },
  {
    id: "trait-supporter-4",
    categorySlug: "traits",
    title: "Disambiguation with Fully Qualified Syntax",
    prompt: "How do you call `TraitA::ping` when both `TraitA` and `TraitB` define `ping`?",
    tags: ["traits","syntax","disambiguation"],
    difficulty: 2,
    language: 'rust',
    code: "trait TraitA { fn ping(&self); }\ntrait TraitB { fn ping(&self); }\n\nstruct Worker;\nimpl TraitA for Worker { fn ping(&self) { println!(\"A\"); } }\nimpl TraitB for Worker { fn ping(&self) { println!(\"B\"); } }\n\nfn main() {\n    let w = Worker;\n    <Worker as TraitA>::ping(&w);\n}",
    options: [
      { label: 'A', text: "By renaming the method in the struct definition module in code" },
      { label: 'B', text: "Using fully qualified syntax `<Worker as TraitA>::ping(&w)`" },
      { label: 'C', text: "By casting `w` with `unsafe { std::mem::transmute(w) }` in code" },
      { label: 'D', text: "Using runtime dynamic dispatch `(&w as &dyn TraitA).ping()`" },
    ],
    correctIndex: 1,
    hint: "Fully qualified syntax <Type as Trait>::method unambiguously specifies the trait.",
    explanation: "When multiple implemented traits have overlapping method names, Fully Qualified Syntax `<Type as Trait>::method(...)` unambiguously tells the compiler which trait method to call.",
  },
  {
    id: "trait-supporter-5",
    categorySlug: "traits",
    title: "Inherent Method Priority",
    prompt: "When a type has an inherent method and a trait method with the same name, which one wins?",
    tags: ["traits","inherent-methods","precedence"],
    difficulty: 2,
    language: 'rust',
    code: "trait Greet { fn hello(&self); }\n\nstruct Person;\nimpl Person {\n    fn hello(&self) { println!(\"inherent\"); }\n}\nimpl Greet for Person {\n    fn hello(&self) { println!(\"trait\"); }\n}\n\nfn main() {\n    Person.hello();\n}",
    options: [
      { label: 'A', text: "The trait method takes precedence and prints \"trait\" in code" },
      { label: 'B', text: "A compiler ambiguity error is produced at the call site in code" },
      { label: 'C', text: "The inherent method takes precedence and prints \"inherent\"" },
      { label: 'D', text: "Both methods execute sequentially in declaration order in code" },
    ],
    correctIndex: 2,
    hint: "Inherent methods always take priority over trait methods in method lookup.",
    explanation: "In Rust's method resolution order, inherent methods are checked first before trait methods in scope. Therefore `Person.hello()` calls the inherent implementation.",
  },
  {
    id: "trait-supporter-6",
    categorySlug: "traits",
    title: "Orphan Rule and Local Types",
    prompt: "Under Rust's orphan rules, when can you implement a trait for a type?",
    tags: ["traits","orphan-rules","coherence"],
    difficulty: 3,
    language: 'rust',
    code: "// impl ForeignTrait for ForeignType { ... } // Error",
    options: [
      { label: 'A', text: "Only when both the trait and type are declared in the same file during execution" },
      { label: 'B', text: "Whenever the trait is marked with the public export attribute in code" },
      { label: 'C', text: "Only if the type implements the core Sized and Sync traits in code" },
      { label: 'D', text: "When either the trait or the type is local to the current crate" },
    ],
    correctIndex: 3,
    hint: "At least one of the trait or type must be defined in the current crate.",
    explanation: "The orphan rule ensures coherence: you can implement trait `T` for type `U` only if either `T` or `U` is defined in your local crate, preventing conflicting implementations across independent crates.",
  },
  {
    id: "trait-supporter-7",
    categorySlug: "traits",
    title: "Blanket Trait Implementation",
    prompt: "What is a blanket implementation in Rust?",
    tags: ["traits","blanket-impl","generics"],
    difficulty: 2,
    language: 'rust',
    code: "trait Describe { fn describe(&self) -> String; }\n\nimpl<T: std::fmt::Display> Describe for T {\n    fn describe(&self) -> String {\n        format!(\"Value: {}\", self)\n    }\n}",
    options: [
      { label: 'A', text: "An implementation of a trait for any type satisfying given bounds" },
      { label: 'B', text: "An implementation that replaces all default trait method bodies in code" },
      { label: 'C', text: "A macro that stamps out implementations for every primitive in code" },
      { label: 'D', text: "An unsafe implementation that bypasses memory borrow checks in code" },
    ],
    correctIndex: 0,
    hint: "impl<T: Bound> Trait for T implements Trait for all types satisfying the bound.",
    explanation: "A blanket implementation implements a trait for any type `T` that satisfies certain trait bounds (e.g. `ToString` for any `T: Display`).",
  },
  {
    id: "trait-supporter-8",
    categorySlug: "traits",
    title: "AsRef vs Borrow",
    prompt: "How does the `Borrow` trait differ from `AsRef`?",
    tags: ["traits","asref","borrow"],
    difficulty: 3,
    language: 'rust',
    code: "use std::borrow::Borrow;\n\nfn lookup<K: Borrow<str>>(map: &std::collections::HashMap<String, i32>, key: K) -> Option<&i32> {\n    map.get(key.borrow())\n}",
    options: [
      { label: 'A', text: "`Borrow` creates heap clones while `AsRef` is zero-cost in runtime memory" },
      { label: 'B', text: "`Borrow` guarantees identical `Hash`, `Eq`, and `Ord` semantics" },
      { label: 'C', text: "`AsRef` is only implemented for primitive numeric types in runtime memory" },
      { label: 'D', text: "`Borrow` requires unsafe code blocks to access inner data in code" },
    ],
    correctIndex: 1,
    hint: "Borrow requires the borrowed form to have matching Eq and Hash implementations.",
    explanation: "`Borrow<Borrowed>` requires that `Eq`, `Ord`, and `Hash` for a borrowed value act identically to the owned value, which is crucial for collections like `HashMap` and `BTreeSet`. `AsRef` makes no such behavioral guarantees.",
  },
  {
    id: "trait-supporter-9",
    categorySlug: "traits",
    title: "GATs with Lifetime Constraints",
    prompt: "What does `type Item<'a> where Self: 'a;` enable in GAT definitions?",
    tags: ["traits","gats","lifetimes"],
    difficulty: 3,
    language: 'rust',
    code: "trait Producer {\n    type Item<'a> where Self: 'a;\n    fn produce<'a>(&'a self) -> Self::Item<'a>;\n}",
    options: [
      { label: 'A', text: "It converts `Item` into a static reference across all threads in code" },
      { label: 'B', text: "It mandates that `Item` must implement the Send and Sync traits in code" },
      { label: 'C', text: "It guarantees that `Item<'a>` cannot outlive the borrowing `Self`" },
      { label: 'D', text: "It enables parallel evaluation of associated types in rustc in code" },
    ],
    correctIndex: 2,
    hint: "The where Self: 'a clause ensures the associated type is valid for the borrow of Self.",
    explanation: "The clause `where Self: 'a` ensures that `Item<'a>` is only well-formed when `Self` outlives 'a, preventing the associated type from referencing deallocated parts of `Self`.",
  },
  {
    id: "trait-supporter-10",
    categorySlug: "traits",
    title: "Trait Object Dispatch Mechanism",
    prompt: "What two pointers comprise a trait object `&dyn Trait` in memory (fat pointer)?",
    tags: ["traits","dyn","vtable"],
    difficulty: 2,
    language: 'rust',
    code: "trait Greeter { fn greet(&self); }\nfn call(g: &dyn Greeter) {\n    g.greet();\n}",
    options: [
      { label: 'A', text: "A stack pointer and a heap allocator memory pointer during execution" },
      { label: 'B', text: "A function pointer and an atomic reference count pointer" },
      { label: 'C', text: "Two identical data pointers for CPU redundancy verification" },
      { label: 'D', text: "A data pointer to the instance and a vtable pointer" },
    ],
    correctIndex: 3,
    hint: "Fat pointers contain a pointer to data and a pointer to the vtable.",
    explanation: "A trait object reference `&dyn Trait` is a 16-byte fat pointer containing: 1) a pointer to the concrete data, and 2) a pointer to the virtual method table (vtable) containing function pointers and drop glue.",
  },
  {
    id: "trait-supporter-11",
    categorySlug: "traits",
    title: "Super Trait Syntax",
    prompt: "What does `trait SubTrait: SuperTrait` enforce on implementing types?",
    tags: ["traits","supertraits","bounds"],
    difficulty: 2,
    language: 'rust',
    code: "trait Animal { fn noise(&self); }\ntrait Dog: Animal { fn bark(&self); }\n\nstruct Beagle;\nimpl Animal for Beagle { fn noise(&self) { println!(\"woof\"); } }\nimpl Dog for Beagle { fn bark(&self) { self.noise(); } }",
    options: [
      { label: 'A', text: "Any type implementing `Dog` must also implement `Animal`" },
      { label: 'B', text: "`Dog` automatically generates default `Animal` implementations" },
      { label: 'C', text: "`Dog` methods override `Animal` methods in all scopes in code" },
      { label: 'D', text: "`Animal` is converted into a private internal submodule in code" },
    ],
    correctIndex: 0,
    hint: "A supertrait bound requires types implementing the sub-trait to also implement the supertrait.",
    explanation: "`trait Dog: Animal` states that `Animal` is a supertrait of `Dog`. Any concrete type implementing `Dog` is required by the compiler to also provide an `impl Animal` block.",
  },
  {
    id: "trait-supporter-12",
    categorySlug: "traits",
    title: "Trait Associated Constants",
    prompt: "How do associated constants in traits participate in const evaluation?",
    tags: ["traits","associated-constants","const"],
    difficulty: 2,
    language: 'rust',
    code: "trait Matrix {\n    const ROWS: usize;\n    const COLS: usize;\n}\n\nstruct Square;\nimpl Matrix for Square {\n    const ROWS: usize = 4;\n    const COLS: usize = 4;\n}",
    options: [
      { label: 'A', text: "They are initialized at runtime upon first trait invocation" },
      { label: 'B', text: "They are known at compile time and can parameterize arrays" },
      { label: 'C', text: "They are allocated as read-only pages in heap memory in code" },
      { label: 'D', text: "They cannot be accessed without an instance of the struct in code" },
    ],
    correctIndex: 1,
    hint: "Associated constants are evaluated at compile time.",
    explanation: "Associated constants in traits are evaluated during compilation and can be used in array size expressions, const generics, and static assertions (e.g. `[u8; Square::ROWS]`).",
  },
  {
    id: "trait-supporter-13",
    categorySlug: "traits",
    title: "From and Into Dual Implementation",
    prompt: "Why does implementing `From<T> for U` automatically provide `Into<U> for T`?",
    tags: ["traits","from","into"],
    difficulty: 2,
    language: 'rust',
    code: "struct Dollars(u32);\nimpl From<u32> for Dollars {\n    fn from(v: u32) -> Self { Dollars(v) }\n}\n\nfn main() {\n    let d: Dollars = 50u32.into();\n    println!(\"{}\", d.0);\n}",
    options: [
      { label: 'A', text: "The compiler rewrites all `.into()` calls into `.from()` during AST parsing in runtime memory" },
      { label: 'B', text: "The `From` trait is a macro that generates an `Into` implementation directly in runtime memory" },
      { label: 'C', text: "A standard library blanket implementation `impl<T, U> Into<U> for T where U: From<T>`" },
      { label: 'D', text: "Type inference generates virtual into methods in the binary symbol table in runtime memory" },
    ],
    correctIndex: 2,
    hint: "The standard library includes a blanket implementation of Into for any type implementing From.",
    explanation: "The Rust standard library provides a blanket implementation: `impl<T, U> Into<U> for T where U: From<T>`. Implementing `From` automatically grants the corresponding `Into` implementation.",
  },
  {
    id: "trait-supporter-14",
    categorySlug: "traits",
    title: "TryFrom Error Associated Type",
    prompt: "What associated type is required when implementing `TryFrom<T>`?",
    tags: ["traits","try-from","errors"],
    difficulty: 2,
    language: 'rust',
    code: "use std::convert::TryFrom;\n\nstruct PositiveI32(i32);\n\nimpl TryFrom<i32> for PositiveI32 {\n    type Error = &'static str;\n    fn try_from(value: i32) -> Result<Self, Self::Error> {\n        if value > 0 { Ok(PositiveI32(value)) } else { Err(\"must be positive\") }\n    }\n}",
    options: [
      { label: 'A', text: "`type Target` specifying the destination struct type during execution" },
      { label: 'B', text: "`type Item` defining the underlying payload type in code" },
      { label: 'C', text: "`type Output` specifying the Result wrapper type in code" },
      { label: 'D', text: "`type Error` defining the failure type on conversion" },
    ],
    correctIndex: 3,
    hint: "TryFrom requires type Error to represent conversion failures.",
    explanation: "`TryFrom<T>` defines `type Error;` and returns `Result<Self, Self::Error>`, allowing the implementor to choose a custom error type for fallible conversions.",
  },
  {
    id: "trait-supporter-15",
    categorySlug: "traits",
    title: "Drop and Copy Mutually Exclusive",
    prompt: "Why does the compiler forbid a type from implementing both `Drop` and `Copy`?",
    tags: ["traits","drop","copy"],
    difficulty: 3,
    language: 'rust',
    code: "// struct Bad;\n// impl Drop for Bad { fn drop(&mut self) {} }\n// impl Copy for Bad {} // compile error",
    options: [
      { label: 'A', text: "Bitwise copying would make it ambiguous when destructors should run" },
      { label: 'B', text: "Copy types cannot have any methods defined in impl blocks in runtime memory" },
      { label: 'C', text: "Drop types must be allocated exclusively on the heap buffer in runtime memory" },
      { label: 'D', text: "The compiler cannot generate debug symbols for both traits in runtime memory" },
    ],
    correctIndex: 0,
    hint: "If Copy types could Drop, every bitwise copy would require tracking and running drops.",
    explanation: "`Copy` types are duplicated via simple bitwise memory copies (`memcpy`). If a `Copy` type implemented `Drop`, the compiler would have no way to track how many copies exist or when to run their destructors.",
  },
  {
    id: "trait-supporter-16",
    categorySlug: "traits",
    title: "Marker Traits in Rust",
    prompt: "What distinguishes a marker trait (such as `Send` or `Sync`) from standard traits?",
    tags: ["traits","marker-traits","types"],
    difficulty: 2,
    language: 'rust',
    code: "trait MarkerTrait {}\nstruct Token;\nimpl MarkerTrait for Token {}",
    options: [
      { label: 'A', text: "Marker traits can only be implemented in unsafe blocks" },
      { label: 'B', text: "Marker traits have no methods or associated items" },
      { label: 'C', text: "Marker traits are executed at runtime during initialization" },
      { label: 'D', text: "Marker traits are stripped from the binary symbol table" },
    ],
    correctIndex: 1,
    hint: "Marker traits contain no methods and serve purely as compile-time type-level flags.",
    explanation: "Marker traits contain no methods or associated types. They serve purely to convey compile-time type-level properties and guarantees (e.g. `Send`, `Sync`, `Unpin`, `Copy`) to the compiler and type system.",
  },
  {
    id: "trait-supporter-17",
    categorySlug: "traits",
    title: "DerefMut Requires Deref",
    prompt: "Why is `Deref` a supertrait of `DerefMut` (`pub trait DerefMut: Deref`)?",
    tags: ["traits","deref","derefmut"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ops::{Deref, DerefMut};\n\nstruct SmartPtr<T>(T);\n\nimpl<T> Deref for SmartPtr<T> {\n    type Target = T;\n    fn deref(&self) -> &Self::Target { &self.0 }\n}\n\nimpl<T> DerefMut for SmartPtr<T> {\n    fn deref_mut(&mut self) -> &mut Self::Target { &mut self.0 }\n}",
    options: [
      { label: 'A', text: "Immutable dereferencing is compiled as an atomic sub-operation" },
      { label: 'B', text: "All mutable operations in Rust must return Result wrappers in code" },
      { label: 'C', text: "Mutable dereferencing requires sharing the same `Target` type" },
      { label: 'D', text: "DerefMut delegates method execution to background threads in code" },
    ],
    correctIndex: 2,
    hint: "DerefMut reuses the type Target defined by Deref.",
    explanation: "`DerefMut` relies on the `type Target` associated type declared in `Deref`. Therefore, any type implementing `DerefMut` must implement `Deref` with a matching target.",
  },
  {
    id: "trait-supporter-18",
    categorySlug: "traits",
    title: "Generic Type Parameter Default",
    prompt: "What does `trait Add<Rhs = Self>` declare for the default right-hand side type?",
    tags: ["traits","generics","defaults"],
    difficulty: 2,
    language: 'rust',
    code: "use std::ops::Add;\n\nstruct Meters(u32);\n\nimpl Add for Meters {\n    type Output = Meters;\n    fn add(self, rhs: Meters) -> Self::Output {\n        Meters(self.0 + rhs.0)\n    }\n}",
    options: [
      { label: 'A', text: "`Rhs` is forced to be a shared reference to `Self` in runtime memory" },
      { label: 'B', text: "`Rhs` is evaluated as a constant zero integer at runtime in code" },
      { label: 'C', text: "The compiler disables addition for mixed-type operations in code" },
      { label: 'D', text: "If unspecified, `Rhs` defaults to the implementor type `Self`" },
    ],
    correctIndex: 3,
    hint: "Rhs = Self defaults the generic operand to the type implementing the trait.",
    explanation: "`trait Add<Rhs = Self>` uses a generic parameter default. When implementing `Add for Meters` without specifying `<Rhs>`, `Rhs` defaults to `Self` (`Meters`).",
  },
  {
    id: "trait-supporter-19",
    categorySlug: "traits",
    title: "Trait Specialization Status",
    prompt: "Why is full trait specialization currently unstable in Rust?",
    tags: ["traits","specialization","soundness"],
    difficulty: 3,
    language: 'rust',
    code: "// default impl<T> MyTrait for T { ... }",
    options: [
      { label: 'A', text: "Soundness interactions with lifetime variance and subtyping" },
      { label: 'B', text: "Inability of LLVM to compile multiple trait functions in code" },
      { label: 'C', text: "Lack of support for virtual function tables in binaries in code" },
      { label: 'D', text: "High memory overhead when generating debug symbols in runtime memory" },
    ],
    correctIndex: 0,
    hint: "Specialization creates subtle soundness holes when lifetimes differ only at runtime.",
    explanation: "Full trait specialization is unstable because lifetime information is erased at compile time. Specializing based on types can create soundness holes where lifetime subtyping interacts with specialized implementations.",
  },
  {
    id: "trait-supporter-20",
    categorySlug: "traits",
    title: "Non-Object-Safe Generic Methods",
    prompt: "Why does a generic method `fn transform<T>(&self, x: T)` make a trait non-object-safe?",
    tags: ["traits","object-safety","generics"],
    difficulty: 3,
    language: 'rust',
    code: "// trait Bad { fn transform<T>(&self, x: T); }\n// fn call(b: &dyn Bad) {} // Error",
    options: [
      { label: 'A', text: "Generic methods can only be called from background worker threads" },
      { label: 'B', text: "The vtable would require infinitely many monomorphized slots" },
      { label: 'C', text: "Trait objects cannot pass pointers through CPU registers in code" },
      { label: 'D', text: "Generic parameters are incompatible with the Sized trait bound" },
    ],
    correctIndex: 1,
    hint: "A vtable must have a fixed, finite set of function pointers at compile time.",
    explanation: "Because `T` could be any type, the compiler cannot generate a finite vtable containing pointers to every possible monomorphized version of `transform<T>`. Thus, generic methods violate object safety unless guarded with `where Self: Sized`.",
  },
  {
    id: "trait-supporter-21",
    categorySlug: "traits",
    title: "Auto Traits Implementation",
    prompt: "How are auto traits (like `Send` and `Sync`) implemented for custom types?",
    tags: ["traits","auto-traits","concurrency"],
    difficulty: 2,
    language: 'rust',
    code: "struct SafeData {\n    id: u64,\n    name: String,\n}",
    options: [
      { label: 'A', text: "Only if explicitly declared with `#[derive(Send, Sync)]` in code" },
      { label: 'B', text: "Through manual unsafe implementation blocks exclusively in runtime memory" },
      { label: 'C', text: "Automatically derived if all constituent fields implement them" },
      { label: 'D', text: "By registering the type in the global runtime process table in code" },
    ],
    correctIndex: 2,
    hint: "Auto traits automatically propagate if all fields implement the trait.",
    explanation: "Auto traits like `Send` and `Sync` are automatically implemented for a struct if and only if all of its fields implement `Send` and `Sync`. Negative `impl !Send` blocks can opt out.",
  },
  {
    id: "trait-supporter-22",
    categorySlug: "traits",
    title: "Negative Trait Bounds",
    prompt: "How does a type explicitly opt out of an auto trait like `Send`?",
    tags: ["traits","negative-impl","send"],
    difficulty: 3,
    language: 'rust',
    code: "struct ThreadBound {\n    _marker: std::marker::PhantomData<*const ()>,\n}",
    options: [
      { label: 'A', text: "By adding the `#[no_send]` compiler attribute on the struct within local thread memory" },
      { label: 'B', text: "By implementing the `std::ops::Not` trait for the type within local thread memory" },
      { label: 'C', text: "By marking the struct declaration with the private keyword within local thread memory" },
      { label: 'D', text: "By containing a non-Send field like raw pointers or `PhantomData<*const ()>`" },
    ],
    correctIndex: 3,
    hint: "Containing a raw pointer (*const T or *mut T) prevents auto-derivation of Send/Sync.",
    explanation: "Raw pointers (`*const T`, `*mut T`) do not implement `Send` or `Sync`. Including `PhantomData<*const ()>` in a struct automatically opts the struct out of `Send` and `Sync`.",
  },
  {
    id: "trait-supporter-23",
    categorySlug: "traits",
    title: "FnOnce vs FnMut vs Fn Inheritance",
    prompt: "What is the inheritance relationship among `Fn`, `FnMut`, and `FnOnce`?",
    tags: ["traits","fn-traits","hierarchy"],
    difficulty: 2,
    language: 'rust',
    code: "fn invoke_fn<F: Fn()>(f: F) {\n    f();\n}",
    options: [
      { label: 'A', text: "`Fn` is a sub-trait of `FnMut`, which is a sub-trait of `FnOnce`" },
      { label: 'B', text: "`FnOnce` is a sub-trait of `FnMut`, which is a sub-trait of `Fn` during execution" },
      { label: 'C', text: "All three traits are independent and mutually exclusive in runtime memory" },
      { label: 'D', text: "`FnMut` is the supertrait of both `Fn` and `FnOnce` in runtime memory" },
    ],
    correctIndex: 0,
    hint: "Any closure that can be called repeatedly without mutation (Fn) can also be called as FnMut and FnOnce.",
    explanation: "The hierarchy is: `pub trait Fn(): FnMut()`, and `pub trait FnMut(): FnOnce()`. Therefore, any closure implementing `Fn` also implements `FnMut` and `FnOnce`.",
  },
  {
    id: "trait-supporter-24",
    categorySlug: "traits",
    title: "impl Trait in Return Position",
    prompt: "What is the primary benefit of `-> impl Trait` over `-> Box<dyn Trait>`?",
    tags: ["traits","impl-trait","performance"],
    difficulty: 2,
    language: 'rust',
    code: "fn make_iter() -> impl Iterator<Item = i32> {\n    (0..10).filter(|x| x % 2 == 0)\n}",
    options: [
      { label: 'A', text: "Ability to return multiple distinct concrete types conditionally" },
      { label: 'B', text: "Static dispatch with zero heap allocation overhead" },
      { label: 'C', text: "Automatic serialization across network socket boundaries" },
      { label: 'D', text: "Dynamic runtime replacement of the returned closure" },
    ],
    correctIndex: 1,
    hint: "impl Trait uses static monomorphization without allocating on the heap.",
    explanation: "`impl Trait` in return position uses static dispatch: the compiler determines the concrete type at compile time, avoiding the heap allocation and vtable indirection of `Box<dyn Trait>`.",
  },
  {
    id: "trait-supporter-25",
    categorySlug: "traits",
    title: "impl Trait Conditional Returns",
    prompt: "Why does returning different concrete types in branches fail for `-> impl Trait`?",
    tags: ["traits","impl-trait","types"],
    difficulty: 2,
    language: 'rust',
    code: "// fn bad(cond: bool) -> impl Iterator<Item = i32> {\n//     if cond { (0..5) } else { (0..10).step_by(2) }\n// } // Error",
    options: [
      { label: 'A', text: "Iterators cannot be evaluated inside conditional if expressions in code" },
      { label: 'B', text: "The step_by adapter requires heap allocation for state in runtime memory" },
      { label: 'C', text: "`impl Trait` requires exactly one underlying concrete return type" },
      { label: 'D', text: "Range types cannot implement the core Iterator trait in runtime memory" },
    ],
    correctIndex: 2,
    hint: "impl Trait is not dynamic dispatch; the function must return a single concrete type.",
    explanation: "`impl Trait` hides the concrete type name but still resolves to a single static type at compile time. Because `Range` and `StepBy<Range>` are different types, you must use `Box<dyn Iterator>` or `Either` to return different types.",
  },
  {
    id: "trait-supporter-26",
    categorySlug: "traits",
    title: "CoerceUnsized and Smart Pointers",
    prompt: "What compiler trait enables `MyBox<T>` to coerce into `MyBox<dyn Trait>`?",
    tags: ["traits","coerce-unsized","compiler-magic"],
    difficulty: 3,
    language: 'rust',
    code: "// impl<T: ?Sized + Unsize<U>, U: ?Sized> CoerceUnsized<MyBox<U>> for MyBox<T> {}",
    options: [
      { label: 'A', text: "Dynamic dispatch via vtable fat pointers" },
      { label: 'B', text: "Static dispatch via compiler monomorphization" },
      { label: 'C', text: "Asynchronous task scheduling via executors" },
      { label: 'D', text: "Thread-safe communication via channels" },
    ],
    correctIndex: 3,
    hint: "CoerceUnsized is the built-in trait for unsizing coercions on custom pointer types.",
    explanation: "`CoerceUnsized` is the compiler-supported trait that allows pointer-like structs (such as `Box`, `Rc`, `Arc`) to participate in unsizing coercions (e.g. `Box<T>` to `Box<dyn Trait>` or `Box<[T]>`).",
  },
  {
    id: "trait-supporter-27",
    categorySlug: "traits",
    title: "DispatchFromDyn for Custom Receivers",
    prompt: "What trait enables custom smart pointers to be used as method receivers on `dyn Trait`?",
    tags: ["traits","dispatch-from-dyn","receivers"],
    difficulty: 3,
    language: 'rust',
    code: "// struct CustomPtr<T: ?Sized>(Box<T>);",
    options: [
      { label: 'A', text: "Static dispatch via compiler monomorphization" },
      { label: 'B', text: "Dynamic dispatch via vtable fat pointers" },
      { label: 'C', text: "Asynchronous task scheduling via executors" },
      { label: 'D', text: "Thread-safe communication via channels" },
    ],
    correctIndex: 0,
    hint: "DispatchFromDyn is the compiler trait allowing types to act as dyn method receivers.",
    explanation: "`DispatchFromDyn` is the internal trait that allows a type to act as a method receiver for trait objects (like `self: CustomPtr<Self>`).",
  },
  {
    id: "trait-supporter-28",
    categorySlug: "traits",
    title: "Unpin Marker Trait",
    prompt: "What does the `Unpin` marker trait indicate about a type?",
    tags: ["traits","unpin","pin"],
    difficulty: 2,
    language: 'rust',
    code: "use std::marker::Unpin;\n\nfn check_unpin<T: Unpin>() {}\n\nfn main() {\n    check_unpin::<String>();\n    check_unpin::<i32>();\n}",
    options: [
      { label: 'A', text: "The type is allocated exclusively in non-movable page memory" },
      { label: 'B', text: "The type is safe to move in memory even after being pinned" },
      { label: 'C', text: "The type cannot be passed across asynchronous task boundaries" },
      { label: 'D', text: "The type requires manual heap deallocation via raw pointers" },
    ],
    correctIndex: 1,
    hint: "Almost all types in Rust implement Unpin automatically unless explicitly opted out.",
    explanation: "`Unpin` indicates that pinning has no effect: instances can be safely moved out of `Pin` wrappers because they contain no self-referential pointers.",
  },
  {
    id: "trait-supporter-29",
    categorySlug: "traits",
    title: "PhantomData and Send/Sync",
    prompt: "How does `PhantomData<std::rc::Rc<()>>` affect the `Send` implementation of a struct?",
    tags: ["traits","phantom-data","send"],
    difficulty: 2,
    language: 'rust',
    code: "use std::marker::PhantomData;\nuse std::rc::Rc;\n\nstruct NotSend {\n    _marker: PhantomData<Rc<()>>,\n}",
    options: [
      { label: 'A', text: "It forces the struct to be allocated on the thread-local heap in runtime memory" },
      { label: 'B', text: "It implements `Send` only when compiled in release optimization mode in code" },
      { label: 'C', text: "It prevents the struct from automatically implementing `Send` and `Sync`" },
      { label: 'D', text: "It converts the struct into a shared atomic reference counter in runtime memory" },
    ],
    correctIndex: 2,
    hint: "Rc is !Send and !Sync, so PhantomData<Rc<()>> makes the container !Send and !Sync.",
    explanation: "Because `Rc` is `!Send` and `!Sync`, including `PhantomData<Rc<()>>` causes the containing struct to also become `!Send` and `!Sync` by auto trait derivation rules.",
  },
  {
    id: "trait-supporter-30",
    categorySlug: "traits",
    title: "Dyn Trait Size at Compile Time",
    prompt: "What is `std::mem::size_of::<dyn Trait>()`?",
    tags: ["traits","dyn","unsized"],
    difficulty: 2,
    language: 'rust',
    code: "// trait Trait {}\n// let size = std::mem::size_of::<dyn Trait>(); // compile error",
    options: [
      { label: 'A', text: "Exactly 16 bytes representing data and vtable pointers within local thread memory" },
      { label: 'B', text: "8 bytes representing a single function pointer address within local thread memory" },
      { label: 'C', text: "0 bytes because traits carry no inherent memory footprint in runtime memory" },
      { label: 'D', text: "It fails to compile because `dyn Trait` is dynamically sized (`?Sized`)" },
    ],
    correctIndex: 3,
    hint: "dyn Trait itself is unsized; size_of requires Sized types.",
    explanation: "`dyn Trait` is a Dynamically Sized Type (DST) with unknown size at compile time. It can only be used behind pointers like `&dyn Trait` (which is 16 bytes). Calling `size_of::<dyn Trait>()` fails to compile because `size_of` requires `T: Sized`.",
  },
  {
    id: "trait-supporter-31",
    categorySlug: "traits",
    title: "Associated Type Equality Constraint",
    prompt: "What does the bound `T: Iterator<Item = u32>` enforce?",
    tags: ["traits","associated-types","constraints"],
    difficulty: 2,
    language: 'rust',
    code: "fn sum_u32<I>(iter: I) -> u32\nwhere\n    I: Iterator<Item = u32>,\n{\n    iter.sum()\n}",
    options: [
      { label: 'A', text: "`I` must be an iterator whose associated `Item` type is exactly `u32`" },
      { label: 'B', text: "`I` must convert all yielded items to u32 at runtime within local thread memory" },
      { label: 'C', text: "`I` must yield a fixed size array of 32 elements during runtime execution" },
      { label: 'D', text: "`I` must be an asynchronous stream running on Rayon within local thread memory" },
    ],
    correctIndex: 0,
    hint: "Item = u32 binds the associated type Item to a concrete type.",
    explanation: "The associated type equality constraint `Item = u32` restricts generic type `I` to implementations of `Iterator` where the associated type `Item` matches `u32`.",
  },
  {
    id: "trait-supporter-32",
    categorySlug: "traits",
    title: "Default Trait Method Overriding",
    prompt: "Can an `impl` block override a default method provided in a trait definition?",
    tags: ["traits","defaults","methods"],
    difficulty: 1,
    language: 'rust',
    code: "trait Greeter {\n    fn hello(&self) { println!(\"default hello\"); }\n}\n\nstruct Custom;\nimpl Greeter for Custom {\n    fn hello(&self) { println!(\"custom hello\"); }\n}",
    options: [
      { label: 'A', text: "No; default methods are permanently fixed in traits" },
      { label: 'B', text: "Yes; any implementor can provide a custom override" },
      { label: 'C', text: "Only if the trait is marked with the default keyword" },
      { label: 'D', text: "Only if the struct is marked with the unsafe keyword" },
    ],
    correctIndex: 1,
    hint: "Default method implementations in traits can be freely overridden by implementors.",
    explanation: "Default method implementations provide a fallback. Any implementing type can optionally override the default implementation to provide specialized or optimized behavior.",
  },
  {
    id: "trait-supporter-33",
    categorySlug: "traits",
    title: "Trait Associated Type vs Generic Parameter",
    prompt: "When should a trait use an associated type instead of a generic parameter?",
    tags: ["traits","associated-types","design"],
    difficulty: 3,
    language: 'rust',
    code: "// trait Iterator { type Item; ... } vs trait Iterator<Item> { ... }",
    options: [
      { label: 'A', text: "When a type needs multiple implementations for different types in code" },
      { label: 'B', text: "When the trait must be implemented exclusively for references in code" },
      { label: 'C', text: "When there is only one logical implementation per concrete type" },
      { label: 'D', text: "When all trait methods are marked with the const qualifier in code" },
    ],
    correctIndex: 2,
    hint: "Associated types model a 1-to-1 relationship; generics model a 1-to-many relationship.",
    explanation: "Use an associated type when there should only be one implementation of the trait per type (e.g. a `Vec<i32>` iterator only yields `i32`). Use a generic parameter when multiple implementations are desired (e.g. `From<T>` for multiple `T`).",
  },
  {
    id: "trait-supporter-34",
    categorySlug: "traits",
    title: "Pointer Coercion in dyn Trait",
    prompt: "Why does passing `&Box<dyn Trait>` to a function expecting `&dyn Trait` work?",
    tags: ["traits","deref-coercion","dyn"],
    difficulty: 2,
    language: 'rust',
    code: "trait Worker { fn work(&self); }\nstruct MyWorker;\nimpl Worker for MyWorker { fn work(&self) {} }\n\nfn execute(w: &dyn Worker) { w.work(); }\n\nfn main() {\n    let b: Box<dyn Worker> = Box::new(MyWorker);\n    execute(&*b);\n}",
    options: [
      { label: 'A', text: "The Box pointer is unpacked and cloned onto the call stack during execution" },
      { label: 'B', text: "Trait objects bypass standard reference borrowing rules in code" },
      { label: 'C', text: "The compiler translates the call into a C ABI function call" },
      { label: 'D', text: "Deref coercion coerces `&Box<dyn Worker>` to `&dyn Worker`" },
    ],
    correctIndex: 3,
    hint: "Deref coercion dereferences Box<T> to &T, which is &dyn Worker here.",
    explanation: "Because `Box<T>` implements `Deref<Target = T>`, `&Box<dyn Worker>` deref-coerces to `&dyn Worker`, allowing seamless function passing.",
  },
  {
    id: "trait-supporter-35",
    categorySlug: "traits",
    title: "Trait Bound in Struct Definition vs Impl Block",
    prompt: "Why is it idiomatic in Rust to put trait bounds on `impl` blocks rather than struct definitions?",
    tags: ["traits","idioms","bounds"],
    difficulty: 2,
    language: 'rust',
    code: "struct Container<T>(T);\n\nimpl<T: std::fmt::Display> Container<T> {\n    fn show(&self) {\n        println!(\"{}\", self.0);\n    }\n}",
    options: [
      { label: 'A', text: "To allow constructing `Container<T>` even when `T` does not implement `Display`" },
      { label: 'B', text: "Because struct definitions cannot syntactically accept where clauses in runtime memory" },
      { label: 'C', text: "To force the compiler to allocate generic containers on the heap in runtime memory" },
      { label: 'D', text: "To enable dynamic trait object conversion without vtable generation in runtime memory" },
    ],
    correctIndex: 0,
    hint: "Omitting bounds on struct definitions avoids repeating bounds across every impl block and constructor.",
    explanation: "Placing bounds on `impl` blocks allows constructing and manipulating `Container<T>` for any `T`, unlocking specific methods only when `T` satisfies the necessary trait bounds without restricting the struct itself.",
  },
  {
    id: "trait-code-impl-summary",
    categorySlug: "traits",
    title: "Summary Trait Implementation",
    difficulty: 1,
    language: 'rust',
    kind: 'coding',
    tags: ["traits", 'coding'],
    prompt: "Define a trait `Summary` with `fn summarize(&self) -> String;`. Implement `Summary` for `struct Article { pub title: String, pub author: String }` returning `\"{title} by {author}\"`.",
    code: "pub trait Summary {\n    fn summarize(&self) -> String;\n}\n\npub struct Article {\n    pub title: String,\n    pub author: String,\n}\n\nimpl Summary for Article {\n    fn summarize(&self) -> String {\n        format!(\"{} by {}\", self.title, self.author)\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let a = Article { title: String::from(\"The Rust Book\"), author: String::from(\"Steve & Carol\") };\n    assert_eq!(a.summarize(), \"The Rust Book by Steve & Carol\");\n    println!(\"test passed\");\n}\n",
    explanation: "Define a trait `Summary` with `fn summarize(&self) -> String;`. Implement `Summary` for `struct Article { pub title: String, pub author: String }` returning `\"{title} by {author}\"`. Review the test cases to verify all assertions.",
  },
  {
    id: "trait-code-impl-from",
    categorySlug: "traits",
    title: "From Tuple to Coordinate",
    difficulty: 1,
    language: 'rust',
    kind: 'coding',
    tags: ["traits", 'coding'],
    prompt: "Given `#[derive(Debug, PartialEq)] pub struct Coordinate { pub x: i32, pub y: i32 }`, implement `From<(i32, i32)>` for `Coordinate`.",
    code: "#[derive(Debug, PartialEq)]\npub struct Coordinate {\n    pub x: i32,\n    pub y: i32,\n}\n\nimpl From<(i32, i32)> for Coordinate {\n    fn from((x, y): (i32, i32)) -> Self {\n        Self { x, y }\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let c = Coordinate::from((10, 20));\n    assert_eq!(c, Coordinate { x: 10, y: 20 });\n    println!(\"test passed\");\n}\n",
    explanation: "Given `#[derive(Debug, PartialEq)] pub struct Coordinate { pub x: i32, pub y: i32 }`, implement `From<(i32, i32)>` for `Coordinate`. Review the test cases to verify all assertions.",
  },
  {
    id: "trait-code-generic-bounds",
    categorySlug: "traits",
    title: "Generic Greater Item",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["traits", 'coding'],
    prompt: "Implement a generic function `greater<T: Ord>(a: T, b: T) -> T` returning the strictly greater value (or `b` if equal).",
    code: "pub fn greater<T: Ord>(a: T, b: T) -> T {\n    if a > b { a } else { b }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    assert_eq!(greater(5, 10), 10);\n    assert_eq!(greater(\"zebra\", \"apple\"), \"zebra\");\n    println!(\"test passed\");\n}\n",
    explanation: "Implement a generic function `greater<T: Ord>(a: T, b: T) -> T` returning the strictly greater value (or `b` if equal). Review the test cases to verify all assertions.",
  },
  {
    id: "trait-code-default-derive",
    categorySlug: "traits",
    title: "Custom Default Implementation",
    difficulty: 1,
    language: 'rust',
    kind: 'coding',
    tags: ["traits", 'coding'],
    prompt: "Implement `Default` for `struct ServerConfig { pub host: String, pub port: u16, pub max_conns: usize }` setting host to `\"127.0.0.1\"`, port to `8080`, and max_conns to `1000`.",
    code: "#[derive(Debug, PartialEq)]\npub struct ServerConfig {\n    pub host: String,\n    pub port: u16,\n    pub max_conns: usize,\n}\n\nimpl Default for ServerConfig {\n    fn default() -> Self {\n        Self {\n            host: String::from(\"127.0.0.1\"),\n            port: 8080,\n            max_conns: 1000,\n        }\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let cfg = ServerConfig::default();\n    assert_eq!(cfg.host, \"127.0.0.1\");\n    assert_eq!(cfg.port, 8080);\n    assert_eq!(cfg.max_conns, 1000);\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `Default` for `struct ServerConfig { pub host: String, pub port: u16, pub max_conns: usize }` setting host to `\"127.0.0.1\"`, port to `8080`, and max_conns to `1000`. Review the test cases to verify all assertions.",
  },
  {
    id: "trait-code-deref",
    categorySlug: "traits",
    title: "Implement Deref for Wrapper",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["traits", 'coding'],
    prompt: "Implement `std::ops::Deref` for `pub struct Wrapped<T>(pub T)` targeting type `T`.",
    code: "use std::ops::Deref;\n\npub struct Wrapped<T>(pub T);\n\nimpl<T> Deref for Wrapped<T> {\n    type Target = T;\n\n    fn deref(&self) -> &Self::Target {\n        &self.0\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let w = Wrapped(String::from(\"inner\"));\n    assert_eq!(w.len(), 5);\n    assert_eq!(&*w, \"inner\");\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `std::ops::Deref` for `pub struct Wrapped<T>(pub T)` targeting type `T`. Review the test cases to verify all assertions.",
  },
  {
    id: "trait-code-add-trait",
    categorySlug: "traits",
    title: "Vector2D Addition",
    difficulty: 2,
    language: 'rust',
    kind: 'coding',
    tags: ["traits", 'coding'],
    prompt: "Implement `std::ops::Add` for `#[derive(Debug, PartialEq, Clone, Copy)] pub struct Vec2 { pub x: i32, pub y: i32 }`.",
    code: "use std::ops::Add;\n\n#[derive(Debug, PartialEq, Clone, Copy)]\npub struct Vec2 {\n    pub x: i32,\n    pub y: i32,\n}\n\nimpl Add for Vec2 {\n    type Output = Self;\n\n    fn add(self, rhs: Self) -> Self::Output {\n        Self {\n            x: self.x + rhs.x,\n            y: self.y + rhs.y,\n        }\n    }\n}",
    testHarness: "{{SOLUTION}}\n\nfn main() {\n    let a = Vec2 { x: 1, y: 2 };\n    let b = Vec2 { x: 3, y: 4 };\n    assert_eq!(a + b, Vec2 { x: 4, y: 6 });\n    println!(\"test passed\");\n}\n",
    explanation: "Implement `std::ops::Add` for `#[derive(Debug, PartialEq, Clone, Copy)] pub struct Vec2 { pub x: i32, pub y: i32 }`. Review the test cases to verify all assertions.",
  },
  {
    id: 'trait-supertrait-dyn-upcast-1',
    categorySlug: 'traits',
    title: 'Trait Object Upcasting Coercion',
    prompt: 'How does Rust support upcasting from `dyn SubTrait` to `dyn SuperTrait`?',
    tags: ['traits', 'dyn', 'upcasting'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'The trait object must be reconstructed using Box::new' },
      { label: 'B', text: 'Rust allows direct coercion from Subtrait to Supertrait' },
      { label: 'C', text: 'Upcasting dyn trait objects requires an unsafe transmute' },
      { label: 'D', text: 'The target supertrait must omit all associated constants' },
    ],
    correctIndex: 1,
    hint: 'Trait object upcasting allows converting dyn Sub to dyn Super directly.',
    explanation: 'Rust provides trait upcasting coercion, allowing references to `dyn SubTrait` to be implicitly coerced to `dyn SuperTrait` via embedded supertrait vtables.',
  },
  {
    id: 'trait-associated-const-bound-1',
    categorySlug: 'traits',
    title: 'Associated Constants in Traits',
    prompt: 'What guarantee is required when declaring `const ID: u32;` inside a trait?',
    tags: ['traits', 'associated-const'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'Associated constants cannot be used inside where bounds' },
      { label: 'B', text: 'The constant must be evaluated dynamically by runtime' },
      { label: 'C', text: 'Implementing types must mark all associated items pub' },
      { label: 'D', text: 'Every implementing type must define the constant value' },
    ],
    correctIndex: 3,
    hint: 'Associated constants without defaults must be provided by every implementor.',
    explanation: 'An associated constant without a default value must be defined as a compile-time constant by every type implementing the trait.',
  },
  {
    id: 'trait-object-safe-sized-self-1',
    categorySlug: 'traits',
    title: 'Self: Sized on Trait Methods',
    prompt: 'Why do traits with `where Self: Sized` on specific methods retain dyn object safety?',
    tags: ['traits', 'object-safety', 'sized'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'The method is excluded from dyn vtables when Self: Sized' },
      { label: 'B', text: 'The trait object can still call the method via dynamic' },
      { label: 'C', text: 'The method triggers a compiler warning on implementation' },
      { label: 'D', text: 'The trait becomes completely impossible to implement on' },
    ],
    correctIndex: 0,
    hint: 'Methods requiring Self: Sized are omitted from the vtable, keeping the trait dyn-safe.',
    explanation: 'Adding `where Self: Sized` to a method excludes it from the `dyn Trait` vtable, allowing the overall trait to remain object safe while providing non-dyn methods for sized types.',
  },
  {
    id: 'trait-gats-lifetime-param-1',
    categorySlug: 'traits',
    title: 'Generic Associated Types (GATs)',
    prompt: 'What fundamental capability do Generic Associated Types (GATs) introduce?',
    tags: ['traits', 'gats'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'Associated types must only depend on concrete type args' },
      { label: 'B', text: 'GATs require allocating the returned items on heap page' },
      { label: 'C', text: 'Associated types can carry their own generic parameters' },
      { label: 'D', text: 'GATs disable trait object creation for the entire crate' },
    ],
    correctIndex: 2,
    hint: 'GATs allow associated types like `type Item<\'a>;`.',
    explanation: 'GATs allow associated types to declare generic type or lifetime parameters (e.g. `type Item<\'a>;`), enabling lending iterators and generic collection abstractions.',
  },
  {
    id: 'trait-impl-trait-return-pos-1',
    categorySlug: 'traits',
    title: 'Return Position Impl Trait (RPIT)',
    prompt: 'What are the static dispatch properties of `fn produce() -> impl Trait`?',
    tags: ['traits', 'impl-trait', 'rpit'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'The return type is dynamically dispatched via fat vtable' },
      { label: 'B', text: 'The concrete type is unnameable but statically resolved' },
      { label: 'C', text: 'The returned value is boxed into an allocated heap node' },
      { label: 'D', text: 'Callers can choose whichever return type they want for fn' },
    ],
    correctIndex: 1,
    hint: 'impl Trait in return position is existential and monomorphized statically without heap allocation.',
    explanation: '`impl Trait` in return position hides the concrete type from caller signatures while monomorphizing statically with zero runtime vtable overhead.',
  },
  {
    id: 'trait-orphan-rule-blanket-1',
    categorySlug: 'traits',
    title: 'Trait Orphan Rule Fundamentals',
    prompt: 'What core constraint does the Rust orphan rule enforce for `impl<T> Trait for Type`?',
    tags: ['traits', 'orphan-rule', 'coherence'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'Either the trait or the implementing type must be local' },
      { label: 'B', text: 'Foreign traits can only be implemented inside std crate' },
      { label: 'C', text: 'Blanket implementations must specify unsafe extern C fn' },
      { label: 'D', text: 'The orphan rule only applies when compiling nightly tests' },
    ],
    correctIndex: 0,
    hint: 'You cannot implement a foreign trait for a foreign type in a third-party crate.',
    explanation: 'The orphan rule (trait coherence) requires that either the trait or the type being implemented must be defined in the current crate, preventing conflicting external implementations.',
  },
  {
    id: 'trait-coherence-covered-type-1',
    categorySlug: 'traits',
    title: 'Covered Types in Trait Coherence',
    prompt: 'What defines a "covered type" under Rust orphan rules for `impl<T> ForeignTrait for MyType<T>`?',
    tags: ['traits', 'coherence', 'orphan-rule'],
    difficulty: 3,
    language: 'rust',
    options: [
      { label: 'A', text: 'Generic parameters are covered only if bounded by Sized' },
      { label: 'B', text: 'Covered types disable all blanket trait implementations' },
      { label: 'C', text: 'A type is covered if it appears inside a local type ctor' },
      { label: 'D', text: 'Covered types must be exported publicly from root module' },
    ],
    correctIndex: 2,
    hint: 'A type parameter is covered if it appears within a local type constructor (e.g. Vec<T> in your crate).',
    explanation: 'Under RFC 2451 coherence rules, a type parameter `T` is "covered" if it appears as an argument to a local type constructor (like `MyStruct<T>`), allowing trait impls on foreign traits.',
  },
  {
    id: 'trait-marker-traits-zero-size-1',
    categorySlug: 'traits',
    title: 'Marker Traits and Zero Cost',
    prompt: 'What characterizes a marker trait (such as `Send`, `Sync`, or `Copy`) in Rust?',
    tags: ['traits', 'marker-traits'],
    difficulty: 1,
    language: 'rust',
    options: [
      { label: 'A', text: 'They have no methods or associated items at compile time' },
      { label: 'B', text: 'They can only be implemented on primitive scalar numbers' },
      { label: 'C', text: 'They allocate dynamic vtables in process executable text' },
      { label: 'D', text: 'They require the compiler to disable all optimizations' },
    ],
    correctIndex: 0,
    hint: 'Marker traits have an empty body and convey type properties to the compiler with zero runtime cost.',
    explanation: 'Marker traits declare no methods or associated items. They serve as zero-overhead compile-time tags indicating structural properties (e.g. thread safety or bitwise copyability).',
  },
  {
    id: 'trait-associated-type-defaults-1',
    categorySlug: 'traits',
    title: 'Associated Type Defaults',
    prompt: 'What capability do associated type defaults (`type Output = Self;`) provide in traits?',
    tags: ['traits', 'associated-types', 'defaults'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'Associated type defaults require an explicit const bound' },
      { label: 'B', text: 'Implementors can omit the associated type to use default' },
      { label: 'C', text: 'Defaults convert associated types into dynamic trait dyn' },
      { label: 'D', text: 'Associated type defaults are forbidden in standard traits' },
    ],
    correctIndex: 1,
    hint: 'Default associated types allow implementors to omit explicit type definitions when the default matches.',
    explanation: 'Associated type defaults allow trait definitions to specify a default type (e.g. `type Output = Self;` in `Add`), which implementors can omit unless customizing.',
  },
  {
    id: 'trait-dispatch-vtable-layout-1',
    categorySlug: 'traits',
    title: 'Dynamic Trait Object Fat Pointer Layout',
    prompt: 'What constitutes the internal memory representation of a `&dyn Trait` fat pointer?',
    tags: ['traits', 'dyn', 'vtable', 'fat-pointer'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'A pointer to heap memory and an atomic reference counter' },
      { label: 'B', text: 'A 64-bit integer hash code representing trait name token' },
      { label: 'C', text: 'A CPU register pair allocated by operating system kernel' },
      { label: 'D', text: 'A pointer to data instance and a pointer to static vtable' },
    ],
    correctIndex: 3,
    hint: 'A trait object fat pointer holds two words: data pointer and vtable pointer.',
    explanation: '`&dyn Trait` is a two-word fat pointer: the first word points to the concrete instance data, and the second points to the static vtable containing type size, align, drop, and method pointers.',
  },
  {
    id: 'trait-auto-traits-negative-impl-1',
    categorySlug: 'traits',
    title: 'Negative Trait Implementations',
    prompt: 'What does a negative trait implementation (`impl !Send for MyType`) declare?',
    tags: ['traits', 'auto-traits', 'negative-impl'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It explicitly opts a type out of an automatic auto trait' },
      { label: 'B', text: 'It converts the trait into an unsafe procedural attribute' },
      { label: 'C', text: 'It forces the compiler to inline all calls to that trait' },
      { label: 'D', text: 'It generates a runtime panic when the type is constructed' },
    ],
    correctIndex: 0,
    hint: 'Negative implementations opt out of auto traits like Send or Sync.',
    explanation: 'Negative trait implementations (`impl !Trait`) explicitly opt a type out of an auto trait (like `Send` or `Sync`), preventing the compiler from automatically deriving it.',
  },
  {
    id: 'trait-unsize-coercion-slice-1',
    categorySlug: 'traits',
    title: 'Unsize Coercion for Fixed Arrays',
    prompt: 'How does the compiler perform Unsize coercion from `&[T; N]` to `&[T]`?',
    tags: ['traits', 'unsize', 'coercion'],
    difficulty: 2,
    language: 'rust',
    options: [
      { label: 'A', text: 'It dynamically reallocates the array on the system heap' },
      { label: 'B', text: 'It clones each element into an intermediate vector struct' },
      { label: 'C', text: 'It adjusts pointer metadata to convert [T; N] into &[T]' },
      { label: 'D', text: 'It converts the array into a raw null-terminated pointer' },
    ],
    correctIndex: 2,
    hint: 'Unsize coercion attaches length metadata to the thin array pointer to create a fat slice pointer.',
    explanation: 'Unsize coercion converts a thin reference to a sized array `&[T; N]` into a fat slice reference `&[T]` by attaching the length `N` into pointer metadata with zero runtime cost.',
  },
]
